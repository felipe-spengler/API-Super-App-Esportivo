services:
  # Backend API (Laravel)
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: app-esportivo-api
    restart: always
    environment:
      APP_ENV: production
      APP_DEBUG: 'false'
      APP_KEY: ${APP_KEY}
      DB_CONNECTION: mysql
      DB_HOST: db
      DB_PORT: 3306
      DB_DATABASE: ${DB_DATABASE}
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
    depends_on:
      - db
    volumes:
      - storage_volume:/var/www/storage
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.api-http.rule=Host(`esportivo.techinteligente.site`) && (PathPrefix(`/api`) || PathPrefix(`/storage`))"
      - "traefik.http.routers.api-http.entrypoints=http"
      - "traefik.http.routers.api-http.middlewares=redirect-to-https"

      - "traefik.http.routers.api-https.rule=Host(`esportivo.techinteligente.site`) && (PathPrefix(`/api`) || PathPrefix(`/storage`))"
      - "traefik.http.routers.api-https.entrypoints=https"
      - "traefik.http.routers.api-https.tls=true"
      - "traefik.http.routers.api-https.tls.certresolver=letsencrypt"

      - "traefik.http.services.api.loadbalancer.server.port=80"

  # Reverb WebSocket Server
  reverb:
    image: app-esportivo-api
    restart: always
    command: sh -c "sleep 10 && php artisan reverb:start --host=0.0.0.0 --port=9090 2>&1 | tee /var/www/storage/logs/reverb.log"
    environment:
      APP_ENV: production
      APP_DEBUG: 'false'
      APP_KEY: ${APP_KEY}
      DB_CONNECTION: mysql
      DB_HOST: db
      DB_PORT: 3306
      DB_DATABASE: ${DB_DATABASE}
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      REVERB_APP_ID: ${REVERB_APP_ID}
      REVERB_APP_KEY: ${REVERB_APP_KEY}
      REVERB_APP_SECRET: ${REVERB_APP_SECRET}
      REVERB_HOST: ${REVERB_HOST}
      REVERB_PORT: 9090
      REVERB_SCHEME: https
      REVERB_SERVER_HOST: 0.0.0.0
      REVERB_SERVER_PORT: 9090
    depends_on:
      - db
      - api
    volumes:
      - storage_volume:/var/www/storage # mesmo volume do api → logs acessíveis em ambos
    expose:
      - "9090"
    logging:
      driver: "json-file"
      options:
        max-size: "10m" # máximo 10MB por arquivo de log do Docker
        max-file: "5" # mantém os últimos 5 arquivos rotacionados
    labels:
      # Labels do Traefik no padrão do Coolify
      - "traefik.enable=true"

      # HTTP -> HTTPS redirect
      - "traefik.http.middlewares.redirect-to-https.redirectscheme.scheme=https"
      - "traefik.http.routers.http-reverb.entryPoints=http"
      - "traefik.http.routers.http-reverb.middlewares=redirect-to-https"
      - "traefik.http.routers.http-reverb.rule=Host(`esportivo.techinteligente.site`) && PathPrefix(`/app`)"

      # HTTPS com WebSocket
      - "traefik.http.routers.https-reverb.entryPoints=https"
      - "traefik.http.routers.https-reverb.rule=Host(`esportivo.techinteligente.site`) && PathPrefix(`/app`)"
      - "traefik.http.routers.https-reverb.tls.certresolver=letsencrypt"
      - "traefik.http.routers.https-reverb.tls=true"

      # Service - Porta do Reverb
      - "traefik.http.services.reverb.loadbalancer.server.port=9090"

      # Conectar router ao service
      - "traefik.http.routers.http-reverb.service=reverb"
      - "traefik.http.routers.https-reverb.service=reverb"

  # Frontend Web (React)
  web:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL}
        VITE_REVERB_APP_KEY: ${REVERB_APP_KEY}
        VITE_REVERB_HOST: ${REVERB_HOST}
        VITE_REVERB_PORT: 443
        VITE_REVERB_SCHEME: https
    image: app-esportivo-web
    restart: always
    expose:
      - "80"
    depends_on:
      - api
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.web-http.rule=Host(`esportivo.techinteligente.site`)"
      - "traefik.http.routers.web-http.entrypoints=http"
      - "traefik.http.routers.web-http.middlewares=redirect-to-https"

      - "traefik.http.routers.web-https.rule=Host(`esportivo.techinteligente.site`)"
      - "traefik.http.routers.web-https.entrypoints=https"
      - "traefik.http.routers.web-https.tls=true"
      - "traefik.http.routers.web-https.tls.certresolver=letsencrypt"

      - "traefik.http.services.web.loadbalancer.server.port=80"

  # Database
  db:
    image: mariadb:10.6
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_USER: ${DB_USERNAME}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    volumes:
      - database_volume:/var/lib/mysql
    healthcheck:
      test: [ "CMD", "mysqladmin", "ping", "-h", "localhost" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # (Opcional) Acesso ao banco para debug
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    restart: always
    expose:
      - "80"
    environment:
      PMA_HOST: db
      #PMA_USER: ${DB_USERNAME}
      #PMA_PASSWORD: ${DB_PASSWORD}
      PMA_ABSOLUTE_URI: https://esportivo.techinteligente.site/db/
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.phpmyadmin-http.rule=Host(`esportivo.techinteligente.site`) && PathPrefix(`/db`)"
      - "traefik.http.routers.phpmyadmin-http.entrypoints=http"
      - "traefik.http.routers.phpmyadmin-http.middlewares=phpmyadmin-stripprefix,redirect-to-https"

      - "traefik.http.routers.phpmyadmin-https.rule=Host(`esportivo.techinteligente.site`) && PathPrefix(`/db`)"
      - "traefik.http.routers.phpmyadmin-https.entrypoints=https"
      - "traefik.http.routers.phpmyadmin-https.tls=true"
      - "traefik.http.routers.phpmyadmin-https.tls.certresolver=letsencrypt"
      - "traefik.http.routers.phpmyadmin-https.middlewares=phpmyadmin-stripprefix"

      - "traefik.http.middlewares.phpmyadmin-stripprefix.stripprefix.prefixes=/db"
      - "traefik.http.services.phpmyadmin.loadbalancer.server.port=80"
    depends_on:
      - db

volumes:
  storage_volume:
  database_volume:
