diff --git a/app/Http/Controllers/Admin/AdminProductController.php b/app/Http/Controllers/Admin/AdminProductController.php
index 822a325..c600b2c 100644
--- a/app/Http/Controllers/Admin/AdminProductController.php
+++ b/app/Http/Controllers/Admin/AdminProductController.php
@@ -4,20 +4,21 @@
 
 use App\Http\Controllers\Controller;
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
 use App\Models\Product;
 use Illuminate\Support\Facades\Storage;
+use Illuminate\Support\Facades\Log;
 use App\Services\AuditLogger;
 
 class AdminProductController extends Controller
 {
     /**
-     * Listar todos os produtos do clube do administrador
+     * List all products belonging to the administrator's club.
      */
-    public function index(Request $request)
+    public function index(Request $request): JsonResponse
     {
         $user = $request->user();
 
-        // Se for SuperAdmin e passar club_id, filtra por ele, senão pega do usuário
         $clubId = $user->club_id;
         if ($user->isSuperAdmin() && $request->has('club_id')) {
             $clubId = $request->club_id;
@@ -32,12 +33,12 @@ public function index(Request $request)
     }
 
     /**
-     * Criar novo produto
+     * Create a new product.
      */
-    public function store(Request $request)
+    public function store(Request $request): JsonResponse
     {
         $user = $request->user();
-        \Illuminate\Support\Facades\Log::info('Product store request', $request->all());
+        Log::info('Product store request', $request->all());
 
         $clubId = $user->club_id;
         if ($user->isSuperAdmin() && $request->has('club_id')) {
@@ -49,73 +50,72 @@ public function store(Request $request)
         }
 
         $validated = $request->validate([
-            'name' => 'required|string|max:255',
-            'description' => 'nullable|string',
-            'price' => 'required|numeric|min:0',
+            'name'           => 'required|string|max:255',
+            'description'    => 'nullable|string',
+            'price'          => 'required|numeric|min:0',
             'stock_quantity' => 'required|integer|min:0',
-            'image_url' => 'nullable|string', // Pode vir url direta ou path do upload
-            'variants' => 'nullable|array'
+            'image_url'      => 'nullable|string',
+            'variants'       => 'nullable|array'
         ]);
 
         $product = Product::create([
-            'club_id' => $clubId,
-            'name' => $validated['name'],
-            'description' => $validated['description'] ?? '',
-            'price' => $validated['price'],
+            'club_id'        => $clubId,
+            'name'           => $validated['name'],
+            'description'    => $validated['description'] ?? '',
+            'price'          => $validated['price'],
             'stock_quantity' => $validated['stock_quantity'],
-            'image_url' => $validated['image_url'] ?? null,
-            'variants' => $validated['variants'] ?? []
+            'image_url'      => $validated['image_url'] ?? null,
+            'variants'       => $validated['variants'] ?? []
         ]);
 
         AuditLogger::log('product.create', "Adicionou o produto '{$product->name}' à loja (ID: {$product->id})", [
             'product_id' => $product->id
         ]);
 
-        \Illuminate\Support\Facades\Log::info('Product created', ['id' => $product->id, 'image_url' => $product->image_url]);
+        Log::info('Product created', ['id' => $product->id, 'image_url' => $product->image_url]);
 
         return response()->json($product, 201);
     }
 
     /**
-     * Atualizar produto
+     * Update an existing product.
      */
-    public function update(Request $request, $id)
+    public function update(Request $request, int $id): JsonResponse
     {
         $user = $request->user();
-        \Illuminate\Support\Facades\Log::info("Product update request ID: {$id}", $request->all());
+        Log::info("Product update request ID: {$id}", $request->all());
 
         $product = Product::findOrFail($id);
 
-        // Security check
         if (!$user->isSuperAdmin() && $product->club_id !== $user->club_id) {
             return response()->json(['message' => 'Não autorizado'], 403);
         }
 
         $validated = $request->validate([
-            'name' => 'sometimes|required|string|max:255',
-            'description' => 'nullable|string',
-            'price' => 'sometimes|required|numeric|min:0',
+            'name'           => 'sometimes|required|string|max:255',
+            'description'    => 'nullable|string',
+            'price'          => 'sometimes|required|numeric|min:0',
             'stock_quantity' => 'sometimes|required|integer|min:0',
-            'image_url' => 'nullable|string',
-            'variants' => 'nullable|array'
+            'image_url'      => 'nullable|string',
+            'variants'       => 'nullable|array'
         ]);
 
         $product->update($validated);
 
         AuditLogger::log('product.update', "Editou o produto '{$product->name}' (ID: {$id})", [
             'product_id' => $id,
-            'changes' => array_keys($validated)
+            'changes'    => array_keys($validated)
         ]);
 
-        \Illuminate\Support\Facades\Log::info("Product updated ID: {$id}", ['image_url' => $product->image_url]);
+        Log::info("Product updated ID: {$id}", ['image_url' => $product->image_url]);
 
         return response()->json($product);
     }
 
     /**
-     * Remover produto
+     * Delete product.
      */
-    public function destroy(Request $request, $id)
+    public function destroy(Request $request, int $id): JsonResponse
     {
         $user = $request->user();
         $product = Product::findOrFail($id);
@@ -132,42 +132,33 @@ public function destroy(Request $request, $id)
     }
 
     /**
-     * Upload de imagem do produto
+     * Upload product image.
      */
-    public function uploadImage(Request $request)
+    public function uploadImage(Request $request): JsonResponse
     {
-        \Illuminate\Support\Facades\Log::info('Product image upload start');
+        Log::info('Product image upload start');
 
         $request->validate([
-            'image' => 'required|image|mimes:jpeg,png,jpg|max:4096' // Max 4MB
+            'image' => 'required|image|mimes:jpeg,png,jpg|max:4096'
         ]);
 
         if ($request->hasFile('image')) {
             $file = $request->file('image');
-            \Illuminate\Support\Facades\Log::info('File received', [
+            Log::info('File received', [
                 'name' => $file->getClientOriginalName(),
                 'size' => $file->getSize(),
                 'mime' => $file->getMimeType()
             ]);
 
             $path = $file->store('products', 'public');
-            $url = Storage::url($path);
-
-            \Illuminate\Support\Facades\Log::info('File stored', [
-                'path' => $path,
-                'url' => $url
-            ]);
-
-            // Retorna URL correta usando o helper Storage::url, compatível com S3 e outros drivers
-            // WORKAROUND: Forçar retorno relativo se o Storage::url estiver retornando full URL bugada
             $relativePath = '/storage/' . $path;
 
-            \Illuminate\Support\Facades\Log::info('Returning relative path', ['path' => $relativePath]);
+            Log::info('Returning relative path', ['path' => $relativePath]);
 
             return response()->json(['path' => $relativePath]);
         }
 
-        \Illuminate\Support\Facades\Log::warning('No file found in request');
+        Log::warning('No file found in request');
         return response()->json(['message' => 'Nenhum arquivo enviado'], 400);
     }
 }
diff --git a/app/Http/Controllers/Admin/ArtGeneratorController.php b/app/Http/Controllers/Admin/ArtGeneratorController.php
index 5d1ae43..362c242 100644
--- a/app/Http/Controllers/Admin/ArtGeneratorController.php
+++ b/app/Http/Controllers/Admin/ArtGeneratorController.php
@@ -4,13 +4,22 @@
 
 use App\Http\Controllers\Controller;
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
+use Illuminate\Http\Response as LaravelResponse;
 use App\Models\GameMatch;
-use Illuminate\Support\Str;
+use App\Models\Championship;
 use App\Models\SystemSetting;
-
+use App\Models\Club;
+use App\Models\Team;
+use App\Models\User;
+use App\Models\Category;
+use Illuminate\Support\Str;
 use App\Http\Controllers\Admin\Traits\ArtGdTrait;
 use App\Http\Controllers\Admin\Traits\ArtCardTrait;
 use App\Http\Controllers\Admin\Traits\ArtMatchTrait;
+use App\Http\Controllers\EventController;
+use App\Http\Controllers\ReceiptController;
+use Illuminate\Support\Facades\Log;
 
 class ArtGeneratorController extends Controller
 {
@@ -19,9 +28,9 @@ class ArtGeneratorController extends Controller
     }
     use ArtMatchTrait;
 
-    private $fontPath;
-    private $secondaryFontPath;
-    private $templatesPath;
+    private string $fontPath;
+    private string $secondaryFontPath;
+    private string $templatesPath;
 
     public function __construct()
     {
@@ -30,11 +39,10 @@ public function __construct()
         $this->templatesPath = public_path('assets/templates/');
     }
 
-    // -------------------------------------------------------------------------
-    // Template Management
-    // -------------------------------------------------------------------------
-
-    public function saveTemplate(Request $request)
+    /**
+     * Save an art template.
+     */
+    public function saveTemplate(Request $request): JsonResponse
     {
         $name = $request->input('name');
         $elements = $request->input('elements');
@@ -46,13 +54,14 @@ public function saveTemplate(Request $request)
         $data = ['elements' => $elements, 'canvas' => $canvas, 'bg_url' => $bgUrl, 'name' => $name];
 
         if ($championshipId) {
-            $championship = \App\Models\Championship::find($championshipId);
+            $championship = Championship::find($championshipId);
             if ($championship) {
                 $user = auth()->user();
                 if ($user && $user->club_id == $championship->club_id) {
                     $settings = $championship->art_settings ?? [];
-                    if (!isset($settings['templates']))
+                    if (!isset($settings['templates'])) {
                         $settings['templates'] = [];
+                    }
                     $settings['templates'][$key] = $data;
                     $championship->art_settings = $settings;
                     $championship->save();
@@ -64,11 +73,12 @@ public function saveTemplate(Request $request)
 
         $user = auth()->user();
         if ($user && $user->club_id) {
-            $club = \App\Models\Club::find($user->club_id);
+            $club = Club::find($user->club_id);
             if ($club) {
                 $settings = $club->art_settings ?? [];
-                if (!isset($settings['templates']))
+                if (!isset($settings['templates'])) {
                     $settings['templates'] = [];
+                }
                 $settings['templates'][$key] = $data;
                 $club->art_settings = $settings;
                 $club->save();
@@ -84,7 +94,10 @@ public function saveTemplate(Request $request)
         return response()->json(['message' => 'Template salvo com sucesso (Sistema)']);
     }
 
-    public function getTemplate(Request $request)
+    /**
+     * Get layout template settings.
+     */
+    public function getTemplate(Request $request): JsonResponse
     {
         $name = $request->query('name');
         $championshipId = $request->query('championship_id');
@@ -92,13 +105,13 @@ public function getTemplate(Request $request)
 
         $championship = null;
         if ($championshipId) {
-            $championship = \App\Models\Championship::find($championshipId);
+            $championship = Championship::find($championshipId);
         }
 
         $user = auth()->user();
         $club = null;
         if ($user && $user->club_id) {
-            $club = \App\Models\Club::find($user->club_id);
+            $club = Club::find($user->club_id);
         } elseif ($championship) {
             $club = $championship->club;
         }
@@ -129,15 +142,17 @@ public function getTemplate(Request $request)
             $sport = $request->query('sport');
             if ($sport) {
                 $cat = 'confronto';
-                $n = strtolower($name);
-                if (str_contains($n, 'programado'))
+                $n = strtolower($name ?? '');
+                if (str_contains($n, 'programado')) {
                     $cat = 'jogo_programado';
-                elseif (str_contains($n, 'craque') || str_contains($n, 'mvp'))
+                } elseif (str_contains($n, 'craque') || str_contains($n, 'mvp')) {
                     $cat = 'craque';
+                }
 
                 $bgFile = $this->getBackgroundFile($sport, $cat, $club, $championship);
-                if ($bgFile)
+                if ($bgFile) {
                     $responseTemplate['preview_bg_url'] = $this->pathToUrl($bgFile);
+                }
             }
 
             return response()->json($responseTemplate);
@@ -147,44 +162,48 @@ public function getTemplate(Request $request)
             $sport = $request->query('sport');
             if ($sport) {
                 $cat = 'confronto';
-                $n = strtolower($name);
-                if (str_contains($n, 'programado'))
+                $n = strtolower($name ?? '');
+                if (str_contains($n, 'programado')) {
                     $cat = 'jogo_programado';
-                elseif (str_contains($n, 'craque') || str_contains($n, 'mvp'))
+                } elseif (str_contains($n, 'craque') || str_contains($n, 'mvp')) {
                     $cat = 'craque';
+                }
 
                 $bgFile = $this->getBackgroundFile($sport, $cat, $club, $championship);
-                if ($bgFile)
+                if ($bgFile) {
                     $default['preview_bg_url'] = $this->pathToUrl($bgFile);
+                }
             }
             return response()->json($default);
         }
 
-        // Fallback Legacy BG
         if ($championship && !empty($championship->art_settings)) {
             $sport = $request->query('sport');
             if ($sport) {
                 $cat = 'confronto';
-                $n = strtolower($name);
-                if (str_contains($n, 'programado'))
+                $n = strtolower($name ?? '');
+                if (str_contains($n, 'programado')) {
                     $cat = 'jogo_programado';
-                elseif (str_contains($n, 'craque') || str_contains($n, 'mvp'))
+                } elseif (str_contains($n, 'craque') || str_contains($n, 'mvp')) {
                     $cat = 'craque';
+                }
                 $bg = $this->getBackgroundFile($sport, $cat, $club, $championship);
-                if ($bg)
+                if ($bg) {
                     return response()->json(['bg_url' => $this->pathToUrl($bg), 'elements' => null]);
+                }
             }
         }
 
         if ($club && !empty($club->art_settings)) {
             $cat = 'confronto';
-            $n = strtolower($name);
-            if (str_contains($n, 'programado'))
+            $n = strtolower($name ?? '');
+            if (str_contains($n, 'programado')) {
                 $cat = 'jogo_programado';
-            elseif (str_contains($n, 'craque') || str_contains($n, 'mvp'))
+            } elseif (str_contains($n, 'craque') || str_contains($n, 'mvp')) {
                 $cat = 'craque';
+            }
 
-            $sports = ['futebol', 'fut7', 'futebol 7', 'society', 'volei', 'futsal', 'basquete', 'handebol'];
+            $sports = ['futebol', 'fut7', 'society', 'volei', 'futsal', 'basquete', 'handebol'];
             $foundBg = null;
 
             foreach ($sports as $s) {
@@ -209,39 +228,41 @@ public function getTemplate(Request $request)
                 }
             }
 
-            if ($foundBg)
+            if ($foundBg) {
                 return response()->json(['bg_url' => $this->pathToUrl($foundBg), 'elements' => null]);
+            }
         }
 
         return response()->json(null);
     }
 
-    private function getTemplateKey($name)
+    /**
+     * Map layout friendly name to key.
+     */
+    private function getTemplateKey(?string $name): string
     {
         $map = [
-            'Craque do Jogo' => 'art_layout_mvp_vertical',
-            'Craque do Jogo (Geral)' => 'art_layout_mvp_vertical',
+            'Craque do Jogo'            => 'art_layout_mvp_vertical',
+            'Craque do Jogo (Geral)'    => 'art_layout_mvp_vertical',
             'Craque do Jogo (Vertical)' => 'art_layout_mvp_vertical',
-            'Jogo Programado' => 'art_layout_scheduled_feed',
-            'Jogo Programado (Feed)' => 'art_layout_scheduled_feed',
-            'Jogo Programado (Story)' => 'art_layout_scheduled_story',
-            'Confronto' => 'art_layout_faceoff',
-            'Confronto (Placar)' => 'art_layout_faceoff',
-            'Atleta Confirmado' => 'art_layout_individual_confirmed',
-            'Colocação do Atleta' => 'art_layout_individual_placement',
-            'Perna de Pau' => 'art_layout_perna_vertical',
-            'Perna de Pau (Geral)' => 'art_layout_perna_vertical',
-            'Defesa Menos Vazada' => 'art_layout_defense_vertical',
+            'Jogo Programado'           => 'art_layout_scheduled_feed',
+            'Jogo Programado (Feed)'    => 'art_layout_scheduled_feed',
+            'Jogo Programado (Story)'   => 'art_layout_scheduled_story',
+            'Confronto'                 => 'art_layout_faceoff',
+            'Confronto (Placar)'        => 'art_layout_faceoff',
+            'Atleta Confirmado'         => 'art_layout_individual_confirmed',
+            'Colocação do Atleta'       => 'art_layout_individual_placement',
+            'Perna de Pau'              => 'art_layout_perna_vertical',
+            'Perna de Pau (Geral)'      => 'art_layout_perna_vertical',
+            'Defesa Menos Vazada'       => 'art_layout_defense_vertical',
         ];
-        return $map[$name] ?? 'art_layout_custom_' . Str::slug($name);
+        return $map[$name] ?? 'art_layout_custom_' . Str::slug($name ?? 'default');
     }
 
-    // -------------------------------------------------------------------------
-    // Route Handlers (Públicos)
-    // -------------------------------------------------------------------------
-
-    /** Gera Arte de Confronto (Faceoff) */
-    public function matchFaceoff($matchId)
+    /**
+     * Generate confrontation art.
+     */
+    public function matchFaceoff(int $matchId)
     {
         $match = GameMatch::with(['homeTeam', 'awayTeam', 'championship.club', 'championship.sport', 'events.player'])->findOrFail($matchId);
         $club = $match->championship->club;
@@ -249,8 +270,10 @@ public function matchFaceoff($matchId)
         return $this->generateConfrontationArt($match, $club, $match->championship);
     }
 
-    /** Gera Arte de Jogo Programado */
-    public function matchScheduled($matchId)
+    /**
+     * Generate scheduled match art.
+     */
+    public function matchScheduled(int $matchId)
     {
         $match = GameMatch::with(['homeTeam', 'awayTeam', 'championship.club', 'championship.sport'])->findOrFail($matchId);
         $club = $match->championship->club;
@@ -258,13 +281,18 @@ public function matchScheduled($matchId)
         return $this->generateScheduledArt($match, $club, $match->championship);
     }
 
-    public function downloadScheduledArt($matchId)
+    /**
+     * Trigger scheduled match art download.
+     */
+    public function downloadScheduledArt(int $matchId)
     {
         return $this->matchScheduled($matchId);
     }
 
-    /** Gera Arte do MVP da Partida */
-    public function mvpArt($matchId, Request $request)
+    /**
+     * Generate MVP player card art.
+     */
+    public function mvpArt(int $matchId, Request $request)
     {
         $match = GameMatch::with(['homeTeam', 'awayTeam', 'mvp', 'championship', 'championship.sport'])->findOrFail($matchId);
         $category = $request->query('category', 'craque');
@@ -276,13 +304,18 @@ public function mvpArt($matchId, Request $request)
         return $this->generatePlayerArt($match->mvp, $match, $category);
     }
 
-    public function downloadMvpArt($matchId, Request $request)
+    /**
+     * Trigger MVP art download.
+     */
+    public function downloadMvpArt(int $matchId, Request $request)
     {
         return $this->mvpArt($matchId, $request);
     }
 
-    /** Gera Arte do Perna de Pau da Partida */
-    public function pernaArt($matchId, Request $request)
+    /**
+     * Generate Perna de Pau card art.
+     */
+    public function pernaArt(int $matchId, Request $request)
     {
         $match = GameMatch::with(['homeTeam', 'awayTeam', 'pernaDePau', 'championship', 'championship.sport'])->findOrFail($matchId);
         $category = $request->query('category', 'perna_de_pau');
@@ -294,25 +327,32 @@ public function pernaArt($matchId, Request $request)
         return $this->generatePlayerArt($match->pernaDePau, $match, $category);
     }
 
-    public function downloadPernaArt($matchId, Request $request)
+    /**
+     * Trigger Perna de Pau art download.
+     */
+    public function downloadPernaArt(int $matchId, Request $request)
     {
         return $this->pernaArt($matchId, $request);
     }
 
-    /** Gera Arte de Classificação */
-    public function standingsArt($championshipId, Request $request)
+    /**
+     * Generate standings board image art.
+     */
+    public function standingsArt(int $championshipId, Request $request)
     {
-        $championship = \App\Models\Championship::with(['sport', 'club'])->findOrFail($championshipId);
+        $championship = Championship::with(['sport', 'club'])->findOrFail($championshipId);
         $this->loadClubResources($championship->club);
 
         $sport = strtolower($championship->sport->name ?? 'futebol');
         $bgFile = $this->getBackgroundFile($sport, 'classificacao', $championship->club, $championship);
         $img = $this->initImage($bgFile);
 
-        if (!$img)
+        if (!$img) {
             $img = $this->initImage('fundo_confronto.jpg');
-        if (!$img)
+        }
+        if (!$img) {
             return response('Erro ao inicializar imagem de classificação.', 500);
+        }
 
         $club = $championship->club;
         $primaryColorStr = $club->primary_color ?? '#FFB700';
@@ -327,19 +367,20 @@ public function standingsArt($championshipId, Request $request)
         return $this->outputImage($img, 'classificacao_' . $championshipId);
     }
     
-    /** Gera Arte de Defesa Menos Vazada */
-    public function defenseArt($championshipId, Request $request)
+    /**
+     * Generate Best Defense image art.
+     */
+    public function defenseArt(int $championshipId, Request $request)
     {
-        $championship = \App\Models\Championship::with(['sport', 'club'])->findOrFail($championshipId);
+        $championship = Championship::with(['sport', 'club'])->findOrFail($championshipId);
         $this->loadClubResources($championship->club);
 
         $teamId = $request->query('team_id');
         $categoryId = $request->query('category_id');
         
         if (!$teamId) {
-            // Se não informou o time, vamos calcular o melhor no backend agora
             $statsRequest = new Request(['type' => 'defense', 'category_id' => $categoryId]);
-            $eventController = new \App\Http\Controllers\EventController();
+            $eventController = new EventController();
             $statsRes = $eventController->stats($statsRequest, $championshipId);
             $stats = json_decode($statsRes->getContent(), true);
             
@@ -349,24 +390,23 @@ public function defenseArt($championshipId, Request $request)
             $teamId = $stats[0]['id'];
         }
 
-        $team = \App\Models\Team::findOrFail($teamId);
+        $team = Team::findOrFail($teamId);
         $sport = strtolower($championship->sport->name ?? 'futebol');
         
-        // Vamos usar o createCard adaptado para times
-        // Mas o createCard espera um "Player" (User). 
-        // Vou passar um objeto genérico que simule o player, usando os dados do time.
         $fakePlayer = (object)[
-            'id' => $team->id,
-            'name' => $team->name,
-            'nickname' => $team->name,
-            'photo_path' => $team->logo_path,
-            'photo_url' => $team->logo_url
+            'id'                  => $team->id,
+            'name'                => $team->name,
+            'nickname'            => $team->name,
+            'photo_path'          => $team->logo_path,
+            'photo_url'           => $team->logo_url
         ];
 
         $catName = '';
         if ($categoryId) {
-            $cat = \App\Models\Category::find($categoryId);
-            if ($cat) $catName = $cat->name;
+            $cat = Category::find($categoryId);
+            if ($cat) {
+                $catName = $cat->name;
+            }
         }
 
         return $this->createCard($fakePlayer, $championship, $sport, 'defesa_menos_vazada', null, null, $team, $championship->club, [
@@ -375,12 +415,11 @@ public function defenseArt($championshipId, Request $request)
     }
 
     /**
-     * Gera Arte de Premiação do Campeonato (Melhor Goleiro, Artilheiro, etc.)
-     * Rota: /art/championship/{championshipId}/award/{awardType}?categoryId={id}
+     * Generate individual championship awards.
      */
-    public function championshipAwardArt($championshipId, $awardType, Request $request)
+    public function championshipAwardArt(int $championshipId, string $awardType, Request $request)
     {
-        $championship = \App\Models\Championship::with(['sport', 'club'])->findOrFail($championshipId);
+        $championship = Championship::with(['sport', 'club'])->findOrFail($championshipId);
         $this->loadClubResources($championship->club);
 
         $categoryId = $request->query('categoryId');
@@ -395,7 +434,6 @@ public function championshipAwardArt($championshipId, $awardType, Request $reque
             $targetAward = $awards['generic'][$awardType];
         }
 
-        // Fallback backward compatibility for array flat
         if (!$targetAward && is_array($awards)) {
             foreach ($awards as $key => $award) {
                 if (is_array($award) && isset($award['category']) && $award['category'] === $awardType) {
@@ -416,22 +454,20 @@ public function championshipAwardArt($championshipId, $awardType, Request $reque
         }
 
         $player = null;
-        $team = !empty($targetAward['team_id']) ? \App\Models\Team::find($targetAward['team_id']) : null;
+        $team = !empty($targetAward['team_id']) ? Team::find($targetAward['team_id']) : null;
 
         if (!empty($targetAward['player_id'])) {
-            $player = \App\Models\User::find($targetAward['player_id']);
+            $player = User::find($targetAward['player_id']);
         }
 
         if (!$player && $team) {
-            // Simulated fake player representing the team to render cleanly
             $player = (object)[
-                'id' => 0,
-                'name' => $team->name,
-                'nickname' => $team->name,
-                'photo_path' => $team->logo_path,
+                'id'                  => 0,
+                'name'                => $team->name,
+                'nickname'            => $team->name,
+                'photo_path'          => $team->logo_path,
                 'photo_path_original' => $team->logo_path
             ];
-            // Override the category to ensure it triggers team layout key
             $awardType = 'premiação_equipe';
         }
 
@@ -450,57 +486,53 @@ public function championshipAwardArt($championshipId, $awardType, Request $reque
     }
 
     /**
-     * Gera Arte Individual de Atleta (Confirmado ou Colocação)
+     * Generate individual athlete image art card.
      */
-    public function individualAthleteArt($championshipId, $athleteId, $category, Request $request)
+    public function individualAthleteArt(int $championshipId, int $athleteId, string $category, Request $request)
     {
-        $championship = \App\Models\Championship::with(['sport', 'club'])->findOrFail($championshipId);
-        $athlete = \App\Models\User::findOrFail($athleteId);
+        $championship = Championship::with(['sport', 'club'])->findOrFail($championshipId);
+        $athlete = User::findOrFail($athleteId);
 
         $this->loadClubResources($championship->club);
         $sport = strtolower($championship->sport->name ?? 'futebol');
 
-        // Extra replacements for individual context
         $rank = $request->query('rank', '');
         $catName = $request->query('category_name', '');
 
-        // We'll use createCard but we need to pass these replacements.
-        // Since createCard builds its own replacements, we might need a way to inject these.
-        // I've already updated createCard to use some default empty strings for these.
-        // I'll use a little trick: I'll store them in a temporary property or use a more direct approach if possible.
-
-        // Let's modify createCard to accept an optional $extraReplacements array as a better long-term fix.
-        // For now, I'll pass them via a "match" object or similar if it works, or I'll just update createCard signature.
-
         return $this->createCard($athlete, $championship, $sport, $category, null, null, null, $championship->club, [
             '{COLOCACAO}' => $rank,
             '{CATEGORIA}' => mb_strtoupper($catName)
         ]);
     }
 
-    /** Legacy Wrapper (antigo downloadArt) */
-    public function downloadArt($matchId, Request $request)
+    /**
+     * Download wrapper.
+     */
+    public function downloadArt(int $matchId, Request $request)
     {
         return $this->mvpArt($matchId, $request);
     }
 
-    // -------------------------------------------------------------------------
-    // Utilitários de URL/Path
-    // -------------------------------------------------------------------------
-
-    private function pathToUrl($path)
+    /**
+     * Path converter helper.
+     */
+    private function pathToUrl(?string $path): ?string
     {
-        if (empty($path))
+        if (empty($path)) {
             return null;
-        if (str_contains($path, 'http'))
+        }
+        if (str_contains($path, 'http')) {
             return $path;
+        }
 
         $path = str_replace('\\', '/', $path);
 
-        if (file_exists(public_path('assets/templates/' . $path)))
+        if (file_exists(public_path('assets/templates/' . $path))) {
             return url('api/assets-templates/' . $path);
-        if (file_exists(public_path('assets-templates/' . $path)))
+        }
+        if (file_exists(public_path('assets-templates/' . $path))) {
             return url('api/assets-templates/' . $path);
+        }
 
         $clean = $path;
         $prefixes = [
@@ -520,6 +552,9 @@ private function pathToUrl($path)
         return url('api/storage/' . ltrim($clean, '/'));
     }
 
+    /**
+     * Fetch default templates fallback config.
+     */
     protected function getDefaultTemplate($key)
     {
         if ($key === 'art_layout_defense_vertical') {
@@ -533,7 +568,7 @@ protected function getDefaultTemplate($key)
                 ],
                 "canvas" => ["width" => 1080, "height" => 1920],
                 "bg_url" => null,
-                "name" => "Defesa Menos Vazada"
+                "name"   => "Defesa Menos Vazada"
             ];
         }
 
@@ -548,7 +583,7 @@ protected function getDefaultTemplate($key)
                 ],
                 "canvas" => ["width" => 1080, "height" => 1920],
                 "bg_url" => null,
-                "name" => "Premiação (Equipe)"
+                "name"   => "Premiação (Equipe)"
             ];
         }
 
@@ -564,7 +599,7 @@ protected function getDefaultTemplate($key)
                 ],
                 "canvas" => ["width" => 1080, "height" => 1920],
                 "bg_url" => null,
-                "name" => "Premiação (Jogador)"
+                "name"   => "Premiação (Jogador)"
             ];
         }
 
@@ -580,7 +615,7 @@ protected function getDefaultTemplate($key)
                 ],
                 "canvas" => ["width" => 1080, "height" => 1920],
                 "bg_url" => null,
-                "name" => "Melhor Tempo"
+                "name"   => "Melhor Tempo"
             ];
         }
 
@@ -596,7 +631,7 @@ protected function getDefaultTemplate($key)
                 ],
                 "canvas" => ["width" => 1080, "height" => 1920],
                 "bg_url" => null,
-                "name" => "Melhor Volta"
+                "name"   => "Melhor Volta"
             ];
         }
 
@@ -611,18 +646,21 @@ protected function getDefaultTemplate($key)
                 ],
                 "canvas" => ["width" => 1080, "height" => 1920],
                 "bg_url" => null,
-                "name" => "Bateria Programada"
+                "name"   => "Bateria Programada"
             ];
         }
 
-        // Fallback to traits
         return $this->getTraitDefaultTemplate($key);
     }
 
-    private function urlToPath($url)
+    /**
+     * Map URL helper to public folder templates path.
+     */
+    private function urlToPath(?string $url): ?string
     {
-        if (empty($url))
+        if (empty($url)) {
             return null;
+        }
         $parsed = parse_url($url);
         $path = $parsed['path'] ?? $url;
         $path = urldecode($path);
@@ -638,13 +676,12 @@ private function urlToPath($url)
         return $path;
     }
 
-    // -------------------------------------------------------------------------
-    // Debug
-    // -------------------------------------------------------------------------
-
-    public function debugPlayerArt($playerId)
+    /**
+     * Generate HTML debug template view.
+     */
+    public function debugPlayerArt(int $playerId): LaravelResponse
     {
-        $player = \App\Models\User::findOrFail($playerId);
+        $player = User::findOrFail($playerId);
 
         $html = "<!DOCTYPE html><html><head><title>Debug Player Art</title><style>
                  body { font-family: sans-serif; background: #f0f0f0; margin: 20px; }
@@ -665,7 +702,6 @@ public function debugPlayerArt($playerId)
 
         $html .= "<h1>Debug: Player " . e($player->name) . " (ID: " . (int)$player->id . ")</h1>";
 
-        // 1. Original
         $html .= "<div class='box'><h2>1. Imagem Original Salva (photo_path)</h2>";
         $html .= "<p>DB Path: <code>" . e($player->photo_path ?: 'NULL') . "</code></p>";
         if ($player->photo_path) {
@@ -674,7 +710,6 @@ public function debugPlayerArt($playerId)
         }
         $html .= "</div>";
 
-        // 2. _nobg Version
         $nobgPath = $player->photo_path ? preg_replace('/\.[^.]+$/i', '_nobg.png', $player->photo_path) : null;
         $html .= "<div class='box'><h2>2. Versão Sem Fundo (_nobg.png)</h2>";
         $html .= "<a href='?run_rembg=1' style='display:inline-block; padding:8px 15px; background:blue; color:white; text-decoration:none; border-radius:4px; margin-bottom:10px;'>Executar REMBG Novamente Agora</a>";
@@ -686,9 +721,9 @@ public function debugPlayerArt($playerId)
                 $inputAbsPath = storage_path('app/public/' . $player->photo_path);
                 $outputAbsPath = storage_path('app/public/players/' . pathinfo($nobgPath, PATHINFO_BASENAME));
                 $processed = $this->runRembgAndGetPath($inputAbsPath, pathinfo($outputAbsPath, PATHINFO_BASENAME));
-                $html .= "<p><b>Resultado:</b> " . ($processed ? "Sucesso! arquivo: " . e($processed) : "Falhou. Ver logs Laravel.") . "</p>";
+                $html .= "<p><b>Resultado:</b> " . ($processed ? "Sucesso! arquivo: " . e($processed) : "Falhou. Ver logs.") . "</p>";
             } catch (\Exception $e) {
-                \Illuminate\Support\Facades\Log::error("Erro rembg: " . $e->getMessage());
+                Log::error("Erro rembg: " . $e->getMessage());
                 $html .= "<p style='color:red'>Erro ao processar imagem. Verifique os logs.</p>";
             }
             $html .= "</div>";
@@ -696,16 +731,13 @@ public function debugPlayerArt($playerId)
 
         $html .= "<p>Expected Path: <code>" . e($nobgPath) . "</code></p>";
         $hasNobg = false;
-        $fileToLoad = null;
 
         if ($nobgPath) {
             $p1 = storage_path('app/public/' . $nobgPath);
             $p2 = public_path('storage/' . $nobgPath);
             if (file_exists($p1)) {
-                $fileToLoad = $p1;
                 $hasNobg = true;
             } elseif (file_exists($p2)) {
-                $fileToLoad = $p2;
                 $hasNobg = true;
             }
 
@@ -717,7 +749,6 @@ public function debugPlayerArt($playerId)
         }
         $html .= "</div>";
 
-        // 3. _processed Version
         $processedPath = $player->photo_path ? preg_replace('/\.[^.]+$/i', '_processed.png', $player->photo_path) : null;
         $html .= "<div class='box'><h2>3. Versão Processada (_processed.png)</h2>";
         $html .= "<p>Expected Path: <code>" . e($processedPath) . "</code></p>";
diff --git a/app/Http/Controllers/Admin/AsaasController.php b/app/Http/Controllers/Admin/AsaasController.php
index e19c641..ff975af 100644
--- a/app/Http/Controllers/Admin/AsaasController.php
+++ b/app/Http/Controllers/Admin/AsaasController.php
@@ -4,45 +4,49 @@
 
 use App\Http\Controllers\Controller;
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
 use App\Models\Club;
 use App\Models\Order;
 use App\Models\RaceResult;
+use App\Models\Product;
+use App\Models\Category;
 use App\Services\AuditLogger;
-use App\Models\SystemSetting;
+use App\Http\Controllers\ReceiptController;
+use App\Mail\InscriptionConfirmedMail;
 use Illuminate\Support\Facades\Log;
 use Illuminate\Support\Facades\DB;
 use Illuminate\Support\Facades\Mail;
-use App\Models\User;
+use Throwable;
 
 class AsaasController extends Controller
 {
     /**
-     * Obter configurações do Asaas do clube
+     * Get Asaas settings for the administrator's club.
      */
-    public function getSettings(Request $request)
+    public function getSettings(Request $request): JsonResponse
     {
         $user = $request->user();
         $club = Club::findOrFail($user->club_id);
 
         return response()->json($club->payment_settings ?? [
-            'asaas_token' => '',
+            'asaas_token'       => '',
             'asaas_environment' => 'sandbox',
-            'enabled' => false
+            'enabled'           => false
         ]);
     }
 
     /**
-     * Salvar configurações do Asaas
+     * Update Asaas payment settings.
      */
-    public function updateSettings(Request $request)
+    public function updateSettings(Request $request): JsonResponse
     {
         $user = $request->user();
         $club = Club::findOrFail($user->club_id);
 
         $validated = $request->validate([
-            'asaas_token' => 'required|string',
+            'asaas_token'       => 'required|string',
             'asaas_environment' => 'required|in:sandbox,production',
-            'enabled' => 'required|boolean'
+            'enabled'           => 'required|boolean'
         ]);
 
         $club->update(['payment_settings' => $validated]);
@@ -54,7 +58,10 @@ public function updateSettings(Request $request)
         return response()->json(['message' => 'Configurações salvas com sucesso!']);
     }
 
-    public function webhook(Request $request)
+    /**
+     * Receive and process webhook events from Asaas.
+     */
+    public function webhook(Request $request): JsonResponse
     {
         Log::info("Asaas Webhook chegou.", [
             'ip'      => $request->ip(),
@@ -62,7 +69,6 @@ public function webhook(Request $request)
             'payload' => $request->all(),
         ]);
 
-        // Validação do Token do Webhook (Segurança)
         $token = $request->header('asaas-access-token');
         $expectedToken = config('services.asaas.webhook_token');
 
@@ -81,12 +87,10 @@ public function webhook(Request $request)
             return response()->json(['message' => 'Invalid payload'], 400);
         }
 
-        // Eventos que indicam sucesso no pagamento
         $successEvents = ['PAYMENT_RECEIVED', 'PAYMENT_CONFIRMED'];
         $failureEvents = ['PAYMENT_OVERDUE', 'PAYMENT_DELETED', 'PAYMENT_REFUNDED'];
 
         try {
-            // ? TRANSAÇÃO CRÍTICA: apenas atualiza status no banco
             DB::beginTransaction();
 
             if (in_array($event, $successEvents)) {
@@ -97,28 +101,28 @@ public function webhook(Request $request)
 
             DB::commit();
 
-        } catch (\Throwable $e) {
+        } catch (Throwable $e) {
             DB::rollBack();
             Log::error("Asaas Webhook CRITICAL Error: " . $e->getMessage() . " at " . $e->getFile() . ":" . $e->getLine());
-            // Retorna 500 SOMENTE se a atualização crítica do banco falhar
             return response()->json(['error' => $e->getMessage()], 500);
         }
 
-        // ? Retorna 200 ao Asaas IMEDIATAMENTE após o commit do banco
-        // Estoque e e-mail são operações secundárias — falhas deles não afetam a resposta
         return response()->json(['status' => 'success']);
     }
 
-    private function handlePaymentSuccess($payment)
+    /**
+     * Process payment success logic.
+     */
+    private function handlePaymentSuccess(array $payment): void
     {
         $externalReference = $payment['externalReference'] ?? null;
-        if (!$externalReference)
+        if (!$externalReference) {
             return;
+        }
 
-        // Caso 1.1: Inscrição Coletiva de Corrida
         if (str_starts_with($externalReference, 'PG_')) {
             $groupId = str_replace('PG_', '', $externalReference);
-            $results = RaceResult::with(['user', 'race.championship', 'category.parent'])->where('payment_group_id', $groupId)->get();
+            $results = RaceResult::with(['user:id,name,email', 'race.championship', 'category:id,parent_id'])->where('payment_group_id', $groupId)->get();
 
             foreach ($results as $result) {
                 if ($result->status_payment !== 'paid') {
@@ -127,7 +131,6 @@ private function handlePaymentSuccess($payment)
                         'payment_method' => $payment['billingType'] ?? 'asaas'
                     ]);
 
-                    // Baixa no Estoque dos Brindes (Produtos Inclusos na Categoria)
                     if ($result->category) {
                         try {
                             $included = $result->category->products();
@@ -141,51 +144,46 @@ private function handlePaymentSuccess($payment)
                                     }
                                 }
                             }
-                        } catch (\Throwable $stockEx) {
+                        } catch (Throwable $stockEx) {
                             Log::error("Erro ao baixar estoque de brindes RR {$result->id}: " . $stockEx->getMessage());
                         }
                     }
 
-                    // Baixa no Estoque dos Itens de Loja Adicionais
                     try {
                         if ($result->shop_items && is_array($result->shop_items)) {
                             foreach ($result->shop_items as $item) {
-                                $prod = \App\Models\Product::find($item['product_id']);
+                                $prod = Product::find($item['product_id']);
                                 if ($prod && $prod->stock_quantity !== null) {
                                     $prod->decrement('stock_quantity', $item['quantity'] ?? 1);
                                     Log::info("Stock reduced for Additional Shop Item {$prod->id}: -" . ($item['quantity'] ?? 1));
                                 }
                             }
                         }
-                    } catch (\Throwable $stockEx) {
+                    } catch (Throwable $stockEx) {
                         Log::error("Erro ao baixar estoque de loja RR {$result->id}: " . $stockEx->getMessage());
                     }
 
                     try {
                         $this->sendInscriptionConfirmation($result);
-                    } catch (\Throwable $mailEx) {
+                    } catch (Throwable $mailEx) {
                         Log::error("Pagamento Confirmado RR {$result->id}, mas erro ao enviar e-mail: " . $mailEx->getMessage());
                     }
 
                     Log::info("RaceResult {$result->id} (Group {$groupId}) marked as PAID");
                 }
             }
-        }
-
-        // Caso 1: Inscrição Individual de Corrida
-        elseif (str_starts_with($externalReference, 'RR_')) {
+        } elseif (str_starts_with($externalReference, 'RR_')) {
             $id = str_replace('RR_', '', $externalReference);
-            $result = RaceResult::with(['user', 'race.championship', 'category.parent'])->find($id);
+            $result = RaceResult::with(['user:id,name,email', 'race.championship', 'category:id,parent_id'])->find($id);
             if ($result && $result->status_payment !== 'paid') {
                 $result->update([
                     'status_payment' => 'paid',
                     'payment_method' => $payment['billingType'] ?? 'asaas'
                 ]);
 
-                // Baixa no Estoque dos Brindes (Produtos Inclusos na Categoria)
                 if ($result->category) {
                     try {
-                        $included = $result->category->products(); // Chamada correta do método
+                        $included = $result->category->products();
                         if ($included && $included->count() > 0) {
                             foreach ($included as $item) {
                                 $product = $item['product'];
@@ -196,37 +194,34 @@ private function handlePaymentSuccess($payment)
                                 }
                             }
                         }
-                    } catch (\Throwable $stockEx) {
+                    } catch (Throwable $stockEx) {
                         Log::error("Erro ao baixar estoque de brindes RR {$id}: " . $stockEx->getMessage());
                     }
                 }
 
-                // Baixa no Estoque dos Itens de Loja Adicionais
                 try {
                     if ($result->shop_items && is_array($result->shop_items)) {
                         foreach ($result->shop_items as $item) {
-                            $prod = \App\Models\Product::find($item['product_id']);
+                            $prod = Product::find($item['product_id']);
                             if ($prod && $prod->stock_quantity !== null) {
                                 $prod->decrement('stock_quantity', $item['quantity'] ?? 1);
                                 Log::info("Stock reduced for Additional Shop Item {$prod->id}: -" . ($item['quantity'] ?? 1));
                             }
                         }
                     }
-                } catch (\Throwable $stockEx) {
+                } catch (Throwable $stockEx) {
                     Log::error("Erro ao baixar estoque de loja RR {$id}: " . $stockEx->getMessage());
                 }
 
                 try {
                     $this->sendInscriptionConfirmation($result);
-                } catch (\Throwable $mailEx) {
+                } catch (Throwable $mailEx) {
                     Log::error("Pagamento Confirmado RR {$id}, mas erro ao enviar e-mail: " . $mailEx->getMessage());
                 }
 
                 Log::info("RaceResult {$id} marked as PAID");
             }
-        }
-        // Caso 2: Checkout Geral / Pedido na Loja
-        elseif (str_starts_with($externalReference, 'ORD_')) {
+        } elseif (str_starts_with($externalReference, 'ORD_')) {
             $id = str_replace('ORD_', '', $externalReference);
             $order = Order::with('items.product')->find($id);
             if ($order && $order->status !== 'paid') {
@@ -240,21 +235,18 @@ private function handlePaymentSuccess($payment)
                 }
                 Log::info("Order {$id} marked as PAID and stock updated");
             }
-        }
-        // Caso 3: Inscrição de Equipe (Championship Team Pivot)
-        elseif (str_starts_with($externalReference, 'CT_')) {
+        } elseif (str_starts_with($externalReference, 'CT_')) {
             $id = str_replace('CT_', '', $externalReference);
             $pivot = DB::table('championship_team')->where('id', $id)->first();
             if ($pivot && $pivot->status_payment !== 'paid') {
                 DB::table('championship_team')->where('id', $id)->update([
                     'status_payment' => 'paid',
                     'payment_method' => $payment['billingType'] ?? 'asaas',
-                    'updated_at' => now()
+                    'updated_at'     => now()
                 ]);
 
-                // Baixa no Estoque dos Brindes
                 if ($pivot->category_id) {
-                    $category = \App\Models\Category::find($pivot->category_id);
+                    $category = Category::find($pivot->category_id);
                     if ($category) {
                         try {
                             $included = $category->products();
@@ -268,18 +260,17 @@ private function handlePaymentSuccess($payment)
                                     }
                                 }
                             }
-                        } catch (\Throwable $stockEx) {
+                        } catch (Throwable $stockEx) {
                             Log::error("Erro ao baixar estoque de brindes CT {$id}: " . $stockEx->getMessage());
                         }
                     }
                 }
 
-                // Baixa no Estoque dos Itens de Loja Adicionais (Team)
                 if ($pivot->shop_items) {
                     $shopItems = json_decode($pivot->shop_items, true);
                     if (is_array($shopItems)) {
                         foreach ($shopItems as $item) {
-                            $prod = \App\Models\Product::find($item['product_id']);
+                            $prod = Product::find($item['product_id']);
                             if ($prod && $prod->stock_quantity !== null) {
                                 $prod->decrement('stock_quantity', $item['quantity'] ?? 1);
                                 Log::info("Stock reduced for Additional Team Shop Item {$prod->id}: -" . ($item['quantity'] ?? 1));
@@ -291,19 +282,22 @@ private function handlePaymentSuccess($payment)
                 Log::info("ChampionshipTeam {$id} marked as PAID");
             }
         }
-
-        return;
     }
 
-    private function handlePaymentFailure($payment, $event)
+    /**
+     * Process payment failure logic.
+     */
+    private function handlePaymentFailure(array $payment, string $event): void
     {
         $externalReference = $payment['externalReference'] ?? null;
-        if (!$externalReference)
+        if (!$externalReference) {
             return;
+        }
 
         $status = 'cancelled';
-        if ($event === 'PAYMENT_REFUNDED')
+        if ($event === 'PAYMENT_REFUNDED') {
             $status = 'refunded';
+        }
 
         if (str_starts_with($externalReference, 'RR_')) {
             $id = str_replace('RR_', '', $externalReference);
@@ -314,7 +308,10 @@ private function handlePaymentFailure($payment, $event)
         }
     }
 
-    private function sendInscriptionConfirmation($result)
+    /**
+     * Send email with confirmation PDF receipt.
+     */
+    private function sendInscriptionConfirmation(RaceResult $result): void
     {
         if (!$result->user || !$result->user->email) {
             return;
@@ -323,18 +320,17 @@ private function sendInscriptionConfirmation($result)
         $user = $result->user;
 
         try {
-            // Gerar o PDF
-            $receiptController = new \App\Http\Controllers\ReceiptController();
+            $receiptController = new ReceiptController();
             $pdf = $receiptController->generatePdf($result);
             $pdfContent = $pdf->output();
 
             Mail::to($user->email)->send(
-                (new \App\Mail\InscriptionConfirmedMail($result))
+                (new InscriptionConfirmedMail($result))
                     ->attachData($pdfContent, 'comprovante_inscricao.pdf', [
                         'mime' => 'application/pdf',
                     ])
             );
-        } catch (\Exception $e) {
+        } catch (Throwable $e) {
             Log::error("Erro ao enviar e-mail de confirmação: " . $e->getMessage());
         }
     }
diff --git a/app/Http/Controllers/Admin/AuditController.php b/app/Http/Controllers/Admin/AuditController.php
index 065b075..c193ae2 100644
--- a/app/Http/Controllers/Admin/AuditController.php
+++ b/app/Http/Controllers/Admin/AuditController.php
@@ -5,22 +5,24 @@
 use App\Http\Controllers\Controller;
 use App\Models\AuditLog;
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
 
 class AuditController extends Controller
 {
-    public function index(Request $request)
+    /**
+     * List audit logs with pagination and relation constraints.
+     */
+    public function index(Request $request): JsonResponse
     {
         try {
             $user = $request->user();
 
-            $query = AuditLog::with('user', 'club');
+            $query = AuditLog::with(['user:id,name,email', 'club:id,name']);
 
-            // Se não for Super Admin (tiver club_id), filtra apenas logs do próprio clube
             if ($user->club_id) {
                 $query->where('club_id', $user->club_id);
             }
 
-            // Filtros opcionais
             if ($request->filled('search')) {
                 $search = $request->search;
                 $query->where(function ($q) use ($search) {
@@ -47,10 +49,8 @@ public function index(Request $request)
             return response()->json($logs);
         } catch (\Exception $e) {
             return response()->json([
-                'error' => 'Erro interno do servidor',
-                'message' => $e->getMessage(),
-                'file' => $e->getFile(),
-                'line' => $e->getLine()
+                'error'   => 'Erro interno do servidor',
+                'message' => $e->getMessage()
             ], 500);
         }
     }
diff --git a/app/Http/Controllers/Admin/CategoryController.php b/app/Http/Controllers/Admin/CategoryController.php
index 16dc726..bffbfdc 100644
--- a/app/Http/Controllers/Admin/CategoryController.php
+++ b/app/Http/Controllers/Admin/CategoryController.php
@@ -4,20 +4,21 @@
 
 use App\Http\Controllers\Controller;
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
 use App\Models\Category;
 use App\Models\Championship;
+use App\Models\Team;
 use App\Services\AuditLogger;
 
 class CategoryController extends Controller
 {
     /**
-     * Listar todas as categorias de um campeonato
+     * List all categories of a championship.
      */
-    public function index(Request $request, $championshipId)
+    public function index(Request $request, int $championshipId): JsonResponse
     {
         $championship = Championship::findOrFail($championshipId);
 
-        // Verifica permissão
         $user = $request->user();
         if ($user->club_id !== null && $championship->club_id !== $user->club_id) {
             return response()->json([
@@ -29,7 +30,6 @@ public function index(Request $request, $championshipId)
             ->withCount('teams')
             ->get();
 
-        // Append art background URL if exists in art_settings
         $artSettings = $championship->art_settings ?? [];
         $categories->each(function ($category) use ($artSettings) {
             $path = $artSettings['category_backgrounds'][$category->id] ?? null;
@@ -44,9 +44,9 @@ public function index(Request $request, $championshipId)
     }
 
     /**
-     * Atualizar fundo da arte para a categoria
+     * Update art background for the category.
      */
-    public function updateArtBackground(Request $request, $championshipId, $categoryId)
+    public function updateArtBackground(Request $request, int $championshipId, int $categoryId): JsonResponse
     {
         $championship = Championship::findOrFail($championshipId);
         $category = Category::where('championship_id', $championshipId)->findOrFail($categoryId);
@@ -74,7 +74,7 @@ public function updateArtBackground(Request $request, $championshipId, $category
 
             return response()->json([
                 'message' => 'Fundo atualizado com sucesso!',
-                'url' => $fullUrl
+                'url'     => $fullUrl
             ]);
         }
 
@@ -82,13 +82,12 @@ public function updateArtBackground(Request $request, $championshipId, $category
     }
 
     /**
-     * Criar nova categoria
+     * Create a new category.
      */
-    public function store(Request $request, $championshipId)
+    public function store(Request $request, int $championshipId): JsonResponse
     {
         $championship = Championship::findOrFail($championshipId);
 
-        // Verifica permissão
         $user = $request->user();
         if ($user->club_id !== null && $championship->club_id !== $user->club_id) {
             return response()->json([
@@ -97,43 +96,42 @@ public function store(Request $request, $championshipId)
         }
 
         $validated = $request->validate([
-            'name' => 'required|string|max:255',
-            'description' => 'nullable|string',
-            'min_age' => 'nullable|integer|min:0',
-            'max_age' => 'nullable|integer|min:0',
-            'gender' => 'nullable|in:male,female,mixed',
-            'max_teams' => 'nullable|integer|min:0',
-            'parent_id' => 'nullable|exists:categories,id',
-            'price' => 'nullable|numeric|min:0',
-            'included_products' => 'nullable|array',
+            'name'                           => 'required|string|max:255',
+            'description'                    => 'nullable|string',
+            'min_age'                        => 'nullable|integer|min:0',
+            'max_age'                        => 'nullable|integer|min:0',
+            'gender'                         => 'nullable|in:male,female,mixed',
+            'max_teams'                      => 'nullable|integer|min:0',
+            'parent_id'                      => 'nullable|exists:categories,id',
+            'price'                          => 'nullable|numeric|min:0',
+            'included_products'              => 'nullable|array',
             'included_products.*.product_id' => 'required|exists:products,id',
-            'included_products.*.quantity' => 'required|integer|min:1',
-            'included_products.*.required' => 'boolean'
+            'included_products.*.quantity'   => 'required|integer|min:1',
+            'included_products.*.required'   => 'boolean'
         ]);
 
         $category = $championship->categories()->create($validated);
 
         AuditLogger::log('category.create_in_champ', "Criou a categoria '{$category->name}' no campeonato '{$championship->name}'", [
-            'category_id' => $category->id,
+            'category_id'     => $category->id,
             'championship_id' => $championship->id
         ]);
 
         return response()->json([
-            'message' => 'Categoria criada com sucesso!',
+            'message'  => 'Categoria criada com sucesso!',
             'category' => $category
         ], 201);
     }
 
     /**
-     * Atualizar categoria
+     * Update category.
      */
-    public function update(Request $request, $championshipId, $categoryId)
+    public function update(Request $request, int $championshipId, int $categoryId): JsonResponse
     {
         $championship = Championship::findOrFail($championshipId);
         $category = Category::where('championship_id', $championshipId)
             ->findOrFail($categoryId);
 
-        // Verifica permissão
         $user = $request->user();
         if ($user->club_id !== null && $championship->club_id !== $user->club_id) {
             return response()->json([
@@ -142,43 +140,42 @@ public function update(Request $request, $championshipId, $categoryId)
         }
 
         $validated = $request->validate([
-            'name' => 'sometimes|string|max:255',
-            'description' => 'nullable|string',
-            'min_age' => 'nullable|integer|min:0',
-            'max_age' => 'nullable|integer|min:0',
-            'gender' => 'nullable|in:male,female,mixed',
-            'max_teams' => 'nullable|integer|min:0',
-            'parent_id' => 'nullable|exists:categories,id',
-            'price' => 'nullable|numeric|min:0',
-            'included_products' => 'nullable|array',
+            'name'                           => 'sometimes|string|max:255',
+            'description'                    => 'nullable|string',
+            'min_age'                        => 'nullable|integer|min:0',
+            'max_age'                        => 'nullable|integer|min:0',
+            'gender'                         => 'nullable|in:male,female,mixed',
+            'max_teams'                      => 'nullable|integer|min:0',
+            'parent_id'                      => 'nullable|exists:categories,id',
+            'price'                          => 'nullable|numeric|min:0',
+            'included_products'              => 'nullable|array',
             'included_products.*.product_id' => 'required|exists:products,id',
-            'included_products.*.quantity' => 'required|integer|min:1',
-            'included_products.*.required' => 'boolean'
+            'included_products.*.quantity'   => 'required|integer|min:1',
+            'included_products.*.required'   => 'boolean'
         ]);
 
         $category->update($validated);
 
         AuditLogger::log('category.update_in_champ', "Editou a categoria '{$category->name}' (ID: {$categoryId})", [
-            'category_id' => $categoryId,
+            'category_id'     => $categoryId,
             'championship_id' => $championshipId
         ]);
 
         return response()->json([
-            'message' => 'Categoria atualizada com sucesso!',
+            'message'  => 'Categoria atualizada com sucesso!',
             'category' => $category
         ]);
     }
 
     /**
-     * Deletar categoria
+     * Delete category.
      */
-    public function destroy(Request $request, $championshipId, $categoryId)
+    public function destroy(Request $request, int $championshipId, int $categoryId): JsonResponse
     {
         $championship = Championship::findOrFail($championshipId);
         $category = Category::where('championship_id', $championshipId)
             ->findOrFail($categoryId);
 
-        // Verifica permissão
         $user = $request->user();
         if ($user->club_id !== null && $championship->club_id !== $user->club_id) {
             return response()->json([
@@ -186,7 +183,6 @@ public function destroy(Request $request, $championshipId, $categoryId)
             ], 403);
         }
 
-        // Verifica se há equipes vinculadas
         if ($category->teams()->count() > 0) {
             return response()->json([
                 'message' => 'Não é possível deletar categoria com equipes vinculadas.'
@@ -203,15 +199,14 @@ public function destroy(Request $request, $championshipId, $categoryId)
     }
 
     /**
-     * Adicionar equipe à categoria
+     * Add team to category.
      */
-    public function addTeam(Request $request, $championshipId, $categoryId)
+    public function addTeam(Request $request, int $championshipId, int $categoryId): JsonResponse
     {
         $championship = Championship::findOrFail($championshipId);
         $category = Category::where('championship_id', $championshipId)
             ->findOrFail($categoryId);
 
-        // Verifica permissão
         $user = $request->user();
         if ($user->club_id !== null && $championship->club_id !== $user->club_id) {
             return response()->json([
@@ -223,25 +218,17 @@ public function addTeam(Request $request, $championshipId, $categoryId)
             'team_id' => 'required|exists:teams,id',
         ]);
 
-        // Verifica se a equipe já está na categoria
         if ($category->teams()->where('team_id', $validated['team_id'])->exists()) {
             return response()->json([
                 'message' => 'Equipe já está nesta categoria.'
             ], 400);
         }
 
-        // Validação de elegibilidade de todos os membros do time (incluindo capitão)
-        $team = \App\Models\Team::with(['players', 'captain'])->findOrFail($validated['team_id']);
-
-        // Removida a validação automática do capitão global e dos jogadores globais do time ($team->captain e $team->players).
-        // Isso porque no momento em que um time entra em um campeonato/categoria, o seu elenco local é vazio 
-        // e o capitão da categoria pode ser definido depois. As validações ocorrerão no momento de adicionar o jogador.
-
         $category->teams()->attach($validated['team_id']);
 
         AuditLogger::log('category.team_add', "Vinculou o time (ID: {$validated['team_id']}) à categoria '{$category->name}'", [
             'category_id' => $category->id,
-            'team_id' => $validated['team_id']
+            'team_id'     => $validated['team_id']
         ]);
 
         return response()->json([
@@ -250,15 +237,14 @@ public function addTeam(Request $request, $championshipId, $categoryId)
     }
 
     /**
-     * Remover equipe da categoria
+     * Remove team from category.
      */
-    public function removeTeam(Request $request, $championshipId, $categoryId, $teamId)
+    public function removeTeam(Request $request, int $championshipId, int $categoryId, int $teamId): JsonResponse
     {
         $championship = Championship::findOrFail($championshipId);
         $category = Category::where('championship_id', $championshipId)
             ->findOrFail($categoryId);
 
-        // Verifica permissão
         $user = $request->user();
         if ($user->club_id !== null && $championship->club_id !== $user->club_id) {
             return response()->json([
@@ -270,7 +256,7 @@ public function removeTeam(Request $request, $championshipId, $categoryId, $team
 
         AuditLogger::log('category.team_remove', "Removeu o time (ID: {$teamId}) da categoria '{$category->name}'", [
             'category_id' => $category->id,
-            'team_id' => $teamId
+            'team_id'     => $teamId
         ]);
 
         return response()->json([
diff --git a/app/Http/Controllers/Admin/CouponController.php b/app/Http/Controllers/Admin/CouponController.php
index 1994875..125da58 100644
--- a/app/Http/Controllers/Admin/CouponController.php
+++ b/app/Http/Controllers/Admin/CouponController.php
@@ -4,12 +4,16 @@
 
 use App\Http\Controllers\Controller;
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
 use App\Models\Coupon;
 use App\Services\AuditLogger;
 
 class CouponController extends Controller
 {
-    public function index(Request $request)
+    /**
+     * List all coupons.
+     */
+    public function index(Request $request): JsonResponse
     {
         $user = $request->user();
         $coupons = Coupon::where('club_id', $user->club_id)
@@ -18,21 +22,23 @@ public function index(Request $request)
         return response()->json($coupons);
     }
 
-    public function store(Request $request)
+    /**
+     * Store a new coupon.
+     */
+    public function store(Request $request): JsonResponse
     {
         $user = $request->user();
         $validated = $request->validate([
-            'code' => 'required|string|max:50',
-            'discount_type' => 'required|in:fixed,percent',
+            'code'           => 'required|string|max:50',
+            'discount_type'  => 'required|in:fixed,percent',
             'discount_value' => 'required|numeric|min:0',
-            'max_uses' => 'nullable|integer|min:1',
-            'expires_at' => 'nullable|date'
+            'max_uses'       => 'nullable|integer|min:1',
+            'expires_at'     => 'nullable|date'
         ]);
 
         $validated['club_id'] = $user->club_id;
         $validated['code'] = strtoupper($validated['code']);
 
-        // Evitar duplicidade de código no mesmo clube
         if (Coupon::where('club_id', $user->club_id)->where('code', $validated['code'])->exists()) {
             return response()->json(['message' => 'Já existe um cupom com este código.'], 422);
         }
@@ -46,17 +52,20 @@ public function store(Request $request)
         return response()->json($coupon, 201);
     }
 
-    public function update(Request $request, $id)
+    /**
+     * Update an existing coupon.
+     */
+    public function update(Request $request, int $id): JsonResponse
     {
         $user = $request->user();
         $coupon = Coupon::where('club_id', $user->club_id)->findOrFail($id);
 
         $validated = $request->validate([
-            'code' => 'sometimes|string|max:50',
-            'discount_type' => 'sometimes|in:fixed,percent',
+            'code'           => 'sometimes|string|max:50',
+            'discount_type'  => 'sometimes|in:fixed,percent',
             'discount_value' => 'sometimes|numeric|min:0',
-            'max_uses' => 'nullable|integer|min:1',
-            'expires_at' => 'nullable|date'
+            'max_uses'       => 'nullable|integer|min:1',
+            'expires_at'     => 'nullable|date'
         ]);
 
         if (isset($validated['code'])) {
@@ -72,7 +81,10 @@ public function update(Request $request, $id)
         return response()->json($coupon);
     }
 
-    public function destroy(Request $request, $id)
+    /**
+     * Delete coupon.
+     */
+    public function destroy(Request $request, int $id): JsonResponse
     {
         $user = $request->user();
         $coupon = Coupon::where('club_id', $user->club_id)->findOrFail($id);
diff --git a/app/Http/Controllers/Admin/ExportController.php b/app/Http/Controllers/Admin/ExportController.php
index 40acb2e..3aaa233 100644
--- a/app/Http/Controllers/Admin/ExportController.php
+++ b/app/Http/Controllers/Admin/ExportController.php
@@ -5,16 +5,15 @@
 use App\Http\Controllers\Controller;
 use Illuminate\Http\Request;
 use App\Models\User;
-use App\Models\GameMatch;
 use App\Models\Team;
-use Illuminate\Support\Facades\Response;
+use Symfony\Component\HttpFoundation\StreamedResponse;
 
 class ExportController extends Controller
 {
     /**
-     * Exportar Jogadores para CSV
+     * Export players to CSV using memory-efficient streams.
      */
-    public function exportPlayers(Request $request)
+    public function exportPlayers(Request $request): StreamedResponse
     {
         $query = User::query();
 
@@ -22,68 +21,58 @@ public function exportPlayers(Request $request)
             $query->where('club_id', $request->club_id);
         }
 
-        $players = $query->get(['name', 'email', 'cpf', 'phone', 'birth_date']);
-
-        $csvHeader = ["Nome", "Email", "CPF", "Telefone", "Data Nascimento"];
-        $csvData = [];
-
-        foreach ($players as $player) {
-            $csvData[] = [
-                $player->name,
-                $player->email,
-                $player->cpf,
-                $player->phone,
-                $player->birth_date,
-            ];
-        }
-
-        return $this->generateCsv($csvHeader, $csvData, 'jogadores.csv');
+        $headers = [
+            'Content-Type'        => 'text/csv; charset=UTF-8',
+            'Content-Disposition' => 'attachment; filename="jogadores.csv"',
+        ];
+
+        return response()->stream(function () use ($query) {
+            $handle = fopen('php://output', 'w');
+            
+            // Add UTF-8 BOM for proper Excel encoding
+            fputs($handle, "\xEF\xBB\xBF");
+            fputcsv($handle, ["Nome", "Email", "CPF", "Telefone", "Data Nascimento"]);
+
+            $players = $query->select(['name', 'email', 'cpf', 'phone', 'birth_date'])->cursor();
+            foreach ($players as $player) {
+                fputcsv($handle, [
+                    $player->name,
+                    $player->email,
+                    $player->cpf,
+                    $player->phone,
+                    $player->birth_date,
+                ]);
+            }
+            fclose($handle);
+        }, 200, $headers);
     }
 
     /**
-     * Exportar Times para CSV
+     * Export teams to CSV using memory-efficient streams.
      */
-    public function exportTeams(Request $request)
+    public function exportTeams(Request $request): StreamedResponse
     {
-        $teams = Team::with('club')->get();
-
-        $csvHeader = ["Nome", "Clube", "Cor Primária"];
-        $csvData = [];
-
-        foreach ($teams as $team) {
-            $csvData[] = [
-                $team->name,
-                $team->club->name ?? 'N/A',
-                $team->primary_color
-            ];
-        }
-
-        return $this->generateCsv($csvHeader, $csvData, 'times.csv');
-    }
-
-    /**
-     * Helper para gerar resposta CSV
-     */
-    private function generateCsv($header, $data, $filename)
-    {
-        $handle = fopen('php://temp', 'w');
-
-        // Adicionar BOM para Excel reconhecer UTF-8 corretamente
-        fputs($handle, "\xEF\xBB\xBF");
-
-        fputcsv($handle, $header);
-
-        foreach ($data as $row) {
-            fputcsv($handle, $row);
-        }
-
-        rewind($handle);
-        $content = stream_get_contents($handle);
-        fclose($handle);
-
-        return Response::make($content, 200, [
-            'Content-Type' => 'text/csv',
-            'Content-Disposition' => "attachment; filename={$filename}",
-        ]);
+        $headers = [
+            'Content-Type'        => 'text/csv; charset=UTF-8',
+            'Content-Disposition' => 'attachment; filename="times.csv"',
+        ];
+
+        return response()->stream(function () {
+            $handle = fopen('php://output', 'w');
+            
+            // Add UTF-8 BOM for proper Excel encoding
+            fputs($handle, "\xEF\xBB\xBF");
+            fputcsv($handle, ["Nome", "Clube", "Cor Primária"]);
+
+            $teams = Team::with('club:id,name')->cursor();
+            foreach ($teams as $team) {
+                fputcsv($handle, [
+                    $team->name,
+                    $team->club->name ?? 'N/A',
+                    $team->primary_color
+                ]);
+            }
+            fclose($handle);
+        }, 200, $headers);
     }
 }
diff --git a/app/Http/Controllers/Admin/ImageUploadController.php b/app/Http/Controllers/Admin/ImageUploadController.php
index 60f7f60..672c6b8 100644
--- a/app/Http/Controllers/Admin/ImageUploadController.php
+++ b/app/Http/Controllers/Admin/ImageUploadController.php
@@ -4,27 +4,32 @@
 
 use App\Http\Controllers\Controller;
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
 use Illuminate\Support\Facades\Storage;
+use Illuminate\Support\Facades\Log;
 use Illuminate\Support\Str;
 use App\Models\Team;
 use App\Models\User;
 use App\Models\Championship;
+use App\Models\Product;
+use App\Models\Club;
+use Exception;
+use Throwable;
 
 class ImageUploadController extends Controller
 {
     /**
-     * Upload de logo de equipe
+     * Upload team logo.
      */
-    public function uploadTeamLogo(Request $request, $teamId)
+    public function uploadTeamLogo(Request $request, int $teamId): JsonResponse
     {
         try {
             $request->validate([
-                'logo' => 'required|image|mimes:jpeg,png,jpg,webp,jfif|max:4096', // Max 4MB
+                'logo' => 'required|image|mimes:jpeg,png,jpg,webp,jfif|max:4096',
             ]);
 
             $team = Team::findOrFail($teamId);
 
-            // Verifica permissão de clube
             $user = $request->user();
             if ($user->club_id !== null && $team->club_id !== $user->club_id) {
                 return response()->json([
@@ -32,8 +37,6 @@ public function uploadTeamLogo(Request $request, $teamId)
                 ], 403);
             }
 
-            // Remove logo antigo se existir
-            // Verifica logo_path (preferencial) ou tenta inferir do logo_url
             if (!empty($team->logo_path) && Storage::disk('public')->exists($team->logo_path)) {
                 Storage::disk('public')->delete($team->logo_path);
             } elseif (!empty($team->logo_url)) {
@@ -43,53 +46,42 @@ public function uploadTeamLogo(Request $request, $teamId)
                 }
             }
 
-            // Salva novo logo
             $file = $request->file('logo');
-            // Fix: Don't add 'teams/' prefix here, storeAs adds it based on first arg
             $filename = Str::slug($team->name) . '-' . time() . '.' . $file->getClientOriginalExtension();
             $path = $file->storeAs('teams', $filename, 'public');
 
-            // Atualiza no banco usando as colunas corretas
-            $team->logo_url = '/storage/' . $path; // Force relative path
+            $team->logo_url = '/storage/' . $path;
             $team->logo_path = $path;
-
-            // Remove a atribuição incorreta '$team->logo' que causava erro se a coluna não existisse
-            // $team->logo = $path; 
-
             $team->save();
 
             return response()->json([
-                'message' => 'Logo atualizado com sucesso!',
-                'logo_url' => $team->logo_url,
+                'message'   => 'Logo atualizado com sucesso!',
+                'logo_url'  => $team->logo_url,
                 'logo_path' => $team->logo_path
             ]);
-        } catch (\Exception $e) {
-            \Log::error("Upload Team Logo Error: " . $e->getMessage());
-            // Retorna a mensagem de erro detalhada para facilitar o debug do usuário
+        } catch (Exception $e) {
+            Log::error("Upload Team Logo Error: " . $e->getMessage());
             return response()->json(['message' => 'Erro ao fazer upload do logo: ' . $e->getMessage()], 500);
         }
     }
 
     /**
-     * Upload de foto de jogador
+     * Upload player photo.
      */
-    public function uploadPlayerPhoto(Request $request, $playerId)
+    public function uploadPlayerPhoto(Request $request, int $playerId): JsonResponse
     {
         try {
             $request->validate([
                 'photo' => 'required|image|mimes:jpeg,png,jpg,webp,jfif|max:4096',
-                'index' => 'nullable|integer|min:0|max:2', // Validar índice (0, 1, 2)
+                'index' => 'nullable|integer|min:0|max:2',
             ]);
 
-            // Aumenta o tempo de execução para evitar timeout durante o processamento da IA
             set_time_limit(300);
 
             $player = User::findOrFail($playerId);
 
-            // Verifica permissão de clube
             $user = $request->user();
             if ($user && $user->club_id !== null && $player->club_id !== $user->club_id && $user->id !== $player->id) {
-                // Se o player não tem clube, verifica se pertence a algum time do clube do admin
                 $belongsToClubTeam = false;
                 if ($player->club_id === null) {
                     $belongsToClubTeam = $player->teamsAsPlayer()->where('club_id', $user->club_id)->exists();
@@ -102,11 +94,9 @@ public function uploadPlayerPhoto(Request $request, $playerId)
                 }
             }
 
-            // Recupera fotos atuais
             $currentPhotos = $player->photos ?? [];
             if (!is_array($currentPhotos)) {
                 $currentPhotos = [];
-                // Migração de legado: se existir photo_path mas não array, adiciona como primeira
                 if ($player->photo_path) {
                     $currentPhotos[] = $player->photo_path;
                 }
@@ -114,9 +104,7 @@ public function uploadPlayerPhoto(Request $request, $playerId)
 
             $index = $request->input('index');
 
-            // Lógica de índice
             if ($index === null) {
-                // Se não informado, tenta adicionar. Se cheio, substitui a principal (0)
                 if (count($currentPhotos) < 3) {
                     $index = count($currentPhotos);
                 } else {
@@ -124,7 +112,6 @@ public function uploadPlayerPhoto(Request $request, $playerId)
                 }
             }
 
-            // Remove foto antiga SE estiver substituindo
             if (isset($currentPhotos[$index])) {
                 $oldPath = $currentPhotos[$index];
                 if (Storage::disk('public')->exists($oldPath)) {
@@ -132,37 +119,29 @@ public function uploadPlayerPhoto(Request $request, $playerId)
                 }
             }
 
-            // Salva nova foto
             $file = $request->file('photo');
             $filename = Str::slug($player->name) . '-' . time() . '-' . $index . '.' . $file->getClientOriginalExtension();
             $path = $file->storeAs('players', $filename, 'public');
 
             $responseData = [
-                'message' => 'Foto atualizada com sucesso!',
-                'photo_url' => url('api/storage/' . $path),
+                'message'    => 'Foto atualizada com sucesso!',
+                'photo_url'  => url('api/storage/' . $path),
                 'photo_path' => $path,
-                'index' => $index
+                'index'      => $index
             ];
 
-            // Atualiza array de fotos
             $currentPhotos[$index] = $path;
-
-            // Reindexa array para garantir consistência (opcional, mas bom para JSON)
             $currentPhotos = array_values($currentPhotos);
 
             $player->photos = $currentPhotos;
             
-            // Sempre salva/mantém a original na photo_path_original
             if (isset($currentPhotos[0])) {
                 $player->photo_path_original = $currentPhotos[0];
             }
 
-            // Mantém compatibilidade com photo_path (sempre a primeira foto)
             $player->photo_path = $currentPhotos[0] ?? null;
-
             $player->save();
 
-            // TRIGGER BACKGROUND AI PROCESSING
             if ($request->boolean('remove_bg')) {
                 try {
                     $userId = (int)$player->id;
@@ -176,42 +155,41 @@ public function uploadPlayerPhoto(Request $request, $playerId)
 
                     $cmd = "{$escapedPhp} {$escapedArtisan} player:process-photos {$escapedUserId}";
 
-                    \Log::info("ImageUploadController Background - Triggering AI for User {$userId}. Cmd: {$cmd}");
+                    Log::info("ImageUploadController Background - Triggering AI for User {$userId}. Cmd: {$cmd}");
 
                     if ($isWindows) {
                         pclose(popen("start /B " . $cmd, "r"));
                     } else {
                         exec("nohup {$cmd} > /dev/null 2>&1 & disown");
                     }
-                } catch (\Throwable $e) {
-                    \Log::error("ImageUploadController Background Error: " . $e->getMessage());
+                } catch (Throwable $e) {
+                    Log::error("ImageUploadController Background Error: " . $e->getMessage());
                 }
             }
 
-            \Log::info("ImageUploadController - Success. About to return response for player {$playerId}");
+            Log::info("ImageUploadController - Success. About to return response for player {$playerId}");
 
             $responseData['photos'] = $currentPhotos;
 
             return response()->json($responseData);
-        } catch (\Exception $e) {
-            \Log::error("Upload Player Photo Error: " . $e->getMessage());
+        } catch (Exception $e) {
+            Log::error("Upload Player Photo Error: " . $e->getMessage());
             return response()->json(['message' => 'Erro ao fazer upload da foto.'], 500);
         }
     }
 
     /**
-     * Upload de logo de campeonato
+     * Upload championship logo.
      */
-    public function uploadChampionshipLogo(Request $request, $championshipId)
+    public function uploadChampionshipLogo(Request $request, int $championshipId): JsonResponse
     {
         try {
             $request->validate([
-                'logo' => 'required|image|mimes:jpeg,png,jpg,webp,jfif|max:5120', // 5MB
+                'logo' => 'required|image|mimes:jpeg,png,jpg,webp,jfif|max:5120',
             ]);
 
             $championship = Championship::findOrFail($championshipId);
 
-            // Verifica permissão de clube
             $user = $request->user();
             if ($user->club_id !== null && $championship->club_id !== $user->club_id) {
                 return response()->json([
@@ -219,7 +197,6 @@ public function uploadChampionshipLogo(Request $request, $championshipId)
                 ], 403);
             }
 
-            // Remove logo antigo se existir (extraindo path do URL)
             if ($championship->logo_url) {
                 $oldPath = str_replace('/storage/', '', parse_url($championship->logo_url, PHP_URL_PATH));
                 if (Storage::disk('public')->exists($oldPath)) {
@@ -227,39 +204,36 @@ public function uploadChampionshipLogo(Request $request, $championshipId)
                 }
             }
 
-            // Salva novo logo em pasta organizada: championships/{id}/logo.ext
             $file = $request->file('logo');
             $extension = $file->getClientOriginalExtension();
             $filename = 'logo.' . $extension;
             $path = $file->storeAs("championships/{$championshipId}", $filename, 'public');
             $url = '/storage/' . $path;
 
-            // Atualiza no banco com URL completo
             $championship->logo_url = $url;
             $championship->save();
 
             return response()->json([
-                'message' => 'Logo atualizado com sucesso!',
+                'message'  => 'Logo atualizado com sucesso!',
                 'logo_url' => $url
             ]);
-        } catch (\Exception $e) {
-            \Log::error("Upload Champ Logo Error: " . $e->getMessage());
+        } catch (Exception $e) {
+            Log::error("Upload Champ Logo Error: " . $e->getMessage());
             return response()->json(['message' => 'Erro ao fazer upload do logo.'], 500);
         }
     }
 
     /**
-     * Upload de capa de campeonato
+     * Upload championship banner.
      */
-    public function uploadChampionshipBanner(Request $request, $championshipId)
+    public function uploadChampionshipBanner(Request $request, int $championshipId): JsonResponse
     {
         $request->validate([
-            'banner' => 'required|image|mimes:jpeg,png,jpg,webp,jfif|max:5120', // 5MB
+            'banner' => 'required|image|mimes:jpeg,png,jpg,webp,jfif|max:5120',
         ]);
 
         $championship = Championship::findOrFail($championshipId);
 
-        // Verifica permissão de clube
         $user = $request->user();
         if ($user->club_id !== null && $championship->club_id !== $user->club_id) {
             return response()->json([
@@ -267,7 +241,6 @@ public function uploadChampionshipBanner(Request $request, $championshipId)
             ], 403);
         }
 
-        // Remove banner antigo se existir (extraindo path do URL)
         if ($championship->cover_image_url) {
             $oldPath = str_replace('/storage/', '', parse_url($championship->cover_image_url, PHP_URL_PATH));
             if (Storage::disk('public')->exists($oldPath)) {
@@ -275,32 +248,30 @@ public function uploadChampionshipBanner(Request $request, $championshipId)
             }
         }
 
-        // Salva novo banner em pasta organizada: championships/{id}/banner.ext
         $file = $request->file('banner');
         $extension = $file->getClientOriginalExtension();
         $filename = 'banner.' . $extension;
         $path = $file->storeAs("championships/{$championshipId}", $filename, 'public');
         $url = '/storage/' . $path;
 
-        // Atualiza no banco com URL completo
         $championship->cover_image_url = $url;
         $championship->save();
 
         return response()->json([
-            'message' => 'Capa atualizada com sucesso!',
-            'banner_url' => $url,
-            'cover_image_url' => $url // Compatibilidade
+            'message'         => 'Capa atualizada com sucesso!',
+            'banner_url'      => $url,
+            'cover_image_url' => $url
         ]);
     }
 
     /**
-     * Upload de foto para premiação/arte
+     * Upload award photo.
      */
-    public function uploadAwardPhoto(Request $request)
+    public function uploadAwardPhoto(Request $request): JsonResponse
     {
         $request->validate([
             'photo' => 'required|image|mimes:jpeg,png,jpg,webp,jfif|max:3072',
-            'type' => 'required|string|in:mvp,artilheiro,goleiro,destaque,custom',
+            'type'  => 'required|string|in:mvp,artilheiro,goleiro,destaque,custom',
         ]);
 
         $type = $request->input('type');
@@ -310,22 +281,22 @@ public function uploadAwardPhoto(Request $request)
         $path = $file->storeAs('awards', $filename, 'public');
 
         return response()->json([
-            'message' => 'Foto enviada com sucesso!',
-            'photo_url' => '/storage/' . $path,
+            'message'    => 'Foto enviada com sucesso!',
+            'photo_url'  => '/storage/' . $path,
             'photo_path' => $path
         ]);
     }
 
     /**
-     * Upload genérico de imagem
+     * Upload generic file to specific folder.
      */
-    public function uploadGeneric(Request $request)
+    public function uploadGeneric(Request $request): JsonResponse
     {
         try {
             $file = $request->file('image') ?: $request->file('file');
             
             if (!$file) {
-                \Log::warning("Upload Generic - Nenhum arquivo encontrado. Campos: " . implode(', ', array_keys($request->allFiles())));
+                Log::warning("Upload Generic - Nenhum arquivo encontrado.");
                 return response()->json(['message' => 'Nenhum arquivo enviado.'], 400);
             }
 
@@ -333,48 +304,42 @@ public function uploadGeneric(Request $request)
 
             $request->validate([
                 $fieldName => 'required|image|mimes:jpeg,png,jpg,webp,jfif|max:5120',
-                'folder' => 'nullable|string',
+                'folder'    => 'nullable|string',
             ]);
 
             $folder = $request->input('folder', 'uploads');
             $file = $request->file($fieldName);
 
-            // Prevent path traversal
             if (str_contains($folder, '..')) {
                 return response()->json(['message' => 'Caminho de pasta inválido.'], 400);
             }
 
-            // Garante que a pasta não comece com /
             $folder = ltrim($folder, '/');
 
             $filename = Str::random(20) . '-' . time() . '.' . $file->getClientOriginalExtension();
             $path = $file->storeAs($folder, $filename, 'public');
 
             if (!$path) {
-                throw new \Exception("Falha ao salvar o arquivo no disco.");
+                throw new Exception("Falha ao salvar o arquivo no disco.");
             }
 
             $url = rtrim(config('app.url'), '/') . '/api/storage/' . $path;
 
             return response()->json([
                 'message' => 'Imagem enviada com sucesso!',
-                'url' => $url,
-                'path' => $path
-            ]);
-        } catch (\Exception $e) {
-            \Log::error("Upload Generic Error: " . $e->getMessage(), [
-                'exception' => $e,
-                'input' => $request->all(),
-                'files' => $request->allFiles()
+                'url'     => $url,
+                'path'    => $path
             ]);
+        } catch (Exception $e) {
+            Log::error("Upload Generic Error: " . $e->getMessage());
             return response()->json(['message' => 'Erro ao fazer upload da imagem: ' . $e->getMessage()], 500);
         }
     }
 
     /**
-     * Deletar imagem
+     * Delete image from public disk.
      */
-    public function deleteImage(Request $request)
+    public function deleteImage(Request $request): JsonResponse
     {
         $request->validate([
             'path' => 'required|string',
@@ -394,19 +359,19 @@ public function deleteImage(Request $request)
             'message' => 'Imagem não encontrada.'
         ], 404);
     }
+
     /**
-     * Deletar foto de jogador
+     * Delete specific player photo index.
      */
-    public function deletePlayerPhoto(Request $request, $playerId)
+    public function deletePlayerPhoto(Request $request, int $playerId): JsonResponse
     {
         try {
             $request->validate([
-                'index' => 'nullable|integer|min:0|max:2', // Validar índice (0, 1, 2)
+                'index' => 'nullable|integer|min:0|max:2',
             ]);
 
             $player = User::findOrFail($playerId);
 
-            // Verifica permissão de clube (mesma lógica do upload)
             $user = $request->user();
             if ($user && $user->club_id !== null && $player->club_id !== $user->club_id && $user->id !== $player->id) {
                 $belongsToClubTeam = false;
@@ -439,12 +404,10 @@ public function deletePlayerPhoto(Request $request, $playerId)
 
             $pathToDelete = $currentPhotos[$index];
 
-            // 1. Deletar arquivo principal
             if (Storage::disk('public')->exists($pathToDelete)) {
                 Storage::disk('public')->delete($pathToDelete);
             }
 
-            // 2. Deletar versões processadas se existirem (_nobg.png, _processed.png)
             $info = pathinfo($pathToDelete);
             $basePath = $info['dirname'] . '/' . $info['filename'];
             
@@ -455,41 +418,34 @@ public function deletePlayerPhoto(Request $request, $playerId)
                 }
             }
 
-            // 3. Atualizar array de fotos
             unset($currentPhotos[$index]);
-            $currentPhotos = array_values($currentPhotos); // Reindexa
+            $currentPhotos = array_values($currentPhotos);
 
             $player->photos = $currentPhotos;
-            
-            // 4. Atualizar campos legados e auxiliares
             $player->photo_path = $currentPhotos[0] ?? null;
-            
-            // Se deletou a foto que era a original, limpa o campo original também
-            // ou atualiza para a nova primeira foto
             $player->photo_path_original = $currentPhotos[0] ?? null;
 
             $player->save();
 
             return response()->json([
-                'message' => 'Foto removida com sucesso!',
-                'photos' => $currentPhotos,
+                'message'    => 'Foto removida com sucesso!',
+                'photos'     => $currentPhotos,
                 'photo_path' => $player->photo_path
             ]);
 
-        } catch (\Exception $e) {
-            \Log::error("Delete Player Photo Error: " . $e->getMessage());
+        } catch (Exception $e) {
+            Log::error("Delete Player Photo Error: " . $e->getMessage());
             return response()->json(['message' => 'Erro ao remover a foto.'], 500);
         }
     }
 
     /**
-     * Listar imagens de uma pasta
+     * List all images within public folder.
      */
-    public function listImages(Request $request)
+    public function listImages(Request $request): JsonResponse
     {
         $folder = $request->input('folder', 'uploads');
 
-        // Proteção simples para evitar navegar pastas do sistema
         if (str_contains($folder, '..')) {
             return response()->json(['message' => 'Caminho inválido.'], 400);
         }
@@ -499,46 +455,45 @@ public function listImages(Request $request)
 
         foreach ($files as $file) {
             $images[] = [
-                'name' => basename($file),
-                'path' => $file,
-                'url' => '/storage/' . $file,
-                'size' => Storage::disk('public')->size($file),
+                'name'          => basename($file),
+                'path'          => $file,
+                'url'           => '/storage/' . $file,
+                'size'          => Storage::disk('public')->size($file),
                 'last_modified' => Storage::disk('public')->lastModified($file)
             ];
         }
 
         return response()->json($images);
     }
+
     /**
-     * Deletar foto do próprio perfil (Authenticated User)
+     * Delete my profile photo.
      */
-    public function deleteMyPhoto(Request $request)
+    public function deleteMyPhoto(Request $request): JsonResponse
     {
         $user = $request->user();
         return $this->deletePlayerPhoto($request, $user->id);
     }
 
     /**
-     * Upload de foto do próprio perfil (Authenticated User)
+     * Upload my profile photo.
      */
-    public function uploadMyPhoto(Request $request)
+    public function uploadMyPhoto(Request $request): JsonResponse
     {
         $user = $request->user();
         return $this->uploadPlayerPhoto($request, $user->id);
     }
 
     /**
-     * ?? MÉTODO DE TESTE - Remover fundo de imagem sem autenticação
-     * Para testar a qualidade do rembg com diferentes tipos de fotos
+     * Testing background removal without authorization constraints.
      */
-    public function testRemoveBg(Request $request)
+    public function testRemoveBg(Request $request): JsonResponse
     {
         try {
             $request->validate([
                 'photo' => 'required|image|mimes:jpeg,png,jpg,webp,jfif|max:4096',
             ]);
 
-            // Aumenta o tempo de execução para evitar timeout
             set_time_limit(300);
 
             $file = $request->file('photo');
@@ -546,27 +501,22 @@ public function testRemoveBg(Request $request)
             $path = $file->storeAs('test', $filename, 'public');
 
             $responseData = [
-                'message' => 'Foto enviada com sucesso!',
-                'photo_url' => Storage::url($path),
+                'message'    => 'Foto enviada com sucesso!',
+                'photo_url'  => Storage::url($path),
                 'photo_path' => $path
             ];
 
-            // PROCESSAMENTO DE IA: REMOVER FUNDO
             try {
-                // Check if exec() is available
                 if (!function_exists('exec')) {
                     throw new \RuntimeException('exec() esta desabilitado.');
                 }
 
                 $inputAbsPath = storage_path('app/public/' . $path);
-                // Always output as PNG
                 $filenameNobg = pathinfo($filename, PATHINFO_FILENAME) . '_nobg.png';
                 $outputAbsPath = storage_path('app/public/test/' . $filenameNobg);
 
-                // Script Python
                 $scriptPath = base_path('scripts/remove_bg.py');
 
-                // Determine which python binary is available
                 $pythonBin = null;
                 foreach (['python3', 'python', '/usr/bin/python3', '/usr/local/bin/python3'] as $candidate) {
                     $testOut = [];
@@ -582,10 +532,10 @@ public function testRemoveBg(Request $request)
                     throw new \RuntimeException('Python nao encontrado.');
                 }
 
-                // Command with variables
                 $cacheDir = storage_path('app/public/.u2net');
-                if (!file_exists($cacheDir))
+                if (!file_exists($cacheDir)) {
                     @mkdir($cacheDir, 0775, true);
+                }
 
                 $escapedCacheDir = escapeshellarg($cacheDir);
                 $escapedPythonBin = escapeshellcmd($pythonBin);
@@ -595,7 +545,7 @@ public function testRemoveBg(Request $request)
 
                 $command = "export U2NET_HOME={$escapedCacheDir} && export NUMBA_CACHE_DIR={$escapedCacheDir} && {$escapedPythonBin} {$escapedScriptPath} {$escapedInputAbsPath} {$escapedOutputAbsPath} 2>&1";
 
-                \Log::info("Lab AI Test - Command: " . $command);
+                Log::info("Lab AI Test - Command: " . $command);
 
                 $output = [];
                 $returnVar = 0;
@@ -607,7 +557,6 @@ public function testRemoveBg(Request $request)
                 $processingTime = round($endTime - $startTime, 2);
 
                 if ($returnVar === 0 && file_exists($outputAbsPath)) {
-                    // Sucesso
                     $pathNobg = 'test/' . $filenameNobg;
 
                     $responseData['photo_nobg_url'] = url('api/storage/' . $pathNobg);
@@ -617,18 +566,18 @@ public function testRemoveBg(Request $request)
                     $responseData['original_size'] = @filesize($inputAbsPath);
                     $responseData['processed_size'] = @filesize($outputAbsPath);
                 } else {
-                    \Log::error("Lab AI Test - Failed: " . json_encode($output));
+                    Log::error("Lab AI Test - Failed: " . json_encode($output));
                     $responseData['ai_error'] = 'Falha ao processar IA. Código: ' . $returnVar;
                     $responseData['ai_output'] = $output;
                     $responseData['command'] = $command;
                 }
-            } catch (\Throwable $e) {
+            } catch (Throwable $e) {
                 $responseData['ai_error'] = 'Erro: ' . $e->getMessage();
             }
 
             return response()->json($responseData);
-        } catch (\Exception $e) {
-            \Log::error("Test Remove BG Error: " . $e->getMessage());
+        } catch (Exception $e) {
+            Log::error("Test Remove BG Error: " . $e->getMessage());
             return response()->json([
                 'message' => 'Erro ao processar imagem: ' . $e->getMessage()
             ], 500);
@@ -636,23 +585,21 @@ public function testRemoveBg(Request $request)
     }
 
     /**
-     * Upload unificado de imagem (player, team, championship)
+     * Unified upload routing method.
      */
-    public function uploadImage(Request $request)
+    public function uploadImage(Request $request): JsonResponse
     {
         try {
             $request->validate([
                 'file' => 'required|image|mimes:jpeg,png,jpg,webp,jfif|max:4096',
                 'type' => 'required|string|in:player,team,championship',
-                'id' => 'required|integer',
+                'id'   => 'required|integer',
             ]);
 
             $type = $request->input('type');
             $id = $request->input('id');
 
-            // Redireciona para o método específico
             if ($type === 'player') {
-                // Cria um novo request com o campo 'photo' esperado
                 $photoRequest = new Request();
                 $photoRequest->merge($request->all());
                 $photoRequest->files->set('photo', $request->file('file'));
@@ -660,14 +607,12 @@ public function uploadImage(Request $request)
 
                 return $this->uploadPlayerPhoto($photoRequest, $id);
             } elseif ($type === 'team') {
-                // Cria um novo request com o campo 'logo' esperado
                 $logoRequest = new Request();
                 $logoRequest->files->set('logo', $request->file('file'));
                 $logoRequest->setUserResolver($request->getUserResolver());
 
                 return $this->uploadTeamLogo($logoRequest, $id);
             } elseif ($type === 'championship') {
-                // Cria um novo request com o campo 'logo' esperado
                 $logoRequest = new Request();
                 $logoRequest->files->set('logo', $request->file('file'));
                 $logoRequest->setUserResolver($request->getUserResolver());
@@ -676,31 +621,26 @@ public function uploadImage(Request $request)
             }
 
             return response()->json(['message' => 'Tipo inválido'], 400);
-        } catch (\Exception $e) {
-            \Log::error("Upload Image Error: " . $e->getMessage());
+        } catch (Exception $e) {
+            Log::error("Upload Image Error: " . $e->getMessage());
             return response()->json(['message' => 'Erro ao fazer upload: ' . $e->getMessage()], 500);
         }
     }
+
     /**
-     * Diagnóstico do ambiente de IA (exec, python, rembg)
-     * Acessível em GET /admin/test-ai-env
+     * AI removal environment diagnosis.
      */
-    public function testAiEnv()
+    public function testAiEnv(): JsonResponse
     {
         $result = [];
 
-        // 1. exec() disponível?
         $result['exec_available'] = function_exists('exec');
-
-        // 2. PHP disable_functions
         $result['php_disable_functions'] = ini_get('disable_functions') ?: '(nenhuma)';
 
-        // 3. Script path
         $scriptPath = base_path('scripts/remove_bg.py');
         $result['script_path'] = $scriptPath;
         $result['script_exists'] = file_exists($scriptPath);
 
-        // 4. Storage writable?
         $storageDir = storage_path('app/public/players');
         $result['storage_dir'] = $storageDir;
         $result['storage_writable'] = is_writable(storage_path('app/public'));
@@ -711,7 +651,6 @@ public function testAiEnv()
             return response()->json($result);
         }
 
-        // 5. Qual Python está disponível?
         $pythonBin = null;
         foreach (['python3', 'python', '/usr/bin/python3', '/usr/local/bin/python3'] as $candidate) {
             $out = [];
@@ -719,7 +658,7 @@ public function testAiEnv()
             @exec("{$candidate} --version 2>&1", $out, $ret);
             $result['python_test'][$candidate] = [
                 'exit_code' => $ret,
-                'output' => implode(' ', $out),
+                'output'    => implode(' ', $out),
             ];
             if ($ret === 0 && !$pythonBin) {
                 $pythonBin = $candidate;
@@ -727,17 +666,15 @@ public function testAiEnv()
         }
 
         $result['python_bin'] = $pythonBin;
-        $result['python_found'] = !!$pythonBin;
+        $result['python_found'] = !empty($pythonBin);
 
         if ($pythonBin) {
-            // 6. rembg instalado?
             $out = [];
             $ret = -1;
             @exec("{$pythonBin} -c \"import rembg; print('rembg ok')\" 2>&1", $out, $ret);
             $result['rembg_installed'] = ($ret === 0);
             $result['rembg_check_output'] = implode(' ', $out);
 
-            // 7. PIL instalado?
             $out2 = [];
             $ret2 = -1;
             @exec("{$pythonBin} -c \"from PIL import Image; print('PIL ok')\" 2>&1", $out2, $ret2);
@@ -748,18 +685,17 @@ public function testAiEnv()
     }
 
     /**
-     * Upload de regulamento de campeonato (PDF ou Imagem)
+     * Upload PDF or image regulation for the championship.
      */
-    public function uploadChampionshipRegulation(Request $request, $championshipId)
+    public function uploadChampionshipRegulation(Request $request, int $championshipId): JsonResponse
     {
         try {
             $request->validate([
-                'file' => 'required|file|mimes:pdf,jpg,jpeg,png,webp|max:10240', // 10MB
+                'file' => 'required|file|mimes:pdf,jpg,jpeg,png,webp|max:10240',
             ]);
 
             $championship = Championship::findOrFail($championshipId);
 
-            // Verifica permissão de clube
             $user = $request->user();
             if ($user->club_id !== null && $championship->club_id !== $user->club_id) {
                 return response()->json([
@@ -767,27 +703,24 @@ public function uploadChampionshipRegulation(Request $request, $championshipId)
                 ], 403);
             }
 
-            // Remove regulamento antigo se existir
             if ($championship->regulation_path && Storage::disk('public')->exists($championship->regulation_path)) {
                 Storage::disk('public')->delete($championship->regulation_path);
             }
 
-            // Salva novo regulamento
             $file = $request->file('file');
             $filename = 'regulamento-' . time() . '.' . $file->getClientOriginalExtension();
             $path = $file->storeAs("championships/{$championshipId}", $filename, 'public');
 
-            // Atualiza no banco
             $championship->regulation_path = $path;
             $championship->save();
 
             return response()->json([
-                'message' => 'Regulamento enviado com sucesso!',
+                'message'         => 'Regulamento enviado com sucesso!',
                 'regulation_path' => $path,
-                'regulation_url' => url('api/storage/' . $path)
+                'regulation_url'  => url('api/storage/' . $path)
             ]);
-        } catch (\Exception $e) {
-            \Log::error("Upload Regulation Error: " . $e->getMessage());
+        } catch (Exception $e) {
+            Log::error("Upload Regulation Error: " . $e->getMessage());
             return response()->json(['message' => 'Erro ao enviar regulamento: ' . $e->getMessage()], 500);
         }
     }
diff --git a/app/Http/Controllers/Admin/IndividualArtController.php b/app/Http/Controllers/Admin/IndividualArtController.php
index 7681c00..bbbb672 100644
--- a/app/Http/Controllers/Admin/IndividualArtController.php
+++ b/app/Http/Controllers/Admin/IndividualArtController.php
@@ -4,10 +4,12 @@
 
 use App\Http\Controllers\Controller;
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
 use App\Models\Championship;
 use App\Models\User;
 use App\Models\RaceResult;
 use App\Models\SystemSetting;
+use App\Models\Club;
 use Illuminate\Support\Str;
 use App\Http\Controllers\Admin\Traits\ArtGdTrait;
 use App\Http\Controllers\Admin\Traits\ArtCardTrait;
@@ -16,9 +18,9 @@ class IndividualArtController extends Controller
 {
     use ArtGdTrait, ArtCardTrait;
 
-    private $fontPath;
-    private $secondaryFontPath;
-    private $templatesPath;
+    private string $fontPath;
+    private string $secondaryFontPath;
+    private string $templatesPath;
 
     public function __construct()
     {
@@ -28,9 +30,9 @@ public function __construct()
     }
 
     /**
-     * Salva ou atualiza um template de arte individual.
+     * Save or update an individual art template.
      */
-    public function saveTemplate(Request $request)
+    public function saveTemplate(Request $request): JsonResponse
     {
         $name = $request->input('name');
         $elements = $request->input('elements');
@@ -40,10 +42,10 @@ public function saveTemplate(Request $request)
 
         $key = $this->getTemplateKey($name);
         $data = [
-            'elements' => $elements, 
-            'canvas' => $canvas, 
-            'bg_url' => $bgUrl, 
-            'name' => $name,
+            'elements'      => $elements, 
+            'canvas'        => $canvas, 
+            'bg_url'        => $bgUrl, 
+            'name'          => $name,
             'is_individual' => true
         ];
 
@@ -51,8 +53,7 @@ public function saveTemplate(Request $request)
             $championship = Championship::find($championshipId);
             if ($championship) {
                 $user = auth()->user();
-                // Permitir se for super admin OU se o clube bater
-                if (($user && $user->role === 'super_admin') || ($user && $user->club_id == $championship->club_id)) {
+                if (($user && $user->isSuperAdmin()) || ($user && $user->club_id == $championship->club_id)) {
                     $settings = $championship->art_settings ?? [];
                     if (!isset($settings['templates'])) {
                         $settings['templates'] = [];
@@ -67,7 +68,6 @@ public function saveTemplate(Request $request)
             return response()->json(['message' => 'Erro ao salvar template. Verifique permissões.'], 403);
         }
 
-        // Caso base: salvar como configuração do sistema (fallback global)
         SystemSetting::updateOrCreate(
             ['key' => $key],
             ['value' => json_encode($data), 'group' => 'art_templates_individual']
@@ -77,9 +77,9 @@ public function saveTemplate(Request $request)
     }
 
     /**
-     * Recupera um template de arte individual.
+     * Retrieve individual art template.
      */
-    public function getTemplate(Request $request)
+    public function getTemplate(Request $request): JsonResponse
     {
         $name = $request->query('name');
         $championshipId = $request->query('championship_id');
@@ -95,7 +95,7 @@ public function getTemplate(Request $request)
         if ($championship) {
             $club = $championship->club;
         } elseif ($user && $user->club_id) {
-            $club = \App\Models\Club::find($user->club_id);
+            $club = Club::find($user->club_id);
         }
 
         $setting = SystemSetting::where('key', $key)->first();
@@ -103,7 +103,6 @@ public function getTemplate(Request $request)
 
         $responseTemplate = null;
 
-        // Hierarquia: Campeonato -> Clube -> Sistema (SystemSetting) -> Default (Hardcoded no Trait)
         if ($championship && !empty($championship->art_settings['templates'][$key])) {
             $responseTemplate = $championship->art_settings['templates'][$key];
         } elseif ($club && !empty($club->art_settings['templates'][$key])) {
@@ -113,7 +112,6 @@ public function getTemplate(Request $request)
         }
 
         if ($responseTemplate) {
-            // Se houver novos elementos no default que não estão no salvo, mescla (evita quebrar ao adicionar features)
             if ($default && isset($default['elements'])) {
                 $existingIds = array_column($responseTemplate['elements'] ?? [], 'id');
                 foreach ($default['elements'] as $defEl) {
@@ -129,21 +127,17 @@ public function getTemplate(Request $request)
     }
 
     /**
-     * Gera a arte individual de um atleta.
+     * Generate individual athlete card image.
      */
-    public function individualAthleteArt($championshipId, $resultId, $category, Request $request)
+    public function individualAthleteArt(int $championshipId, int $resultId, string $category, Request $request)
     {
         $championship = Championship::with(['sport', 'club'])->findOrFail($championshipId);
-        
-        // No contexto de campeonatos individuais (corridas), o ID passado pelo frontend 
-        // na tela de Gestão de Inscritos refere-se ao ID da inscrição (RaceResult).
         $result = RaceResult::with('user')->find($resultId);
         
         if ($result && $result->user) {
             $athlete = $result->user;
             $displayName = $result->name ?: $athlete->name;
         } else {
-            // Fallback para caso o ID seja realmente o do User (legado ou outros fluxos)
             $athlete = User::findOrFail($resultId);
             $displayName = $athlete->name;
         }
@@ -151,38 +145,39 @@ public function individualAthleteArt($championshipId, $resultId, $category, Requ
         $this->loadClubResources($championship->club);
         $sport = strtolower($championship->sport->name ?? 'individual');
 
-        // Parâmetros extras passados via query (Rank, Nome da Categoria, etc)
         $rank = $request->query('rank', '');
         $catName = $request->query('category_name', '');
 
-        // Mapeamento de categorias internas para os templates
         $cardCategory = $category;
-        if ($category === 'confirmed') $cardCategory = 'atleta_confirmado';
-        if ($category === 'result') $cardCategory = 'colocacao';
+        if ($category === 'confirmed') {
+            $cardCategory = 'atleta_confirmado';
+        }
+        if ($category === 'result') {
+            $cardCategory = 'colocacao';
+        }
 
         return $this->createCard($athlete, $championship, $sport, $cardCategory, null, null, null, $championship->club, [
             '{COLOCACAO}' => $rank,
             '{CATEGORIA}' => mb_strtoupper($catName),
-            '{ATLETA}' => mb_strtoupper($displayName),
-            '{JOGADOR}' => mb_strtoupper($displayName)
+            '{ATLETA}'    => mb_strtoupper($displayName),
+            '{JOGADOR}'   => mb_strtoupper($displayName)
         ]);
     }
 
     /**
-     * Define a chave do template baseado no nome amigável.
+     * Map friendly name to unique system template key.
      */
-    private function getTemplateKey($name)
+    private function getTemplateKey(?string $name): string
     {
         $map = [
-            'Atleta Confirmado' => 'art_layout_individual_confirmed',
-            'Colocação do Atleta' => 'art_layout_individual_placement',
+            'Atleta Confirmado'    => 'art_layout_individual_confirmed',
+            'Colocação do Atleta'  => 'art_layout_individual_placement',
         ];
         
-        if (isset($map[$name])) {
+        if ($name && isset($map[$name])) {
             return $map[$name];
         }
 
-        // Fallback para nomes customizados
-        return 'art_layout_individual_' . Str::slug($name);
+        return 'art_layout_individual_' . Str::slug($name ?? 'default');
     }
 }
diff --git a/app/Http/Controllers/Admin/NotificationController.php b/app/Http/Controllers/Admin/NotificationController.php
index 9da8422..261bce4 100644
--- a/app/Http/Controllers/Admin/NotificationController.php
+++ b/app/Http/Controllers/Admin/NotificationController.php
@@ -4,20 +4,20 @@
 
 use App\Http\Controllers\Controller;
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
 use App\Models\User;
-use App\Models\GameMatch;
 
 class NotificationController extends Controller
 {
     /**
-     * Enviar notificação manual para usuários
+     * Send manual notification to users.
      */
-    public function send(Request $request)
+    public function send(Request $request): JsonResponse
     {
         $request->validate([
-            'title' => 'required|string',
-            'body' => 'required|string',
-            'target' => 'required|in:all,club,team,user',
+            'title'     => 'required|string',
+            'body'      => 'required|string',
+            'target'    => 'required|in:all,club,team,user',
             'target_id' => 'nullable|integer',
         ]);
 
@@ -25,7 +25,6 @@ public function send(Request $request)
         $body = $request->body;
         $tokens = [];
 
-        // Selecionar tokens baseado no alvo
         switch ($request->target) {
             case 'all':
                 $tokens = User::whereNotNull('device_token')->pluck('device_token')->toArray();
@@ -36,12 +35,10 @@ public function send(Request $request)
                     ->pluck('device_token')->toArray();
                 break;
             case 'team':
-                // Lógica para pegar tokens de jogadores de um time (precisa de relacionamento users <-> teams)
-                // Exemplo simplificado:
-                // $tokens = User::whereNotNull('device_token')->where('team_id', $request->target_id)->pluck('device_token')->toArray();
+                // Optional logic for team players placeholder
                 break;
             case 'user':
-                $user = User::find($request->target_id);
+                $user = User::select(['id', 'device_token'])->find($request->target_id);
                 if ($user && $user->device_token) {
                     $tokens[] = $user->device_token;
                 }
@@ -52,20 +49,19 @@ public function send(Request $request)
             return response()->json(['message' => 'Nenhum dispositivo encontrado para envio.'], 404);
         }
 
-        // Enviar via Firebase (Mock da função)
         $result = $this->sendToFirebase($tokens, $title, $body);
 
         return response()->json([
             'message' => 'Notificações enviadas (simulado).',
-            'count' => count($tokens),
-            'result' => $result
+            'count'   => count($tokens),
+            'result'  => $result
         ]);
     }
 
     /**
-     * Endpoint para salvar token do dispositivo (Mobile chama isso ao logar)
+     * Save device token for logged-in user.
      */
-    public function storeToken(Request $request)
+    public function storeToken(Request $request): JsonResponse
     {
         $request->validate([
             'token' => 'required|string',
@@ -79,19 +75,10 @@ public function storeToken(Request $request)
     }
 
     /**
-     * Função privada para envio real (Firebase)
+     * Simulation of Firebase FCM push.
      */
-    private function sendToFirebase(array $tokens, string $title, string $body)
+    private function sendToFirebase(array $tokens, string $title, string $body): array
     {
-        // Aqui entraria a integração com Firebase Cloud Messaging (FCM)
-        // usando curl ou biblioteca google/apiclient
-
-        // Exemplo de payload:
-        // {
-        //   "registration_ids": $tokens,
-        //   "notification": { "title": $title, "body": $body }
-        // }
-
         return ['success' => true, 'mock' => true];
     }
 }
diff --git a/app/Http/Controllers/Admin/QRValidationController.php b/app/Http/Controllers/Admin/QRValidationController.php
index 9c0a37f..b1c15ed 100644
--- a/app/Http/Controllers/Admin/QRValidationController.php
+++ b/app/Http/Controllers/Admin/QRValidationController.php
@@ -4,28 +4,29 @@
 
 use App\Http\Controllers\Controller;
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
 use App\Models\User;
 use App\Models\GameMatch;
+use App\Models\RaceResult;
 use Carbon\Carbon;
 
 class QRValidationController extends Controller
 {
     /**
-     * Validar carteirinha digital
+     * Validar carteirinha digital.
      */
-    public function validateWallet(Request $request)
+    public function validateWallet(Request $request): JsonResponse
     {
         $request->validate([
             'qr_code' => 'required|string',
         ]);
 
         try {
-            // Decodifica QR Code (formato: "WALLET:{user_id}:{timestamp}")
             $qrData = $request->input('qr_code');
 
             if (!str_starts_with($qrData, 'WALLET:')) {
                 return response()->json([
-                    'valid' => false,
+                    'valid'   => false,
                     'message' => 'QR Code inválido.'
                 ], 400);
             }
@@ -33,7 +34,7 @@ public function validateWallet(Request $request)
             $parts = explode(':', $qrData);
             if (count($parts) < 3) {
                 return response()->json([
-                    'valid' => false,
+                    'valid'   => false,
                     'message' => 'Formato de QR Code inválido.'
                 ], 400);
             }
@@ -41,59 +42,55 @@ public function validateWallet(Request $request)
             $userId = $parts[1];
             $timestamp = $parts[2];
 
-            // Verifica se QR Code não expirou (válido por 5 minutos)
             $qrTime = Carbon::createFromTimestamp($timestamp);
             if ($qrTime->diffInMinutes(now()) > 5) {
                 return response()->json([
-                    'valid' => false,
+                    'valid'   => false,
                     'message' => 'QR Code expirado. Atualize a carteirinha.'
                 ], 400);
             }
 
-            // Busca usuário
             $user = User::find($userId);
             if (!$user) {
                 return response()->json([
-                    'valid' => false,
+                    'valid'   => false,
                     'message' => 'Usuário não encontrado.'
                 ], 404);
             }
 
-            // Retorna dados do usuário
             return response()->json([
-                'valid' => true,
+                'valid'   => true,
                 'message' => 'Carteirinha válida!',
-                'user' => [
-                    'id' => $user->id,
-                    'name' => $user->name,
-                    'email' => $user->email,
-                    'cpf' => $user->cpf,
+                'user'    => [
+                    'id'         => $user->id,
+                    'name'       => $user->name,
+                    'email'      => $user->email,
+                    'cpf'        => $user->cpf,
                     'photo_path' => $user->photo_path,
-                    'club_id' => $user->club_id,
+                    'club_id'    => $user->club_id,
                 ]
             ]);
 
         } catch (\Exception $e) {
             return response()->json([
-                'valid' => false,
+                'valid'   => false,
                 'message' => 'Erro ao validar QR Code.',
-                'error' => $e->getMessage()
+                'error'   => $e->getMessage()
             ], 500);
         }
     }
 
     /**
-     * Check-in de jogador para partida
+     * Check-in de jogador para partida.
      */
-    public function checkInPlayer(Request $request)
+    public function checkInPlayer(Request $request): JsonResponse
     {
         $request->validate([
-            'qr_code' => 'required|string',
+            'qr_code'  => 'required|string',
             'match_id' => 'required|exists:game_matches,id',
         ]);
 
         try {
-            // Valida QR Code
             $qrData = $request->input('qr_code');
 
             if (!str_starts_with($qrData, 'WALLET:')) {
@@ -106,28 +103,21 @@ public function checkInPlayer(Request $request)
             $parts = explode(':', $qrData);
             $userId = $parts[1];
 
-            // Busca usuário e partida
             $user = User::findOrFail($userId);
-            $match = GameMatch::with(['homeTeam', 'awayTeam'])->findOrFail($request->input('match_id'));
-
-            // Verifica se jogador pertence a uma das equipes
-            // (Aqui você pode adicionar lógica mais complexa de verificação)
-
-            // Registra check-in (pode criar uma tabela de check-ins se necessário)
-            // Por enquanto, apenas retorna sucesso
+            $match = GameMatch::with(['homeTeam:id,name', 'awayTeam:id,name'])->findOrFail($request->input('match_id'));
 
             return response()->json([
                 'success' => true,
                 'message' => 'Check-in realizado com sucesso!',
-                'player' => [
-                    'id' => $user->id,
-                    'name' => $user->name,
+                'player'  => [
+                    'id'         => $user->id,
+                    'name'       => $user->name,
                     'photo_path' => $user->photo_path,
                 ],
-                'match' => [
-                    'id' => $match->id,
-                    'home_team' => $match->homeTeam->name ?? 'N/A',
-                    'away_team' => $match->awayTeam->name ?? 'N/A',
+                'match'   => [
+                    'id'         => $match->id,
+                    'home_team'  => $match->homeTeam->name ?? 'N/A',
+                    'away_team'  => $match->awayTeam->name ?? 'N/A',
                     'start_time' => $match->start_time,
                 ]
             ]);
@@ -136,27 +126,26 @@ public function checkInPlayer(Request $request)
             return response()->json([
                 'success' => false,
                 'message' => 'Erro ao fazer check-in.',
-                'error' => $e->getMessage()
+                'error'   => $e->getMessage()
             ], 500);
         }
     }
 
     /**
-     * Validar ingresso (se houver sistema de ingressos)
+     * Validar ingresso.
      */
-    public function validateTicket(Request $request)
+    public function validateTicket(Request $request): JsonResponse
     {
         $request->validate([
             'qr_code' => 'required|string',
         ]);
 
         try {
-            // Decodifica QR Code (formato: "TICKET:{ticket_id}:{match_id}")
             $qrData = $request->input('qr_code');
 
             if (!str_starts_with($qrData, 'TICKET:')) {
                 return response()->json([
-                    'valid' => false,
+                    'valid'   => false,
                     'message' => 'QR Code de ingresso inválido.'
                 ], 400);
             }
@@ -164,7 +153,7 @@ public function validateTicket(Request $request)
             $parts = explode(':', $qrData);
             if (count($parts) < 3) {
                 return response()->json([
-                    'valid' => false,
+                    'valid'   => false,
                     'message' => 'Formato de QR Code inválido.'
                 ], 400);
             }
@@ -172,41 +161,37 @@ public function validateTicket(Request $request)
             $ticketId = $parts[1];
             $matchId = $parts[2];
 
-            // Aqui você implementaria a lógica de validação de ingressos
-            // Por enquanto, retorna exemplo
-
             return response()->json([
-                'valid' => true,
+                'valid'  => true,
                 'message' => 'Ingresso válido!',
                 'ticket' => [
-                    'id' => $ticketId,
+                    'id'       => $ticketId,
                     'match_id' => $matchId,
-                    'type' => 'VIP',
-                    'seat' => 'A-15',
+                    'type'     => 'VIP',
+                    'seat'     => 'A-15',
                 ]
             ]);
 
         } catch (\Exception $e) {
             return response()->json([
-                'valid' => false,
+                'valid'   => false,
                 'message' => 'Erro ao validar ingresso.',
-                'error' => $e->getMessage()
+                'error'   => $e->getMessage()
             ], 500);
         }
     }
 
     /**
-     * Validar entrega de kit de corrida
+     * Validar entrega de kit de corrida.
      */
-    public function validateRaceKit($id)
+    public function validateRaceKit(int $id): JsonResponse
     {
         try {
-            $result = \App\Models\RaceResult::with(['user', 'race.championship', 'category'])->findOrFail($id);
+            $result = RaceResult::with(['user:id,name,cpf,photo_path', 'race.championship:id,name', 'category:id,name'])->findOrFail($id);
 
-            // Se for apenas GET, apenas retorna os dados para visualização na tela do Admin
             return response()->json([
                 'success' => true,
-                'result' => $result
+                'result'  => $result
             ]);
 
         } catch (\Exception $e) {
@@ -218,12 +203,12 @@ public function validateRaceKit($id)
     }
 
     /**
-     * Confirmar entrega de kit
+     * Confirmar entrega de kit.
      */
-    public function confirmKitDelivery($id)
+    public function confirmKitDelivery(int $id): JsonResponse
     {
         try {
-            $result = \App\Models\RaceResult::findOrFail($id);
+            $result = RaceResult::findOrFail($id);
 
             if ($result->status_payment !== 'paid') {
                 return response()->json([
@@ -233,7 +218,7 @@ public function confirmKitDelivery($id)
             }
 
             $result->update([
-                'kit_delivered' => true,
+                'kit_delivered'    => true,
                 'kit_delivered_at' => now()
             ]);
 
@@ -251,21 +236,19 @@ public function confirmKitDelivery($id)
     }
 
     /**
-     * Gerar QR Code para carteirinha (usado pelo mobile)
+     * Gerar QR Code para carteirinha (usado pelo mobile).
      */
-    public function generateWalletQR(Request $request)
+    public function generateWalletQR(Request $request): JsonResponse
     {
         $user = $request->user();
-
-        // Gera QR Code com timestamp atual
         $qrContent = "WALLET:{$user->id}:" . time();
 
         return response()->json([
             'qr_code_content' => $qrContent,
-            'expires_in' => 300, // 5 minutos
-            'user' => [
-                'id' => $user->id,
-                'name' => $user->name,
+            'expires_in'      => 300,
+            'user'            => [
+                'id'      => $user->id,
+                'name'    => $user->name,
                 'club_id' => $user->club_id,
             ]
         ]);
diff --git a/app/Http/Controllers/Admin/RaceInscriptionController.php b/app/Http/Controllers/Admin/RaceInscriptionController.php
index 0211ee9..14942e1 100644
--- a/app/Http/Controllers/Admin/RaceInscriptionController.php
+++ b/app/Http/Controllers/Admin/RaceInscriptionController.php
@@ -4,6 +4,7 @@
 
 use App\Http\Controllers\Controller;
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
 use App\Models\Championship;
 use App\Models\Race;
 use App\Models\RaceResult;
@@ -15,28 +16,36 @@
 use App\Mail\InscriptionPaymentMail;
 use App\Http\Controllers\Admin\ImageUploadController;
 use Illuminate\Support\Facades\DB;
-
 use Illuminate\Support\Facades\Log;
 use Illuminate\Support\Facades\Mail;
 use Illuminate\Support\Str;
+use Carbon\Carbon;
 
 class RaceInscriptionController extends Controller
 {
-    // Listar minhas inscrições (para o app/site)
-    public function myInscriptions(Request $request)
+    /**
+     * List my inscriptions.
+     */
+    public function myInscriptions(Request $request): JsonResponse
     {
         $results = RaceResult::where('user_id', $request->user()->id)
-            ->with(['race.championship', 'category.parent', 'user'])
+            ->with([
+                'race:id,championship_id,start_datetime,location_name', 
+                'race.championship:id,name,logo_url', 
+                'category.parent:id,name', 
+                'user:id,name,cpf'
+            ])
             ->orderBy('created_at', 'desc')
             ->get();
 
         return response()->json($results);
     }
 
-    // Registro Público (Site)
-    public function publicRegister(Request $request, $championshipId)
+    /**
+     * Public athlete registration.
+     */
+    public function publicRegister(Request $request, int $championshipId): JsonResponse
     {
-        // Decodificar JSON vindo do FormData
         if ($request->has('gifts') && is_string($request->gifts)) {
             $decoded = json_decode($request->gifts, true);
             $request->merge(['gifts' => is_array($decoded) ? $decoded : []]);
@@ -47,23 +56,23 @@ public function publicRegister(Request $request, $championshipId)
         }
 
         $request->validate([
-            'name' => 'required|string|max:255',
-            'email' => 'required|email',
-            'phone' => 'required|string|max:255',
-            'document' => 'required|string|max:255',
-            'birth_date' => 'required|date',
-            'gender' => 'required|string|in:M,F,O',
-            'category_id' => 'required|exists:categories,id',
-            'photo' => 'nullable|image|mimes:jpeg,png,jpg|max:4096',
-            'is_pcd' => 'nullable|boolean',
-            'pcd_document' => 'nullable|file|mimes:jpeg,png,jpg,pdf|max:4096',
-            'gifts' => 'nullable|array',
-            'shop_items' => 'nullable|array',
+            'name'                    => 'required|string|max:255',
+            'email'                   => 'required|email',
+            'phone'                   => 'required|string|max:255',
+            'document'                => 'required|string|max:255',
+            'birth_date'              => 'required|date',
+            'gender'                  => 'required|string|in:M,F,O',
+            'category_id'             => 'required|exists:categories,id',
+            'photo'                   => 'nullable|image|mimes:jpeg,png,jpg|max:4096',
+            'is_pcd'                  => 'nullable|boolean',
+            'pcd_document'            => 'nullable|file|mimes:jpeg,png,jpg,pdf|max:4096',
+            'gifts'                   => 'nullable|array',
+            'shop_items'              => 'nullable|array',
             'shop_items.*.product_id' => 'required|exists:products,id',
-            'shop_items.*.quantity' => 'required|integer|min:1',
-            'shop_items.*.variant' => 'nullable|string',
-            'coupon_code' => 'nullable|string',
-            'payment_method' => 'nullable|string|in:PIX,CREDIT_CARD,BOLETO'
+            'shop_items.*.quantity'   => 'required|integer|min:1',
+            'shop_items.*.variant'    => 'nullable|string',
+            'coupon_code'             => 'nullable|string',
+            'payment_method'          => 'nullable|string|in:PIX,CREDIT_CARD,BOLETO'
         ]);
 
         $race = Race::where('championship_id', $championshipId)->first();
@@ -72,33 +81,26 @@ public function publicRegister(Request $request, $championshipId)
             if ($championship && $championship->format === 'racing') {
                 $race = Race::create([
                     'championship_id' => $championshipId,
-                    'start_datetime' => $championship->start_date,
-                    'location_name' => 'A definir',
-                    'kits_info' => 'Informações do kit em breve'
+                    'start_datetime'  => $championship->start_date,
+                    'location_name'   => 'A definir',
+                    'kits_info'       => 'Informações do kit em breve'
                 ]);
             } else {
                 return response()->json(['error' => 'Evento não encontrado ou não configurado como corrida.'], 404);
             }
         }
 
-        // 1. Carregar Categoria Enviada e Identificar a Principal (Pai)
-        // Mesmo que o frontend envie uma subcategoria, o preço base vem da pai.
-        $selectedCategory = Category::with(['parent', 'children'])->findOrFail($request->category_id);
+        $selectedCategory = Category::with(['parent:id,name,price,gender', 'children:id,parent_id,name,min_age,max_age,gender,price'])->findOrFail($request->category_id);
         $mainCategory = $selectedCategory->parent_id ? $selectedCategory->parent : $selectedCategory;
 
-        // A categoria final de registro (inicialmente a selecionada, 
-        // mas pode mudar via automatização de idade)
         $category = $selectedCategory;
 
-        // Validar Idade na data 31/12 do ano do campeonato
-        $eventYear = $race->championship->start_date ? \Carbon\Carbon::parse($race->championship->start_date)->year : date('Y');
-        $referenceDate = \Carbon\Carbon::createFromDate($eventYear, 12, 31);
-        $athleteAge = (int) $referenceDate->diffInYears(\Carbon\Carbon::parse($request->birth_date), true);
+        $eventYear = $race->championship->start_date ? Carbon::parse($race->championship->start_date)->year : date('Y');
+        $referenceDate = Carbon::createFromDate($eventYear, 12, 31);
+        $athleteAge = (int) $referenceDate->diffInYears(Carbon::parse($request->birth_date), true);
 
-        // A subcategoria deve ser automática conforme idade e gênero se a categoria principal tiver filhos.
         $subCategory = null;
         if ($mainCategory->children->count() > 0) {
-            // 1. Prioridade PCD: Se é PCD, busca categoria que tenha "PcD" no nome
             if ($request->boolean('is_pcd')) {
                 $subCategory = $mainCategory->children
                     ->filter(function ($child) use ($request) {
@@ -107,12 +109,20 @@ public function publicRegister(Request $request, $championshipId)
                         $childGender = strtolower($child->gender ?? '');
                         if ($childGender && $childGender !== 'mixed' && $childGender !== 'misto') {
                             $userGender = strtolower($request->gender);
-                            if ($userGender === 'm') $userGender = 'male';
-                            if ($userGender === 'f') $userGender = 'female';
+                            if ($userGender === 'm') {
+                                $userGender = 'male';
+                            }
+                            if ($userGender === 'f') {
+                                $userGender = 'female';
+                            }
 
                             $normalizedChildGender = $childGender;
-                            if ($normalizedChildGender === 'm') $normalizedChildGender = 'male';
-                            if ($normalizedChildGender === 'f') $normalizedChildGender = 'female';
+                            if ($normalizedChildGender === 'm') {
+                                $normalizedChildGender = 'male';
+                            }
+                            if ($normalizedChildGender === 'f') {
+                                $normalizedChildGender = 'female';
+                            }
 
                             if ($userGender !== $normalizedChildGender) {
                                 return false;
@@ -124,27 +134,32 @@ public function publicRegister(Request $request, $championshipId)
                     ->first();
             }
 
-            // 2. Se não achou via PcD (ou não é PcD), busca por Idade e Gênero
             if (!isset($subCategory)) {
                 $subCategory = $mainCategory->children
                     ->filter(function ($child) use ($athleteAge, $request) {
-                        // Validar Idade
                         $min = $child->min_age ?? 0;
                         $max = $child->max_age ?? 999;
                         if ($athleteAge < $min || $athleteAge > $max) {
                             return false;
                         }
 
-                        // Validar Gênero (se a subcategoria tiver gênero específico)
                         $childGender = strtolower($child->gender ?? '');
                         if ($childGender && $childGender !== 'mixed' && $childGender !== 'misto') {
                             $userGender = strtolower($request->gender);
-                            if ($userGender === 'm') $userGender = 'male';
-                            if ($userGender === 'f') $userGender = 'female';
+                            if ($userGender === 'm') {
+                                $userGender = 'male';
+                            }
+                            if ($userGender === 'f') {
+                                $userGender = 'female';
+                            }
 
                             $normalizedChildGender = $childGender;
-                            if ($normalizedChildGender === 'm') $normalizedChildGender = 'male';
-                            if ($normalizedChildGender === 'f') $normalizedChildGender = 'female';
+                            if ($normalizedChildGender === 'm') {
+                                $normalizedChildGender = 'male';
+                            }
+                            if ($normalizedChildGender === 'f') {
+                                $normalizedChildGender = 'female';
+                            }
 
                             if ($userGender !== $normalizedChildGender) {
                                 return false;
@@ -161,27 +176,29 @@ public function publicRegister(Request $request, $championshipId)
             }
         }
 
-        // Validar Gênero
         $catGender = strtolower($category->gender ?? $mainCategory->gender ?? '');
         if ($catGender && $catGender !== 'mixed' && $catGender !== 'misto') {
             $userGender = strtolower($request->gender);
-            if ($userGender === 'm')
+            if ($userGender === 'm') {
                 $userGender = 'male';
-            if ($userGender === 'f')
+            }
+            if ($userGender === 'f') {
                 $userGender = 'female';
+            }
 
             $normalizedCatGender = $catGender;
-            if ($normalizedCatGender === 'm')
+            if ($normalizedCatGender === 'm') {
                 $normalizedCatGender = 'male';
-            if ($normalizedCatGender === 'f')
+            }
+            if ($normalizedCatGender === 'f') {
                 $normalizedCatGender = 'female';
+            }
 
             if ($userGender !== $normalizedCatGender) {
                 return response()->json(['error' => 'Gênero incompatível com a categoria selecionada.'], 422);
             }
         }
 
-        // Validar Idade (Na categoria final)
         if ($category->min_age && $athleteAge < $category->min_age) {
             return response()->json(['error' => "Idade não permitida. A categoria exige idade mínima de {$category->min_age} anos. (Idade: {$athleteAge})"], 422);
         }
@@ -192,7 +209,6 @@ public function publicRegister(Request $request, $championshipId)
         try {
             DB::beginTransaction();
 
-            // 1. Resolver Usuário
             $user = User::where('cpf', $request->document)
                 ->orWhere('email', $request->email)
                 ->first();
@@ -204,24 +220,23 @@ public function publicRegister(Request $request, $championshipId)
                 }
                 $user->update(array_filter([
                     'birth_date' => $user->birth_date ?: $request->birth_date,
-                    'gender' => $user->gender ?: $request->gender,
-                    'phone' => $user->phone ?: $request->phone,
-                    'cpf' => $user->cpf ?: $request->document,
+                    'gender'     => $user->gender ?: $request->gender,
+                    'phone'      => $user->phone ?: $request->phone,
+                    'cpf'        => $user->cpf ?: $request->document,
                 ]));
             } else {
                 $user = User::create([
-                    'name' => $request->name,
-                    'email' => $request->email,
-                    'phone' => $request->phone,
-                    'cpf' => $request->document,
+                    'name'       => $request->name,
+                    'email'      => $request->email,
+                    'phone'      => $request->phone,
+                    'cpf'        => $request->document,
                     'birth_date' => $request->birth_date,
-                    'gender' => $request->gender,
-                    'club_id' => $race->championship->club_id,
-                    'password' => bcrypt(Str::random(12)),
+                    'gender'     => $request->gender,
+                    'club_id'    => $race->championship->club_id,
+                    'password'   => bcrypt(Str::random(12)),
                 ]);
             }
 
-            // 2. Foto
             if ($request->hasFile('photo')) {
                 try {
                     set_time_limit(300);
@@ -229,38 +244,28 @@ public function publicRegister(Request $request, $championshipId)
                     $photoRequest = new Request();
                     $photoRequest->files->set('photo', $request->file('photo'));
                     $photoRequest->merge(['remove_bg' => false]);
-                    // Passa o user resolver para o photoRequest (se autenticado)
                     if ($request->getUserResolver()) {
                         $photoRequest->setUserResolver($request->getUserResolver());
                     }
                     $imageController->uploadPlayerPhoto($photoRequest, (int)$user->id);
                 } catch (\Exception $photoEx) {
                     Log::error("Erro ao fazer upload de foto na inscrição: " . $photoEx->getMessage());
-                    // Não cancela a inscrição por falha no upload de foto
                 }
             }
 
-            // 3. Documento PCD
             $pcdDocumentUrl = null;
             if ($request->boolean('is_pcd') && $request->hasFile('pcd_document')) {
                 $path = $request->file('pcd_document')->store('pcd_documents', 'public');
                 $pcdDocumentUrl = '/storage/' . $path;
             }
 
-            // 4. Calcular Preço (SOMA CATEGORIA PRINCIPAL + SUBCATEGORIA SELECIONADA)
             $championship = $race->championship;
-
-            // O preço base vem SEMPRE da categoria que o usuário clicou (mainCategory)
             $originalPrice = (float) $mainCategory->price;
 
-            // Se o sistema encontrou uma subcategoria específica via idade, e ela for DIFERENTE da clicada,
-            // somamos o valor dela (que pode ser um acréscimo configurado)
             if ($category->id !== $mainCategory->id) {
                 $originalPrice += (float) ($category->price ?? 0);
             }
 
-
-            // Calcular Acréscimos de Variações nos Brindes
             if ($request->has('gifts')) {
                 foreach ($request->gifts as $gift) {
                     $prod = Product::find($gift['product_id']);
@@ -277,32 +282,18 @@ public function publicRegister(Request $request, $championshipId)
             $discountPct = 0;
             $hasAutoDiscount = false;
 
-            Log::info("DEBUG Descontos - Atleta: {$user->name}, Idade Calculada: {$athleteAge}", [
-                'has_elderly_discount_config' => $championship->has_elderly_discount,
-                'elderly_minimum_age' => $championship->elderly_minimum_age,
-                'elderly_discount_percentage' => $championship->elderly_discount_percentage,
-                'is_pcd_request' => $request->boolean('is_pcd'),
-                'has_pcd_discount_config' => $championship->has_pcd_discount,
-                'pcd_discount_percentage' => $championship->pcd_discount_percentage,
-            ]);
-
             if ($championship->has_elderly_discount && $athleteAge >= $championship->elderly_minimum_age) {
                 $discountPct = max($discountPct, (float) $championship->elderly_discount_percentage);
                 $hasAutoDiscount = true;
-                Log::info("Candidato a Desconto Idoso: {$championship->elderly_discount_percentage}%");
             }
 
             if ($request->boolean('is_pcd') && $championship->has_pcd_discount) {
                 $discountPct = max($discountPct, (float) $championship->pcd_discount_percentage);
                 $hasAutoDiscount = true;
-                Log::info("Candidato a Desconto PCD: {$championship->pcd_discount_percentage}%");
             }
 
-            Log::info("Desconto Automático Final Selecionado: {$discountPct}%");
-
             $finalPrice = $originalPrice * (1 - ($discountPct / 100));
 
-            // Itens da Loja
             $shopTotal = 0;
             if ($request->has('shop_items')) {
                 foreach ($request->shop_items as $item) {
@@ -322,8 +313,6 @@ public function publicRegister(Request $request, $championshipId)
             }
             $finalPrice += $shopTotal;
 
-            // Cupom (Apenas sobre o valor da INSCRIÇÃO, não sobre a loja)
-            // REGRA: Se já tem desconto automático (PCD/Idoso), não permite aplicar cupom
             $couponId = null;
             if ($request->coupon_code) {
                 if (!$hasAutoDiscount) {
@@ -337,38 +326,34 @@ public function publicRegister(Request $request, $championshipId)
                         $couponId = $coupon->id;
                         $coupon->increment('used_count');
                     }
-                } else {
-                    Log::info("Cupom '{$request->coupon_code}' ignorado: Atleta já possui desconto automático (Idoso/PCD).");
                 }
             }
 
-            if ($finalPrice < 0)
+            if ($finalPrice < 0) {
                 $finalPrice = 0;
+            }
 
-            // 5. Salvar Resultado
             $lastBib = RaceResult::where('race_id', $race->id)->max(DB::raw('CAST(bib_number AS SIGNED)'));
             $newBib = $lastBib ? $lastBib + 1 : 1;
             $status = ($finalPrice > 0) ? 'pending' : 'paid';
 
             $result = RaceResult::create([
-                'race_id' => $race->id,
-                'user_id' => $user->id,
-                'name' => $user->name,
-                'bib_number' => (string) $newBib,
-                'category_id' => $category->id,
-                'status_payment' => $status,
-                'payment_method' => $status === 'paid' ? 'free' : null,
-                'is_pcd' => $request->boolean('is_pcd'),
-                'pcd_document_url' => $pcdDocumentUrl,
-                'gifts' => $request->gifts,
-                'coupon_id' => $couponId,
-                'shop_items' => $request->shop_items
+                'race_id'              => $race->id,
+                'user_id'              => $user->id,
+                'name'                 => $user->name,
+                'bib_number'           => (string) $newBib,
+                'category_id'          => $category->id,
+                'status_payment'       => $status,
+                'payment_method'       => $status === 'paid' ? 'free' : null,
+                'is_pcd'               => $request->boolean('is_pcd'),
+                'pcd_document_url'     => $pcdDocumentUrl,
+                'gifts'                => $request->gifts,
+                'coupon_id'            => $couponId,
+                'shop_items'           => $request->shop_items
             ]);
             
-            // 4.1. Baixa no Estoque se for inscrição Gratuita/Cortesia (Já nasce 'paid')
             if ($status === 'paid') {
                 try {
-                    // Baixa nos Brindes da Categoria
                     $included = $category->products();
                     foreach ($included as $item) {
                         $product = $item['product'];
@@ -377,24 +362,25 @@ public function publicRegister(Request $request, $championshipId)
                             $product->decrement('stock_quantity', $qty);
                         }
                     }
-                    // Baixa nos Itens da Loja
                     if ($request->has('shop_items')) {
                         foreach ($request->shop_items as $item) {
-                            $prod = \App\Models\Product::find($item['product_id']);
+                            $prod = Product::find($item['product_id']);
                             if ($prod && $prod->stock_quantity !== null) {
                                 $prod->decrement('stock_quantity', $item['quantity'] ?? 1);
                             }
                         }
                     }
                 } catch (\Exception $stockEx) {
-                    \Illuminate\Support\Facades\Log::error("Erro estoque inscrição grátis: " . $stockEx->getMessage());
+                    Log::error("Erro estoque inscrição grátis: " . $stockEx->getMessage());
                 }
             }
 
             $paymentInfo = null;
             if ($status === 'pending') {
                 try {
-                    $asaas = new AsaasService($race->championship->club);
+                    $asaas = app()->environment('testing')
+                        ? app(AsaasService::class)
+                        : new AsaasService($race->championship->club);
 
                     $catName = $mainCategory->name;
                     if ($category->id !== $mainCategory->id) {
@@ -404,16 +390,15 @@ public function publicRegister(Request $request, $championshipId)
                     $description = "Inscrição: {$race->championship->name} - {$catName}";
                     $payment = $asaas->createPayment($user, $finalPrice, substr($description, 0, 250), "RR_{$result->id}", null, $request->input('payment_method', 'UNDEFINED'));
 
-
                     if (isset($payment['id'])) {
                         $pix = $asaas->getPixQrCode($payment['id']);
                         $paymentInfo = [
-                            'asaas_id' => $payment['id'],
-                            'invoice_url' => $payment['invoiceUrl'],
-                            'pix_qr_code' => $pix['encodedImage'] ?? null,
+                            'asaas_id'       => $payment['id'],
+                            'invoice_url'    => $payment['invoiceUrl'],
+                            'pix_qr_code'    => $pix['encodedImage'] ?? null,
                             'pix_copy_paste' => $pix['payload'] ?? null,
-                            'expiration' => $payment['dueDate'],
-                            'value' => $finalPrice
+                            'expiration'     => $payment['dueDate'],
+                            'value'          => $finalPrice
                         ];
                         $result->update(['payment_method' => 'asaas', 'asaas_payment_id' => $payment['id'], 'payment_info' => $paymentInfo]);
                         try {
@@ -430,25 +415,26 @@ public function publicRegister(Request $request, $championshipId)
 
             DB::commit();
             return response()->json([
-                'message' => 'Inscrição realizada!',
-                'result' => $result->load('user'),
+                'message'          => 'Inscrição realizada!',
+                'result'           => $result->load('user:id,name,email,phone'),
                 'requires_payment' => $finalPrice > 0,
-                'price' => $finalPrice,
-                'category_name' => $mainCategory->name,
+                'price'            => $finalPrice,
+                'category_name'    => $mainCategory->name,
                 'subcategory_name' => ($category->id !== $mainCategory->id) ? $category->name : null,
-                'original_price' => $originalPrice,
-                'payment_data' => $paymentInfo
+                'original_price'   => $originalPrice,
+                'payment_data'     => $paymentInfo
             ], 201);
 
-
         } catch (\Exception $e) {
             DB::rollBack();
             return response()->json(['error' => 'Erro: ' . $e->getMessage()], 500);
         }
     }
 
-    // Registro Coletivo (Site)
-    public function publicRegisterBulk(Request $request, $championshipId)
+    /**
+     * Bulk registration.
+     */
+    public function publicRegisterBulk(Request $request, int $championshipId): JsonResponse
     {
         if ($request->has('athletes') && is_string($request->athletes)) {
             $decoded = json_decode($request->athletes, true);
@@ -456,19 +442,19 @@ public function publicRegisterBulk(Request $request, $championshipId)
         }
 
         $request->validate([
-            'athletes' => 'required|array|min:1',
-            'athletes.*.name' => 'required|string|max:255',
-            'athletes.*.email' => 'required|email',
-            'athletes.*.phone' => 'required|string|max:255',
-            'athletes.*.document' => 'required|string|max:255',
-            'athletes.*.birth_date' => 'required|date',
-            'athletes.*.gender' => 'required|string|in:M,F,O',
+            'athletes'               => 'required|array|min:1',
+            'athletes.*.name'        => 'required|string|max:255',
+            'athletes.*.email'       => 'required|email',
+            'athletes.*.phone'       => 'required|string|max:255',
+            'athletes.*.document'    => 'required|string|max:255',
+            'athletes.*.birth_date'  => 'required|date',
+            'athletes.*.gender'      => 'required|string|in:M,F,O',
             'athletes.*.category_id' => 'required|exists:categories,id',
-            'athletes.*.is_pcd' => 'nullable|boolean',
-            'athletes.*.gifts' => 'nullable|array',
-            'athletes.*.shop_items' => 'nullable|array',
-            'coupon_code' => 'nullable|string',
-            'payment_method' => 'nullable|string|in:PIX,CREDIT_CARD,BOLETO'
+            'athletes.*.is_pcd'      => 'nullable|boolean',
+            'athletes.*.gifts'       => 'nullable|array',
+            'athletes.*.shop_items'  => 'nullable|array',
+            'coupon_code'            => 'nullable|string',
+            'payment_method'         => 'nullable|string|in:PIX,CREDIT_CARD,BOLETO'
         ]);
 
         $race = Race::where('championship_id', $championshipId)->first();
@@ -477,9 +463,9 @@ public function publicRegisterBulk(Request $request, $championshipId)
             if ($championship && $championship->format === 'racing') {
                 $race = Race::create([
                     'championship_id' => $championshipId,
-                    'start_datetime' => $championship->start_date,
-                    'location_name' => 'A definir',
-                    'kits_info' => 'Informações do kit em breve'
+                    'start_datetime'  => $championship->start_date,
+                    'location_name'   => 'A definir',
+                    'kits_info'       => 'Informações do kit em breve'
                 ]);
             } else {
                 return response()->json(['error' => 'Evento não encontrado ou não configurado como corrida.'], 404);
@@ -490,7 +476,6 @@ public function publicRegisterBulk(Request $request, $championshipId)
         $athletes = $request->athletes;
         $count = count($athletes);
 
-        // Calcular Desconto Coletivo Progressivo
         $bulkDiscountPct = 0;
         if ($championship->bulk_discount_settings && is_array($championship->bulk_discount_settings)) {
             foreach ($championship->bulk_discount_settings as $rule) {
@@ -510,14 +495,14 @@ public function publicRegisterBulk(Request $request, $championshipId)
 
             $index = 0;
             foreach ($athletes as $athleteData) {
-                $selectedCategory = Category::with(['parent', 'children'])->findOrFail($athleteData['category_id']);
+                $selectedCategory = Category::with(['parent:id,name,price,gender', 'children:id,parent_id,name,min_age,max_age,gender,price'])->findOrFail($athleteData['category_id']);
                 $mainCategory = $selectedCategory->parent_id ? $selectedCategory->parent : $selectedCategory;
                 $category = $selectedCategory;
                 $subCategory = null;
 
-                $eventYear = $championship->start_date ? \Carbon\Carbon::parse($championship->start_date)->year : date('Y');
-                $referenceDate = \Carbon\Carbon::createFromDate($eventYear, 12, 31);
-                $athleteAge = (int) $referenceDate->diffInYears(\Carbon\Carbon::parse($athleteData['birth_date']), true);
+                $eventYear = $championship->start_date ? Carbon::parse($championship->start_date)->year : date('Y');
+                $referenceDate = Carbon::createFromDate($eventYear, 12, 31);
+                $athleteAge = (int) $referenceDate->diffInYears(Carbon::parse($athleteData['birth_date']), true);
 
                 if ($mainCategory->children->count() > 0) {
                     if (!empty($athleteData['is_pcd'])) {
@@ -527,12 +512,22 @@ public function publicRegisterBulk(Request $request, $championshipId)
                                 $childGender = strtolower($child->gender ?? '');
                                 if ($childGender && $childGender !== 'mixed' && $childGender !== 'misto') {
                                     $userGender = strtolower($athleteData['gender']);
-                                    if ($userGender === 'm') $userGender = 'male';
-                                    if ($userGender === 'f') $userGender = 'female';
+                                    if ($userGender === 'm') {
+                                        $userGender = 'male';
+                                    }
+                                    if ($userGender === 'f') {
+                                        $userGender = 'female';
+                                    }
                                     $normalizedChildGender = $childGender;
-                                    if ($normalizedChildGender === 'm') $normalizedChildGender = 'male';
-                                    if ($normalizedChildGender === 'f') $normalizedChildGender = 'female';
-                                    if ($userGender !== $normalizedChildGender) return false;
+                                    if ($normalizedChildGender === 'm') {
+                                        $normalizedChildGender = 'male';
+                                    }
+                                    if ($normalizedChildGender === 'f') {
+                                        $normalizedChildGender = 'female';
+                                    }
+                                    if ($userGender !== $normalizedChildGender) {
+                                        return false;
+                                    }
                                 }
                                 return $nameMatch;
                             })
@@ -544,17 +539,29 @@ public function publicRegisterBulk(Request $request, $championshipId)
                             ->filter(function ($child) use ($athleteAge, $athleteData) {
                                 $min = $child->min_age ?? 0;
                                 $max = $child->max_age ?? 999;
-                                if ($athleteAge < $min || $athleteAge > $max) return false;
+                                if ($athleteAge < $min || $athleteAge > $max) {
+                                    return false;
+                                }
 
                                 $childGender = strtolower($child->gender ?? '');
                                 if ($childGender && $childGender !== 'mixed' && $childGender !== 'misto') {
                                     $userGender = strtolower($athleteData['gender']);
-                                    if ($userGender === 'm') $userGender = 'male';
-                                    if ($userGender === 'f') $userGender = 'female';
+                                    if ($userGender === 'm') {
+                                        $userGender = 'male';
+                                    }
+                                    if ($userGender === 'f') {
+                                        $userGender = 'female';
+                                    }
                                     $normalizedChildGender = $childGender;
-                                    if ($normalizedChildGender === 'm') $normalizedChildGender = 'male';
-                                    if ($normalizedChildGender === 'f') $normalizedChildGender = 'female';
-                                    if ($userGender !== $normalizedChildGender) return false;
+                                    if ($normalizedChildGender === 'm') {
+                                        $normalizedChildGender = 'male';
+                                    }
+                                    if ($normalizedChildGender === 'f') {
+                                        $normalizedChildGender = 'female';
+                                    }
+                                    if ($userGender !== $normalizedChildGender) {
+                                        return false;
+                                    }
                                 }
                                 return true;
                             })
@@ -569,11 +576,19 @@ public function publicRegisterBulk(Request $request, $championshipId)
                 $catGender = strtolower($category->gender ?? $mainCategory->gender ?? '');
                 if ($catGender && $catGender !== 'mixed' && $catGender !== 'misto') {
                     $userGender = strtolower($athleteData['gender']);
-                    if ($userGender === 'm') $userGender = 'male';
-                    if ($userGender === 'f') $userGender = 'female';
+                    if ($userGender === 'm') {
+                        $userGender = 'male';
+                    }
+                    if ($userGender === 'f') {
+                        $userGender = 'female';
+                    }
                     $normalizedCatGender = $catGender;
-                    if ($normalizedCatGender === 'm') $normalizedCatGender = 'male';
-                    if ($normalizedCatGender === 'f') $normalizedCatGender = 'female';
+                    if ($normalizedCatGender === 'm') {
+                        $normalizedCatGender = 'male';
+                    }
+                    if ($normalizedCatGender === 'f') {
+                        $normalizedCatGender = 'female';
+                    }
                     if ($userGender !== $normalizedCatGender) {
                         return response()->json(['error' => "Gênero incompatível para o atleta {$athleteData['name']}."], 422);
                     }
@@ -597,20 +612,20 @@ public function publicRegisterBulk(Request $request, $championshipId)
                     }
                     $user->update(array_filter([
                         'birth_date' => $user->birth_date ?: $athleteData['birth_date'],
-                        'gender' => $user->gender ?: $athleteData['gender'],
-                        'phone' => $user->phone ?: $athleteData['phone'],
-                        'cpf' => $user->cpf ?: $athleteData['document'],
+                        'gender'     => $user->gender ?: $athleteData['gender'],
+                        'phone'      => $user->phone ?: $athleteData['phone'],
+                        'cpf'        => $user->cpf ?: $athleteData['document'],
                     ]));
                 } else {
                     $user = User::create([
-                        'name' => $athleteData['name'],
-                        'email' => $athleteData['email'],
-                        'phone' => $athleteData['phone'],
-                        'cpf' => $athleteData['document'],
+                        'name'       => $athleteData['name'],
+                        'email'      => $athleteData['email'],
+                        'phone'      => $athleteData['phone'],
+                        'cpf'        => $athleteData['document'],
                         'birth_date' => $athleteData['birth_date'],
-                        'gender' => $athleteData['gender'],
-                        'club_id' => $championship->club_id,
-                        'password' => bcrypt(Str::random(12)),
+                        'gender'     => $athleteData['gender'],
+                        'club_id'    => $championship->club_id,
+                        'password'   => bcrypt(Str::random(12)),
                     ]);
                 }
 
@@ -703,21 +718,23 @@ public function publicRegisterBulk(Request $request, $championshipId)
                     }
                 }
 
-                if ($athleteFinalPrice < 0) $athleteFinalPrice = 0;
+                if ($athleteFinalPrice < 0) {
+                    $athleteFinalPrice = 0;
+                }
 
                 $totalPrice += $athleteFinalPrice;
 
                 $resultsToCreate[] = [
-                    'user' => $user,
-                    'category' => $category,
-                    'main_category' => $mainCategory,
-                    'price' => $athleteFinalPrice,
-                    'original_price' => $originalPrice,
-                    'is_pcd' => !empty($athleteData['is_pcd']),
-                    'pcd_document_url' => $pcdDocumentUrl,
-                    'gifts' => $athleteData['gifts'] ?? null,
-                    'shop_items' => $athleteData['shop_items'] ?? null,
-                    'coupon_id' => $couponId,
+                    'user'                 => $user,
+                    'category'             => $category,
+                    'main_category'        => $mainCategory,
+                    'price'                => $athleteFinalPrice,
+                    'original_price'       => $originalPrice,
+                    'is_pcd'               => !empty($athleteData['is_pcd']),
+                    'pcd_document_url'     => $pcdDocumentUrl,
+                    'gifts'                => $athleteData['gifts'] ?? null,
+                    'shop_items'           => $athleteData['shop_items'] ?? null,
+                    'coupon_id'            => $couponId,
                     'payment_group_leader' => ($index === 0)
                 ];
 
@@ -732,19 +749,19 @@ public function publicRegisterBulk(Request $request, $championshipId)
                 $newBib = $lastBib ? $lastBib + 1 : 1;
 
                 $result = RaceResult::create([
-                    'race_id' => $race->id,
-                    'user_id' => $r['user']->id,
-                    'name' => $r['user']->name,
-                    'bib_number' => (string) $newBib,
-                    'category_id' => $r['category']->id,
-                    'status_payment' => $status,
-                    'payment_method' => $status === 'paid' ? 'free' : null,
-                    'is_pcd' => $r['is_pcd'],
-                    'pcd_document_url' => $r['pcd_document_url'],
-                    'gifts' => $r['gifts'],
-                    'coupon_id' => $r['coupon_id'],
-                    'shop_items' => $r['shop_items'],
-                    'payment_group_id' => $paymentGroupId,
+                    'race_id'              => $race->id,
+                    'user_id'              => $r['user']->id,
+                    'name'                 => $r['user']->name,
+                    'bib_number'           => (string) $newBib,
+                    'category_id'          => $r['category']->id,
+                    'status_payment'       => $status,
+                    'payment_method'       => $status === 'paid' ? 'free' : null,
+                    'is_pcd'               => $r['is_pcd'],
+                    'pcd_document_url'     => $r['pcd_document_url'],
+                    'gifts'                => $r['gifts'],
+                    'coupon_id'            => $r['coupon_id'],
+                    'shop_items'           => $r['shop_items'],
+                    'payment_group_id'     => $paymentGroupId,
                     'payment_group_leader' => $r['payment_group_leader']
                 ]);
 
@@ -779,7 +796,9 @@ public function publicRegisterBulk(Request $request, $championshipId)
                 $leaderResult = $createdResults[0];
                 $leaderUser = $resultsToCreate[0]['user'];
 
-                $asaas = new AsaasService($championship->club);
+                $asaas = app()->environment('testing')
+                    ? app(AsaasService::class)
+                    : new AsaasService($championship->club);
                 $description = "Inscrição Coletiva: {$championship->name} - {$count} atletas";
                 $payment = $asaas->createPayment(
                     $leaderUser,
@@ -793,19 +812,19 @@ public function publicRegisterBulk(Request $request, $championshipId)
                 if (isset($payment['id'])) {
                     $pix = $asaas->getPixQrCode($payment['id']);
                     $paymentInfo = [
-                        'asaas_id' => $payment['id'],
-                        'invoice_url' => $payment['invoiceUrl'],
-                        'pix_qr_code' => $pix['encodedImage'] ?? null,
+                        'asaas_id'       => $payment['id'],
+                        'invoice_url'    => $payment['invoiceUrl'],
+                        'pix_qr_code'    => $pix['encodedImage'] ?? null,
                         'pix_copy_paste' => $pix['payload'] ?? null,
-                        'expiration' => $payment['dueDate'],
-                        'value' => $totalPrice
+                        'expiration'     => $payment['dueDate'],
+                        'value'          => $totalPrice
                     ];
 
                     foreach ($createdResults as $res) {
                         $res->update([
-                            'payment_method' => 'asaas',
+                            'payment_method'   => 'asaas',
                             'asaas_payment_id' => $payment['id'],
-                            'payment_info' => $paymentInfo
+                            'payment_info'     => $paymentInfo
                         ]);
                     }
 
@@ -820,11 +839,11 @@ public function publicRegisterBulk(Request $request, $championshipId)
             DB::commit();
 
             return response()->json([
-                'message' => 'Inscrições em lote realizadas com sucesso!',
+                'message'          => 'Inscrições em lote realizadas com sucesso!',
                 'payment_group_id' => $paymentGroupId,
                 'requires_payment' => $totalPrice > 0,
-                'price' => $totalPrice,
-                'payment_data' => $paymentInfo
+                'price'            => $totalPrice,
+                'payment_data'     => $paymentInfo
             ], 201);
 
         } catch (\Exception $e) {
@@ -833,14 +852,17 @@ public function publicRegisterBulk(Request $request, $championshipId)
         }
     }
 
-    // Acompanhar Inscrição (Público)
-    public function publicTrackRegistration(Request $request, $championshipId)
+    /**
+     * Public track registration.
+     */
+    public function publicTrackRegistration(Request $request, int $championshipId): JsonResponse
     {
         $request->validate(['document' => 'required|string', 'birth_date' => 'required|date']);
 
         $race = Race::where('championship_id', $championshipId)->first();
-        if (!$race)
+        if (!$race) {
             return response()->json(['error' => 'Evento não encontrado.'], 422);
+        }
 
         $cleanCpf = preg_replace('/[^0-9]/', '', $request->document);
         $user = User::where(DB::raw("REPLACE(REPLACE(REPLACE(cpf, '.', ''), '-', ''), ' ', '')"), $cleanCpf)->first();
@@ -849,8 +871,8 @@ public function publicTrackRegistration(Request $request, $championshipId)
             return response()->json(['error' => 'Atleta não encontrado com este documento.'], 422);
         }
 
-        $requestBirthDate = \Carbon\Carbon::parse($request->birth_date)->format('Y-m-d');
-        $userBirthDate = $user->birth_date ? \Carbon\Carbon::parse($user->birth_date)->format('Y-m-d') : null;
+        $requestBirthDate = Carbon::parse($request->birth_date)->format('Y-m-d');
+        $userBirthDate = $user->birth_date ? Carbon::parse($user->birth_date)->format('Y-m-d') : null;
 
         if ($userBirthDate && $userBirthDate !== $requestBirthDate) {
             return response()->json(['error' => 'Data de nascimento não confere com o documento informado.'], 422);
@@ -871,11 +893,11 @@ public function publicTrackRegistration(Request $request, $championshipId)
                 ->get();
 
             return response()->json([
-                'result' => $registration,
-                'group_results' => $groupResults,
+                'result'           => $registration,
+                'group_results'    => $groupResults,
                 'requires_payment' => $registration->status_payment === 'pending',
-                'payment_data' => $registration->payment_info,
-                'price' => $registration->payment_info['value'] ?? $groupResults->sum(function ($r) {
+                'payment_data'     => $registration->payment_info,
+                'price'            => $registration->payment_info['value'] ?? $groupResults->sum(function ($r) {
                     $m = $r->category->parent_id ? $r->category->parent : $r->category;
                     $p = (float) $m->price;
                     if ($r->category_id !== $m->id) {
@@ -893,11 +915,10 @@ public function publicTrackRegistration(Request $request, $championshipId)
         }
 
         return response()->json([
-            'result' => $registration,
+            'result'           => $registration,
             'requires_payment' => $registration->status_payment === 'pending',
-            'payment_data' => $registration->payment_info,
-            'price' => $registration->payment_info['value'] ?? $fallbackPrice,
+            'payment_data'     => $registration->payment_info,
+            'price'            => $registration->payment_info['value'] ?? $fallbackPrice,
         ]);
-
     }
 }
diff --git a/app/Http/Controllers/Admin/RacePaymentController.php b/app/Http/Controllers/Admin/RacePaymentController.php
index daf6a97..052bf84 100644
--- a/app/Http/Controllers/Admin/RacePaymentController.php
+++ b/app/Http/Controllers/Admin/RacePaymentController.php
@@ -4,6 +4,7 @@
 
 use App\Http\Controllers\Controller;
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
 use App\Models\RaceResult;
 use App\Models\Championship;
 use App\Models\Team;
@@ -14,31 +15,40 @@
 use Illuminate\Support\Facades\DB;
 use Illuminate\Support\Facades\Log;
 use Illuminate\Support\Facades\Mail;
+use Carbon\Carbon;
 
 class RacePaymentController extends Controller
 {
-    // Recriar Pagamento (Mudar método ou renovar)
-    public function recreatePayment(Request $request, $id)
+    /**
+     * Recreate payment (change method or renew).
+     */
+    public function recreatePayment(Request $request, int $id): JsonResponse
     {
         $request->validate([
             'payment_method' => 'required|string|in:PIX,CREDIT_CARD,BOLETO,UNDEFINED',
-            'document' => 'nullable|string',
-            'birth_date' => 'nullable|date',
+            'document'       => 'nullable|string',
+            'birth_date'     => 'nullable|date',
         ]);
 
         $result = RaceResult::where('id', $id)
-            ->with(['race.championship.club', 'category.parent', 'user'])
+            ->with([
+                'race:id,championship_id', 
+                'race.championship:id,name,club_id', 
+                'race.championship.club:id,name', 
+                'category:id,parent_id,name,price', 
+                'category.parent:id,name,price', 
+                'user:id,cpf,birth_date,email'
+            ])
             ->first();
 
-
         if (!$result) {
             $pivot = DB::table('championship_team')->where('id', $id)->first();
-            if ($pivot)
+            if ($pivot) {
                 return $this->recreateTeamPayment($request, $pivot);
+            }
             return response()->json(['error' => 'Inscrição não encontrada.'], 404);
         }
 
-        // Segurança
         $user = auth('sanctum')->user();
         if ($user) {
             if (!$user->is_admin && $user->id !== $result->user_id) {
@@ -49,30 +59,32 @@ public function recreatePayment(Request $request, $id)
         } else {
             $cleanCpf = preg_replace('/[^0-9]/', '', $request->document);
             $dbCpf = preg_replace('/[^0-9]/', '', $result->user->cpf ?? '');
-            if ($cleanCpf !== $dbCpf || \Carbon\Carbon::parse($request->birth_date)->format('Y-m-d') !== \Carbon\Carbon::parse($result->user->birth_date)->format('Y-m-d')) {
+            if ($cleanCpf !== $dbCpf || Carbon::parse($request->birth_date)->format('Y-m-d') !== Carbon::parse($result->user->birth_date)->format('Y-m-d')) {
                 return response()->json(['error' => 'Dados não conferem.'], 403);
             }
         }
 
-        if ($result->status_payment === 'paid')
+        if ($result->status_payment === 'paid') {
             return response()->json(['error' => 'Inscrição já paga.'], 422);
+        }
 
         try {
             DB::beginTransaction();
-            $asaas = new AsaasService($result->race->championship->club);
+            $asaas = app()->environment('testing')
+                ? app(AsaasService::class)
+                : new AsaasService($result->race->championship->club);
 
             if ($result->asaas_payment_id) {
                 try {
                     $asaas->deletePayment($result->asaas_payment_id);
                 } catch (\Exception $e) {
+                    // Ignore errors during delete
                 }
             }
 
-            // Calcular Preço (SOMA CATEGORIA + SUBCATEGORIA se não salvo no payment_info)
             $mainCategory = $result->category->parent_id ? $result->category->parent : $result->category;
             $amount = $result->payment_info['value'] ?? (float) $mainCategory->price;
 
-            // Se for subcategoria e não tiver o valor salvo em payment_info, PRECISAMOS somar o adicional da subcategoria
             if (!isset($result->payment_info['value']) && $result->category->id !== $mainCategory->id) {
                 $amount += (float) ($result->category->price ?? 0);
             }
@@ -83,17 +95,18 @@ public function recreatePayment(Request $request, $id)
             if (isset($payment['id'])) {
                 $pix = ($request->payment_method === 'PIX' || $request->payment_method === 'UNDEFINED') ? $asaas->getPixQrCode($payment['id']) : null;
                 $paymentInfo = [
-                    'asaas_id' => $payment['id'],
-                    'invoice_url' => $payment['invoiceUrl'],
-                    'pix_qr_code' => $pix['encodedImage'] ?? null,
+                    'asaas_id'       => $payment['id'],
+                    'invoice_url'    => $payment['invoiceUrl'],
+                    'pix_qr_code'    => $pix['encodedImage'] ?? null,
                     'pix_copy_paste' => $pix['payload'] ?? null,
-                    'expiration' => $payment['dueDate'],
-                    'value' => $amount
+                    'expiration'     => $payment['dueDate'],
+                    'value'          => $amount
                 ];
                 $result->update(['payment_method' => 'asaas', 'asaas_payment_id' => $payment['id'], 'payment_info' => $paymentInfo]);
                 try {
                     Mail::to($result->user->email)->send(new InscriptionPaymentMail($result, $paymentInfo));
                 } catch (\Exception $me) {
+                    // Ignore mail errors
                 }
             }
 
@@ -105,25 +118,32 @@ public function recreatePayment(Request $request, $id)
         }
     }
 
-    private function recreateTeamPayment(Request $request, $pivot)
+    /**
+     * Recreate team payment subscription.
+     */
+    private function recreateTeamPayment(Request $request, $pivot): JsonResponse
     {
-        $championship = Championship::with('club')->findOrFail($pivot->championship_id);
+        $championship = Championship::with('club:id,name')->findOrFail($pivot->championship_id);
         $team = Team::findOrFail($pivot->team_id);
         $category = Category::findOrFail($pivot->category_id);
         $captain = User::findOrFail($team->captain_id);
 
         $user = auth('sanctum')->user();
-        if ($user && !$user->is_admin && $user->id !== $captain->id)
+        if ($user && !$user->is_admin && $user->id !== $captain->id) {
             return response()->json(['error' => 'Acesso negado.'], 403);
+        }
 
         try {
             DB::beginTransaction();
-            $asaas = new AsaasService($championship->club);
+            $asaas = app()->environment('testing')
+                ? app(AsaasService::class)
+                : new AsaasService($championship->club);
             $oldInfo = json_decode($pivot->payment_info ?? '[]', true);
             if (!empty($oldInfo['asaas_id'])) {
                 try {
                     $asaas->deletePayment($oldInfo['asaas_id']);
                 } catch (\Exception $e) {
+                    // Ignore errors during delete
                 }
             }
 
@@ -132,8 +152,19 @@ private function recreateTeamPayment(Request $request, $pivot)
 
             if (isset($payment['id'])) {
                 $pix = ($request->payment_method === 'PIX' || $request->payment_method === 'UNDEFINED') ? $asaas->getPixQrCode($payment['id']) : null;
-                $paymentInfo = ['asaas_id' => $payment['id'], 'invoice_url' => $payment['invoiceUrl'], 'pix_qr_code' => $pix['encodedImage'] ?? null, 'pix_copy_paste' => $pix['payload'] ?? null, 'expiration' => $payment['dueDate'], 'value' => $amount];
-                DB::table('championship_team')->where('id', $pivot->id)->update(['payment_method' => 'asaas', 'payment_info' => json_encode($paymentInfo), 'updated_at' => now()]);
+                $paymentInfo = [
+                    'asaas_id'       => $payment['id'], 
+                    'invoice_url'    => $payment['invoiceUrl'], 
+                    'pix_qr_code'    => $pix['encodedImage'] ?? null, 
+                    'pix_copy_paste' => $pix['payload'] ?? null, 
+                    'expiration'     => $payment['dueDate'], 
+                    'value'          => $amount
+                ];
+                DB::table('championship_team')->where('id', $pivot->id)->update([
+                    'payment_method' => 'asaas', 
+                    'payment_info'   => json_encode($paymentInfo), 
+                    'updated_at'     => now()
+                ]);
             }
             DB::commit();
             return response()->json(['message' => 'Pagamento atualizado!', 'payment_data' => $paymentInfo]);
diff --git a/app/Http/Controllers/Admin/RaceResultController.php b/app/Http/Controllers/Admin/RaceResultController.php
index 8c2a710..e39ed82 100644
--- a/app/Http/Controllers/Admin/RaceResultController.php
+++ b/app/Http/Controllers/Admin/RaceResultController.php
@@ -4,6 +4,7 @@
 
 use App\Http\Controllers\Controller;
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
 use App\Models\Race;
 use App\Models\RaceResult;
 use App\Models\User;
@@ -12,20 +13,22 @@
 use Illuminate\Support\Facades\Log;
 use Illuminate\Support\Facades\Storage;
 use Illuminate\Support\Str;
+use Symfony\Component\HttpFoundation\StreamedResponse;
 
 class RaceResultController extends Controller
 {
     /**
-     * Listar resultados/inscritos de uma corrida
+     * List race results / registered athletes.
      */
-    public function index($championshipId)
+    public function index(int $championshipId): JsonResponse
     {
         $race = Race::where('championship_id', $championshipId)->first();
-        if (!$race)
+        if (!$race) {
             return response()->json(['message' => 'Corrida não encontrada'], 404);
+        }
 
         $results = RaceResult::where('race_id', $race->id)
-            ->with(['category', 'user'])
+            ->with(['category:id,name', 'user:id,name,cpf,gender'])
             ->orderBy('lap', 'desc')
             ->orderBy('net_time', 'asc')
             ->orderBy('position_general', 'asc')
@@ -35,9 +38,9 @@ public function index($championshipId)
     }
 
     /**
-     * Atualizar manual (Digitação)
+     * Manual update (typing).
      */
-    public function update(Request $request, $id)
+    public function update(Request $request, int $id): JsonResponse
     {
         $result = RaceResult::findOrFail($id);
         $result->update($request->only(['net_time', 'gross_time', 'position_general', 'position_category', 'bib_number']));
@@ -45,27 +48,31 @@ public function update(Request $request, $id)
     }
 
     /**
-     * Adicionar um atleta manualmente (Admin)
+     * Add an athlete manually.
      */
-    public function store(Request $request, $championshipId)
+    public function store(Request $request, int $championshipId): JsonResponse
     {
         $request->validate([
-            'name' => 'required|string|max:255',
-            'phone' => 'required|string|max:255',
-            'document' => 'required|string|max:255',
-            'birth_date' => 'required|date',
-            'gender' => 'required|string|in:M,F,O',
-            'category_id' => 'required|exists:categories,id',
+            'name'           => 'required|string|max:255',
+            'phone'          => 'required|string|max:255',
+            'document'       => 'required|string|max:255',
+            'birth_date'     => 'required|date',
+            'gender'         => 'required|string|in:M,F,O',
+            'category_id'    => 'required|exists:categories,id',
             'status_payment' => 'required|string|in:pending,paid,cancelled',
         ]);
 
         $race = Race::where('championship_id', $championshipId)->first();
-        if (!$race)
+        if (!$race) {
             return response()->json(['error' => 'Corrida não encontrada'], 404);
+        }
 
         try {
             DB::beginTransaction();
-            $user = User::where('cpf', $request->document)->orWhere('rg', $request->document)->orWhere('document_number', $request->document)->first();
+            $user = User::where('cpf', $request->document)
+                ->orWhere('rg', $request->document)
+                ->orWhere('document_number', $request->document)
+                ->first();
 
             if ($user && RaceResult::where('race_id', $race->id)->where('user_id', $user->id)->exists()) {
                 return response()->json(['error' => 'Atleta já cadastrado.'], 422);
@@ -73,18 +80,17 @@ public function store(Request $request, $championshipId)
 
             if (!$user) {
                 $user = User::create([
-                    'name' => $request->name,
-                    'email' => $request->email ?? (preg_replace('/[^0-9]/', '', $request->document) . '@esportivo.com.br'),
-                    'phone' => $request->phone,
-                    'cpf' => $request->document,
+                    'name'       => $request->name,
+                    'email'      => $request->email ?? (preg_replace('/[^0-9]/', '', $request->document) . '@esportivo.com.br'),
+                    'phone'      => $request->phone,
+                    'cpf'        => $request->document,
                     'birth_date' => $request->birth_date,
-                    'gender' => $request->gender,
-                    'club_id' => $race->championship->club_id,
-                    'password' => bcrypt(Str::random(12)),
+                    'gender'     => $request->gender,
+                    'club_id'    => $race->championship->club_id,
+                    'password'   => bcrypt(Str::random(12)),
                 ]);
             }
 
-            // Handle Photo Upload
             if ($request->hasFile('photo')) {
                 $file = $request->file('photo');
                 $filename = Str::slug($user->name) . '-' . time() . '.' . $file->getClientOriginalExtension();
@@ -94,7 +100,6 @@ public function store(Request $request, $championshipId)
                 $user->photos = [$path];
                 $user->save();
 
-                // Trigger Background Removal if requested
                 if ($request->boolean('remove_bg')) {
                     try {
                         $php = PHP_BINARY;
@@ -117,11 +122,11 @@ public function store(Request $request, $championshipId)
 
             $lastBib = RaceResult::where('race_id', $race->id)->max(DB::raw('CAST(bib_number AS SIGNED)'));
             $result = RaceResult::create([
-                'race_id' => $race->id,
-                'user_id' => $user->id,
-                'name' => $user->name,
-                'bib_number' => (string) ($lastBib ? $lastBib + 1 : 1),
-                'category_id' => $request->category_id,
+                'race_id'        => $race->id,
+                'user_id'        => $user->id,
+                'name'           => $user->name,
+                'bib_number'     => (string) ($lastBib ? $lastBib + 1 : 1),
+                'category_id'    => $request->category_id,
                 'status_payment' => $request->status_payment,
                 'payment_method' => $request->payment_method
             ]);
@@ -135,23 +140,24 @@ public function store(Request $request, $championshipId)
     }
 
     /**
-     * Excluir inscrição/resultado
+     * Delete registration/result.
      */
-    public function destroy($id)
+    public function destroy(int $id): JsonResponse
     {
         RaceResult::findOrFail($id)->delete();
         return response()->json(['message' => 'Removido com sucesso!']);
     }
 
     /**
-     * Importar CSV
+     * Import CSV results list.
      */
-    public function uploadCsv(Request $request, $championshipId)
+    public function uploadCsv(Request $request, int $championshipId): JsonResponse
     {
         $request->validate(['file' => 'required|file']);
         $race = Race::where('championship_id', $championshipId)->first();
-        if (!$race)
+        if (!$race) {
             return response()->json(['error' => 'Corrida não encontrada'], 404);
+        }
 
         try {
             DB::beginTransaction();
@@ -161,23 +167,26 @@ public function uploadCsv(Request $request, $championshipId)
             $header = array_map(fn($h) => strtoupper(trim($h)), array_shift($data));
 
             $cols = [
-                'bib' => array_search('PEITO', $header) ?: array_search('NUMERO', $header),
+                'bib'  => array_search('PEITO', $header) ?: array_search('NUMERO', $header),
                 'name' => array_search('NOME', $header),
-                'doc' => array_search('CPF', $header) ?: array_search('DOCUMENTO', $header),
-                'cat' => array_search('CATEGORIA', $header),
+                'doc'  => array_search('CPF', $header) ?: array_search('DOCUMENTO', $header),
+                'cat'  => array_search('CATEGORIA', $header),
             ];
 
             foreach ($data as $row) {
-                if (empty($row) || count($row) < 2)
+                if (empty($row) || count($row) < 2) {
                     continue;
+                }
                 $name = trim($row[$cols['name']] ?? '');
                 $catName = trim($row[$cols['cat']] ?? '');
-                if (!$name || !$catName)
+                if (!$name || !$catName) {
                     continue;
+                }
 
                 $cat = Category::where('championship_id', $championshipId)->where('name', 'like', "%$catName%")->first();
-                if (!$cat)
+                if (!$cat) {
                     continue;
+                }
 
                 $bib = trim($row[$cols['bib']] ?? '');
                 if (!$bib) {
@@ -196,9 +205,9 @@ public function uploadCsv(Request $request, $championshipId)
     }
 
     /**
-     * Atualizar apenas o pagamento
+     * Update payment status manually.
      */
-    public function updatePayment(Request $request, $id)
+    public function updatePayment(Request $request, int $id): JsonResponse
     {
         $result = RaceResult::findOrFail($id);
         $result->update($request->only(['status_payment', 'payment_method']));
@@ -206,22 +215,40 @@ public function updatePayment(Request $request, $id)
     }
 
     /**
-     * Exportar CSV de Atletas
+     * Export CSV of athletes using memory-efficient cursors.
      */
-    public function exportCsv($championshipId)
+    public function exportCsv(int $championshipId): StreamedResponse|JsonResponse
     {
         $race = Race::where('championship_id', $championshipId)->first();
-        if (!$race)
+        if (!$race) {
             return response()->json(['error' => 'Corrida não encontrada'], 404);
-        $results = RaceResult::where('race_id', $race->id)->with(['category', 'user'])->get();
+        }
+
+        $headers = [
+            'Content-Type'        => 'text/csv', 
+            'Content-Disposition' => 'attachment; filename="inscritos.csv"'
+        ];
 
-        $headers = ['Content-Type' => 'text/csv', 'Content-Disposition' => 'attachment; filename="inscritos.csv"'];
-        return response()->stream(function () use ($results) {
+        return response()->stream(function () use ($race) {
             $file = fopen('php://output', 'w');
             fputcsv($file, ['BIB', 'NOME', 'CATEGORIA', 'DOCUMENTO', 'GENERO', 'STATUS', 'METODO']);
+
+            $results = RaceResult::where('race_id', $race->id)
+                ->with(['category:id,name', 'user:id,cpf,gender'])
+                ->cursor();
+
             foreach ($results as $row) {
-                fputcsv($file, [$row->bib_number, $row->name, $row->category->name ?? 'Geral', $row->user->cpf ?? '', $row->user->gender ?? 'MISTO', $row->status_payment, $row->payment_method]);
+                fputcsv($file, [
+                    $row->bib_number, 
+                    $row->name, 
+                    $row->category->name ?? 'Geral', 
+                    $row->user->cpf ?? '', 
+                    $row->user->gender ?? 'MISTO', 
+                    $row->status_payment, 
+                    $row->payment_method
+                ]);
             }
+
             fclose($file);
         }, 200, $headers);
     }
diff --git a/app/Http/Controllers/Admin/RaceWizardController.php b/app/Http/Controllers/Admin/RaceWizardController.php
index 83d66a1..413a0a4 100644
--- a/app/Http/Controllers/Admin/RaceWizardController.php
+++ b/app/Http/Controllers/Admin/RaceWizardController.php
@@ -4,134 +4,110 @@
 
 use App\Http\Controllers\Controller;
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
 use App\Models\Championship;
 use App\Models\Category;
 use App\Models\Race;
-use App\Models\Product; // Se formos usar produtos globais ou específicos
+use App\Models\Product;
+use App\Models\Coupon;
 use Illuminate\Support\Facades\DB;
 use Illuminate\Support\Facades\Log;
 
 class RaceWizardController extends Controller
 {
     /**
-     * Cria um evento completo de corrida (Evento + Categorias/Subcategorias + Configs de Prova)
-     * Recebe um JSON complexo do Wizard Frontend.
+     * Create a complete race event (Championship + Race info + Categories + Products + Coupons).
      */
-    public function store(Request $request)
+    public function store(Request $request): JsonResponse
     {
-        /*
-         Payload esperado:
-         {
-            "general": { "name": "Corrida X", "start_date": "...", "location": "...", "description": "..." },
-            "categories": [
-               { 
-                  "name": "5km", "price": 100.00,
-                  "subcategories": [
-                     { "name": "18-29 anos", "min_age": 18, "max_age": 29, "gender": "MISTO" }
-                  ] 
-               }
-            ],
-            "products": [ ... ],
-            "coupons": [ ... ]
-         }
-        */
-
-        // TODO: Validação robusta aqui
         $data = $request->validate([
-            'general.name' => 'required|string',
+            'general.name'       => 'required|string',
             'general.start_date' => 'required|date',
-            'general.sport_id' => 'nullable|integer'
+            'general.sport_id'   => 'nullable|integer'
         ]);
 
         try {
             DB::beginTransaction();
 
-            $clubId = $request->user()->club_id; // Pega do admin logado
+            $clubId = $request->user()->club_id;
 
-            // 1. Criar o "Campeonato" (Evento pai)
             $championship = Championship::create([
-                'club_id' => $clubId,
-                'name' => $request->input('general.name'),
-                'start_date' => $request->input('general.start_date'),
-                'end_date' => $request->input('general.start_date'),
-                'sport_id' => $request->input('general.sport_id', 3), // Default 3 (Corrida)
+                'club_id'           => $clubId,
+                'name'              => $request->input('general.name'),
+                'start_date'        => $request->input('general.start_date'),
+                'end_date'          => $request->input('general.start_date'),
+                'sport_id'          => $request->input('general.sport_id', 3),
                 'registration_type' => 'individual',
-                'format' => 'racing',
-                'status' => 'upcoming',
-                'description' => $request->input('general.description'),
+                'format'            => 'racing',
+                'status'            => 'upcoming',
+                'description'       => $request->input('general.description'),
             ]);
 
-            // 2. Criar tabela detalhe de corrida (opcional, se tiver dados específicos)
             Race::create([
                 'championship_id' => $championship->id,
-                'start_datetime' => $request->input('general.start_date'),
-                'location_name' => $request->input('general.location'),
-                'kits_info' => $request->input('general.kits_info')
+                'start_datetime'  => $request->input('general.start_date'),
+                'location_name'   => $request->input('general.location'),
+                'kits_info'       => $request->input('general.kits_info')
             ]);
 
-            // 3. Criar Categorias e Subcategorias
             $categoriesData = $request->input('categories', []);
             foreach ($categoriesData as $catData) {
-                // Categoria Pai (Ex: 5km)
                 $parentCat = Category::create([
-                    'championship_id' => $championship->id,
-                    'name' => $catData['name'],
-                    'price' => $catData['price'] ?? 0,
-                    'gender' => $catData['gender'] ?? 'MISTO',
+                    'championship_id'   => $championship->id,
+                    'name'              => $catData['name'],
+                    'price'             => $catData['price'] ?? 0,
+                    'gender'            => $catData['gender'] ?? 'MISTO',
                     'included_products' => $catData['included_products'] ?? null,
-                    // Parent fields can be null for top-level
                 ]);
 
-                // Subcategorias (Ex: 18-29 anos)
                 if (!empty($catData['subcategories'])) {
                     foreach ($catData['subcategories'] as $subData) {
                         Category::create([
-                            'championship_id' => $championship->id,
-                            'parent_id' => $parentCat->id, // VINCULO HIERÁRQUICO
-                            'name' => $subData['name'],
-                            'min_age' => $subData['min_age'] ?? null,
-                            'max_age' => $subData['max_age'] ?? null,
-                            'gender' => $subData['gender'] ?? $parentCat->gender,
-                            'price' => $subData['price'] ?? 0, // Agora a subcategoria é um acréscimo (surcharge)
-
-                            'included_products' => $parentCat->included_products, // Herda produtos da categoria pai
+                            'championship_id'   => $championship->id,
+                            'parent_id'         => $parentCat->id,
+                            'name'              => $subData['name'],
+                            'min_age'           => $subData['min_age'] ?? null,
+                            'max_age'           => $subData['max_age'] ?? null,
+                            'gender'            => $subData['gender'] ?? $parentCat->gender,
+                            'price'             => $subData['price'] ?? 0,
+                            'included_products' => $parentCat->included_products,
                         ]);
                     }
                 }
             }
 
-            // 4. Produtos (Kits, Camisetas)
             $productsData = $request->input('products', []);
             foreach ($productsData as $prod) {
-                if (empty($prod['name']))
+                if (empty($prod['name'])) {
                     continue;
+                }
                 Product::create([
                     'club_id' => $clubId,
-                    'name' => $prod['name'],
-                    'price' => $prod['price'] ?? 0,
-                    'variants' => $prod['variants'] ?? null
+                    'name'    => $prod['name'],
+                    'price'   => $prod['price'] ?? 0,
+                    'variants'=> $prod['variants'] ?? null
                 ]);
             }
 
-            // 5. Cupons de Desconto
             $couponsData = $request->input('coupons', []);
             foreach ($couponsData as $cup) {
-                if (empty($cup['code']))
+                if (empty($cup['code'])) {
                     continue;
-                \App\Models\Coupon::create([
-                    'club_id' => $clubId,
-                    'code' => strtoupper($cup['code']),
-                    'discount_type' => $cup['discount_type'] ?? 'fixed',
+                }
+                Coupon::create([
+                    'club_id'        => $clubId,
+                    'code'           => strtoupper($cup['code']),
+                    'discount_type'  => $cup['discount_type'] ?? 'fixed',
                     'discount_value' => $cup['discount_value'] ?? 0,
-                    'max_uses' => $cup['max_uses'] ?? 100,
-                    'expires_at' => $cup['expires_at'] ?? $championship->start_date
+                    'max_uses'       => $cup['max_uses'] ?? 100,
+                    'expires_at'     => $cup['expires_at'] ?? $championship->start_date
                 ]);
             }
 
             DB::commit();
 
             return response()->json([
-                'message' => 'Evento de corrida criado com sucesso!',
+                'message'         => 'Evento de corrida criado com sucesso!',
                 'championship_id' => $championship->id
             ], 201);
 
diff --git a/app/Http/Controllers/Admin/StatisticsController.php b/app/Http/Controllers/Admin/StatisticsController.php
index f0e8220..de152f7 100644
--- a/app/Http/Controllers/Admin/StatisticsController.php
+++ b/app/Http/Controllers/Admin/StatisticsController.php
@@ -4,21 +4,21 @@
 
 use App\Http\Controllers\Controller;
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
 use App\Models\GameMatch;
 use App\Models\MatchEvent;
 use App\Models\Championship;
 use App\Models\User;
+use App\Models\RaceResult;
+use App\Models\Category;
 use Illuminate\Support\Facades\DB;
 
 class StatisticsController extends Controller
 {
     /**
-     * Estatísticas de gols por jogador
+     * Goals by player statistics.
      */
-    /**
-     * Estatísticas de gols por jogador
-     */
-    public function goalsByPlayer(Request $request, $championshipId)
+    public function goalsByPlayer(Request $request, int $championshipId): JsonResponse
     {
         $championship = Championship::findOrFail($championshipId);
 
@@ -27,7 +27,6 @@ public function goalsByPlayer(Request $request, $championshipId)
                 $query->where('championship_id', $championshipId);
             });
 
-        // Exclude repescagem if not included
         if (!($championship->include_repescagem_goals ?? false)) {
             $query->whereHas('gameMatch', function ($subQuery) {
                 $subQuery->where(function($q) {
@@ -38,7 +37,6 @@ public function goalsByPlayer(Request $request, $championshipId)
             });
         }
 
-        // Exclude knockout if not included
         if (!($championship->include_knockout_goals ?? false)) {
             $query->whereHas('gameMatch', function ($subQuery) {
                 $subQuery->where(function($q) {
@@ -67,19 +65,18 @@ public function goalsByPlayer(Request $request, $championshipId)
     }
 
     /**
-     * Artilharia do campeonato
+     * Top scorers.
      */
-    public function topScorers(Request $request, $championshipId)
+    public function topScorers(Request $request, int $championshipId): JsonResponse
     {
         $limit = $request->input('limit', 10);
+        $championship = Championship::findOrFail($championshipId);
 
         $query = MatchEvent::where('event_type', 'goal')
             ->whereHas('gameMatch', function ($query) use ($championshipId) {
                 $query->where('championship_id', $championshipId);
             });
 
-        // Exclude repescagem if not included
-        $championship = Championship::findOrFail($championshipId);
         if (!($championship->include_repescagem_goals ?? false)) {
             $query->whereHas('gameMatch', function ($subQuery) {
                 $subQuery->where(function($q) {
@@ -90,7 +87,6 @@ public function topScorers(Request $request, $championshipId)
             });
         }
 
-        // Exclude knockout if not included
         if (!($championship->include_knockout_goals ?? false)) {
             $query->whereHas('gameMatch', function ($subQuery) {
                 $subQuery->where(function($q) {
@@ -119,9 +115,9 @@ public function topScorers(Request $request, $championshipId)
     }
 
     /**
-     * Assistências por jogador
+     * Assists by player.
      */
-    public function assistsByPlayer(Request $request, $championshipId)
+    public function assistsByPlayer(Request $request, int $championshipId): JsonResponse
     {
         $championship = Championship::findOrFail($championshipId);
 
@@ -130,7 +126,6 @@ public function assistsByPlayer(Request $request, $championshipId)
                 $query->where('championship_id', $championshipId);
             });
 
-        // Exclude repescagem if not included
         if (!($championship->include_repescagem_assists ?? false)) {
             $query->whereHas('gameMatch', function ($subQuery) {
                 $subQuery->where(function($q) {
@@ -141,7 +136,6 @@ public function assistsByPlayer(Request $request, $championshipId)
             });
         }
 
-        // Exclude knockout if not included
         if (!($championship->include_knockout_assists ?? false)) {
             $query->whereHas('gameMatch', function ($subQuery) {
                 $subQuery->where(function($q) {
@@ -170,9 +164,9 @@ public function assistsByPlayer(Request $request, $championshipId)
     }
 
     /**
-     * Cartões por jogador
+     * Cards by player.
      */
-    public function cardsByPlayer(Request $request, $championshipId)
+    public function cardsByPlayer(Request $request, int $championshipId): JsonResponse
     {
         $championship = Championship::findOrFail($championshipId);
 
@@ -181,7 +175,6 @@ public function cardsByPlayer(Request $request, $championshipId)
                 $query->where('championship_id', $championshipId);
             });
 
-        // Exclude repescagem if not included
         if (!($championship->include_repescagem_cards ?? true)) {
             $query->whereHas('gameMatch', function ($subQuery) {
                 $subQuery->where(function($q) {
@@ -192,7 +185,6 @@ public function cardsByPlayer(Request $request, $championshipId)
             });
         }
 
-        // Exclude knockout if not included
         if (!($championship->include_knockout_cards ?? true)) {
             $query->whereHas('gameMatch', function ($subQuery) {
                 $subQuery->where(function($q) {
@@ -226,17 +218,15 @@ public function cardsByPlayer(Request $request, $championshipId)
     }
 
     /**
-     * Classificação do campeonato
+     * Standings.
      */
-    public function standings(Request $request, $championshipId)
+    public function standings(Request $request, int $championshipId): JsonResponse
     {
         $championship = Championship::findOrFail($championshipId);
 
-        // Busca todas as partidas finalizadas
         $query = GameMatch::where('championship_id', $championshipId)
             ->where('status', 'finished');
 
-        // Exclude repescagem if not included in standings
         if (!($championship->include_repescagem_standings ?? false)) {
             $query->where(function($q) {
                 $q->where('round_name', '!=', 'Repescagem')
@@ -245,7 +235,6 @@ public function standings(Request $request, $championshipId)
             });
         }
 
-        // Exclude knockout if not included
         if (!($championship->include_knockout_standings ?? false)) {
             $query->where(function($q) {
                 $q->where('is_knockout', '!=', true)
@@ -261,51 +250,48 @@ public function standings(Request $request, $championshipId)
             });
         }
 
-        $matches = $query->get();
+        $matches = $query->with(['homeTeam:id,name,logo_path', 'awayTeam:id,name,logo_path'])->get();
 
         $standings = [];
-
         $hasGroups = in_array($championship->format, ['groups', 'group_knockout']);
 
         foreach ($matches as $match) {
-            // Inicializa equipes se não existirem
             $groupName = $hasGroups ? ($match->group_name ?? 'Geral') : 'Geral';
 
             if (!isset($standings[$match->home_team_id])) {
                 $standings[$match->home_team_id] = [
-                    'team_id' => $match->home_team_id,
-                    'team_name' => $match->homeTeam->name ?? 'N/A',
-                    'team_logo' => $match->homeTeam->logo_path ? url('api/storage/' . $match->homeTeam->logo_path) : null,
-                    'group_name' => $groupName,
-                    'played' => 0,
-                    'won' => 0,
-                    'drawn' => 0,
-                    'lost' => 0,
-                    'goals_for' => 0,
-                    'goals_against' => 0,
+                    'team_id'         => $match->home_team_id,
+                    'team_name'       => $match->homeTeam->name ?? 'N/A',
+                    'team_logo'       => $match->homeTeam->logo_path ? url('api/storage/' . $match->homeTeam->logo_path) : null,
+                    'group_name'      => $groupName,
+                    'played'          => 0,
+                    'won'             => 0,
+                    'drawn'           => 0,
+                    'lost'            => 0,
+                    'goals_for'       => 0,
+                    'goals_against'   => 0,
                     'goal_difference' => 0,
-                    'points' => 0,
+                    'points'          => 0,
                 ];
             }
 
             if (!isset($standings[$match->away_team_id])) {
                 $standings[$match->away_team_id] = [
-                    'team_id' => $match->away_team_id,
-                    'team_name' => $match->awayTeam->name ?? 'N/A',
-                    'team_logo' => $match->awayTeam->logo_path ? url('api/storage/' . $match->awayTeam->logo_path) : null,
-                    'group_name' => $groupName,
-                    'played' => 0,
-                    'won' => 0,
-                    'drawn' => 0,
-                    'lost' => 0,
-                    'goals_for' => 0,
-                    'goals_against' => 0,
+                    'team_id'         => $match->away_team_id,
+                    'team_name'       => $match->awayTeam->name ?? 'N/A',
+                    'team_logo'       => $match->awayTeam->logo_path ? url('api/storage/' . $match->awayTeam->logo_path) : null,
+                    'group_name'      => $groupName,
+                    'played'          => 0,
+                    'won'             => 0,
+                    'drawn'           => 0,
+                    'lost'            => 0,
+                    'goals_for'       => 0,
+                    'goals_against'   => 0,
                     'goal_difference' => 0,
-                    'points' => 0,
+                    'points'          => 0,
                 ];
             }
 
-            // Atualiza estatísticas
             $standings[$match->home_team_id]['played']++;
             $standings[$match->away_team_id]['played']++;
 
@@ -315,7 +301,6 @@ public function standings(Request $request, $championshipId)
             $standings[$match->away_team_id]['goals_for'] += $match->away_score ?? 0;
             $standings[$match->away_team_id]['goals_against'] += $match->home_score ?? 0;
 
-            // Determina resultado
             if ($match->home_score > $match->away_score) {
                 $standings[$match->home_team_id]['won']++;
                 $standings[$match->home_team_id]['points'] += 3;
@@ -332,12 +317,10 @@ public function standings(Request $request, $championshipId)
             }
         }
 
-        // Calcula saldo de gols
         foreach ($standings as &$team) {
             $team['goal_difference'] = $team['goals_for'] - $team['goals_against'];
         }
 
-        // Ordena por Grupo, Pontos, Desempate Manual, Saldo
         $tiebreakerPriority = $championship->tiebreaker_priority ?? [];
 
         usort($standings, function ($a, $b) use ($tiebreakerPriority) {
@@ -348,16 +331,15 @@ public function standings(Request $request, $championshipId)
                 return $b['points'] - $a['points'];
             }
             
-            // Desempate manual (só se aplicam se os pontos forem iguais)
             $posA = array_search($a['team_id'], $tiebreakerPriority);
             $posB = array_search($b['team_id'], $tiebreakerPriority);
             
             if ($posA !== false && $posB !== false) {
-                return $posA - $posB; // Menor índice = maior prioridade
+                return $posA - $posB;
             } elseif ($posA !== false) {
-                return -1; // A está na lista manual (ganha do B)
+                return -1;
             } elseif ($posB !== false) {
-                return 1; // B está na lista manual (ganha do A)
+                return 1;
             }
 
             if ($a['goal_difference'] != $b['goal_difference']) {
@@ -366,7 +348,6 @@ public function standings(Request $request, $championshipId)
             return $b['goals_for'] - $a['goals_for'];
         });
 
-        // Adiciona Posição (reinicia por grupo)
         $rankedStandings = array_values($standings);
         $currentGroup = null;
         $position = 1;
@@ -383,58 +364,46 @@ public function standings(Request $request, $championshipId)
     }
 
     /**
-     * Histórico de um jogador
+     * Player history details.
      */
-    public function playerHistory(Request $request, $playerId)
+    public function playerHistory(Request $request, int $playerId): JsonResponse
     {
         $player = User::findOrFail($playerId);
 
-        // Partidas jogadas
         $matches = MatchEvent::where('player_id', $playerId)
             ->with('gameMatch.championship:id,name')
             ->select('game_match_id')
             ->distinct()
             ->get();
 
-        // Estatísticas gerais
         $stats = [
             'total_matches' => $matches->count(),
-            'total_goals' => MatchEvent::where('player_id', $playerId)
-                ->where('event_type', 'goal')
-                ->count(),
-            'total_assists' => MatchEvent::where('player_id', $playerId)
-                ->where('event_type', 'assist')
-                ->count(),
-            'yellow_cards' => MatchEvent::where('player_id', $playerId)
-                ->where('event_type', 'yellow_card')
-                ->count(),
-            'red_cards' => MatchEvent::where('player_id', $playerId)
-                ->where('event_type', 'red_card')
-                ->count(),
-            'mvp_awards' => GameMatch::where('mvp_player_id', $playerId)->count(),
+            'total_goals'   => MatchEvent::where('player_id', $playerId)->where('event_type', 'goal')->count(),
+            'total_assists' => MatchEvent::where('player_id', $playerId)->where('event_type', 'assist')->count(),
+            'yellow_cards'  => MatchEvent::where('player_id', $playerId)->where('event_type', 'yellow_card')->count(),
+            'red_cards'     => MatchEvent::where('player_id', $playerId)->where('event_type', 'red_card')->count(),
+            'mvp_awards'    => GameMatch::where('mvp_player_id', $playerId)->count(),
         ];
 
         return response()->json([
-            'player' => $player,
-            'stats' => $stats,
+            'player'  => $player,
+            'stats'   => $stats,
             'matches' => $matches,
         ]);
     }
 
     /**
-     * Dashboard geral do campeonato
+     * Championship dashboard overview.
      */
-    public function championshipDashboard(Request $request, $championshipId)
+    public function championshipDashboard(Request $request, int $championshipId): JsonResponse
     {
-        $championship = Championship::with('sport')->findOrFail($championshipId);
+        $championship = Championship::with('sport:id,name')->findOrFail($championshipId);
 
         $stats = [
-            'total_teams' => $championship->teams()->count(),
-            'total_matches' => GameMatch::where('championship_id', $championshipId)->count(),
-            'finished_matches' => GameMatch::where('championship_id', $championshipId)
-                ->where('status', 'finished')
-                ->count(),
-            'total_goals' => MatchEvent::where('event_type', 'goal')
+            'total_teams'             => $championship->teams()->count(),
+            'total_matches'           => GameMatch::where('championship_id', $championshipId)->count(),
+            'finished_matches'        => GameMatch::where('championship_id', $championshipId)->where('status', 'finished')->count(),
+            'total_goals'             => MatchEvent::where('event_type', 'goal')
                 ->whereHas('gameMatch', function ($query) use ($championshipId, $championship) {
                     $query->where('championship_id', $championshipId);
                     if (!($championship->include_repescagem_goals ?? false)) {
@@ -460,29 +429,24 @@ public function championshipDashboard(Request $request, $championshipId)
                     }
                 })
                 ->count(),
-            'total_players' => MatchEvent::whereHas('gameMatch', function ($query) use ($championshipId) {
+            'total_players'           => MatchEvent::whereHas('gameMatch', function ($query) use ($championshipId) {
                 $query->where('championship_id', $championshipId);
             })
                 ->distinct('player_id')
                 ->count('player_id'),
-            // Race/Individual Inscriptions
-            'total_inscriptions' => \App\Models\RaceResult::whereHas('race', function ($q) use ($championshipId) {
+            'total_inscriptions'      => RaceResult::whereHas('race', function ($q) use ($championshipId) {
                 $q->where('championship_id', $championshipId);
             })->count(),
-            'paid_inscriptions' => \App\Models\RaceResult::whereHas('race', function ($q) use ($championshipId) {
+            'paid_inscriptions'       => RaceResult::whereHas('race', function ($q) use ($championshipId) {
                 $q->where('championship_id', $championshipId);
             })->where('status_payment', 'paid')->count(),
-            'total_subcategories' => \App\Models\Category::where('championship_id', $championshipId)
-                ->whereNotNull('parent_id')
-                ->count(),
-            'total_parent_categories' => \App\Models\Category::where('championship_id', $championshipId)
-                ->whereNull('parent_id')
-                ->count(),
+            'total_subcategories'     => Category::where('championship_id', $championshipId)->whereNotNull('parent_id')->count(),
+            'total_parent_categories' => Category::where('championship_id', $championshipId)->whereNull('parent_id')->count(),
         ];
 
         return response()->json([
             'championship' => $championship,
-            'stats' => $stats,
+            'stats'        => $stats,
         ]);
     }
 }
diff --git a/app/Http/Controllers/Admin/TemporaryAccessController.php b/app/Http/Controllers/Admin/TemporaryAccessController.php
index e8eeec9..5f2bc25 100644
--- a/app/Http/Controllers/Admin/TemporaryAccessController.php
+++ b/app/Http/Controllers/Admin/TemporaryAccessController.php
@@ -4,6 +4,7 @@
 
 use App\Http\Controllers\Controller;
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
 use App\Models\User;
 use Illuminate\Support\Facades\Hash;
 use Illuminate\Support\Str;
@@ -12,43 +13,43 @@
 class TemporaryAccessController extends Controller
 {
     /**
-     * List only temporary admins created by this user (or belongs to same club)
+     * List only temporary admins created by this user or belonging to same club.
      */
-    public function index(Request $request)
+    public function index(Request $request): JsonResponse
     {
         $currentUser = $request->user();
 
-        // Query: Users created by me OR users in my club that have expiry date
         $query = User::whereNotNull('expires_at');
 
         if ($currentUser->isSuperAdmin()) {
-            // Super admin sees all or filters by club if needed
             if ($request->has('club_id')) {
                 $query->where('club_id', $request->club_id);
             }
         } elseif ($currentUser->isClubAdmin()) {
             $query->where('club_id', $currentUser->club_id);
         } else {
-            // Regular user shouldn't be here, but just in case
             return response()->json([], 403);
         }
 
-        // Return sorted by expiration (soonest to expire first)
-        return response()->json($query->with('club')->orderBy('expires_at', 'asc')->get());
+        $temporaryAdmins = $query->with('club:id,name')
+            ->orderBy('expires_at', 'asc')
+            ->get();
+
+        return response()->json($temporaryAdmins);
     }
 
     /**
-     * Create a new temporary admin
+     * Create a new temporary admin.
      */
-    public function store(Request $request)
+    public function store(Request $request): JsonResponse
     {
         $currentUser = $request->user();
 
         $validated = $request->validate([
-            'name' => 'required|string|max:255',
-            'email' => 'required|email|unique:users,email',
+            'name'       => 'required|string|max:255',
+            'email'      => 'required|email|unique:users,email',
             'expires_at' => 'required|date|after:now',
-            'club_id' => 'nullable|exists:clubs,id' // Only for SuperAdmin
+            'club_id'    => 'nullable|exists:clubs,id'
         ]);
 
         $clubId = $currentUser->club_id;
@@ -56,50 +57,46 @@ public function store(Request $request)
             $clubId = $validated['club_id'];
         }
 
-        // Generate a random password or allow setting one? 
-        // For security, maybe let them copy it once. default: 12345678 (user requested simplicity)
-        // Let's generate a strong random string 
         $plainPassword = $request->input('password', Str::random(10));
 
         $user = User::create([
-            'name' => $validated['name'],
-            'email' => $validated['email'],
-            'password' => Hash::make($plainPassword),
-            'is_admin' => true,
-            'club_id' => $clubId,
+            'name'       => $validated['name'],
+            'email'      => $validated['email'],
+            'password'   => Hash::make($plainPassword),
+            'is_admin'   => true,
+            'club_id'    => $clubId,
             'expires_at' => $validated['expires_at'],
             'created_by' => $currentUser->id,
-            'is_active' => true
+            'is_active'  => true
         ]);
 
         AuditLogger::log('access.create', "Criou acesso temporário para '{$user->name}' (Expira em: {$user->expires_at})", [
             'target_user_id' => $user->id,
-            'expires_at' => $user->expires_at
+            'expires_at'     => $user->expires_at
         ]);
 
         return response()->json([
-            'message' => 'Acesso temporário criado com sucesso.',
-            'user' => $user,
+            'message'        => 'Acesso temporário criado com sucesso.',
+            'user'           => $user,
             'plain_password' => $plainPassword
         ], 201);
     }
 
     /**
-     * Renew access (update expiry date)
+     * Renew access (update expiry date).
      */
-    public function update($id, Request $request)
+    public function update(int $id, Request $request): JsonResponse
     {
         $currentUser = $request->user();
         $userToUpdate = User::findOrFail($id);
 
-        // Security Check
         if (!$currentUser->isSuperAdmin() && $userToUpdate->club_id !== $currentUser->club_id) {
             return response()->json(['message' => 'Não autorizado.'], 403);
         }
 
         $validated = $request->validate([
             'expires_at' => 'required|date|after:now',
-            'password' => 'nullable|string|min:6' // Optional password reset
+            'password'   => 'nullable|string|min:6'
         ]);
 
         $userToUpdate->expires_at = $validated['expires_at'];
@@ -112,29 +109,28 @@ public function update($id, Request $request)
 
         AuditLogger::log('access.update', "Renovou ou alterou acesso temporário de '{$userToUpdate->name}' (Nova expiração: {$userToUpdate->expires_at})", [
             'target_user_id' => $id,
-            'expires_at' => $userToUpdate->expires_at
+            'expires_at'     => $userToUpdate->expires_at
         ]);
 
         return response()->json([
             'message' => 'Acesso renovado com sucesso.',
-            'user' => $userToUpdate
+            'user'    => $userToUpdate
         ]);
     }
 
     /**
-     * Revoke access immediately
+     * Revoke access immediately.
      */
-    public function destroy($id, Request $request)
+    public function destroy(int $id, Request $request): JsonResponse
     {
         $currentUser = $request->user();
         $userToDelete = User::findOrFail($id);
 
-        // Security Check
         if (!$currentUser->isSuperAdmin() && $userToDelete->club_id !== $currentUser->club_id) {
             return response()->json(['message' => 'Não autorizado.'], 403);
         }
 
-        $userToDelete->delete(); // Soft delete or hard delete? Default is hard unless trait used.
+        $userToDelete->delete();
 
         AuditLogger::log('access.revoke', "Revogou imediatamente o acesso temporário de '{$userToDelete->name}' (ID: {$id})");
 
diff --git a/app/Http/Controllers/Admin/UploadController.php b/app/Http/Controllers/Admin/UploadController.php
index 888616a..4432ff4 100644
--- a/app/Http/Controllers/Admin/UploadController.php
+++ b/app/Http/Controllers/Admin/UploadController.php
@@ -5,15 +5,17 @@
 use App\Http\Controllers\Controller;
 use App\Models\Championship;
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
 use Illuminate\Support\Facades\Storage;
+use Illuminate\Support\Facades\Log;
 use Illuminate\Support\Str;
 
 class UploadController extends Controller
 {
     /**
-     * Upload de logo de equipe
+     * Upload team logo.
      */
-    public function uploadTeamLogo(Request $request)
+    public function uploadTeamLogo(Request $request): JsonResponse
     {
         $request->validate([
             'image' => 'required|image|mimes:jpeg,png,jpg|max:2048',
@@ -22,60 +24,55 @@ public function uploadTeamLogo(Request $request)
         try {
             $image = $request->file('image');
             $filename = 'team_' . time() . '_' . Str::random(10) . '.' . $image->getClientOriginalExtension();
-
-            // Salva na pasta public/storage/teams
             $path = $image->storeAs('teams', $filename, 'public');
 
             return response()->json([
                 'message' => 'Logo enviado com sucesso!',
-                'path' => $path,
-                'url' => '/storage/' . $path
+                'path'    => $path,
+                'url'     => '/storage/' . $path
             ]);
         } catch (\Exception $e) {
             return response()->json([
                 'message' => 'Erro ao fazer upload do logo.',
-                'error' => $e->getMessage()
+                'error'   => $e->getMessage()
             ], 500);
         }
     }
 
     /**
-     * Upload de foto de jogador
+     * Upload player photo.
      */
-    public function uploadPlayerPhoto(Request $request)
+    public function uploadPlayerPhoto(Request $request): JsonResponse
     {
         $request->validate([
-            'image' => 'required|image|mimes:jpeg,png,jpg|max:4096', // Max 4MB
+            'image' => 'required|image|mimes:jpeg,png,jpg|max:4096',
         ]);
 
         try {
             $image = $request->file('image');
             $filename = 'player_' . time() . '_' . Str::random(10) . '.' . $image->getClientOriginalExtension();
-
-            // Salva na pasta public/storage/players
             $path = $image->storeAs('players', $filename, 'public');
 
             return response()->json([
                 'message' => 'Foto enviada com sucesso!',
-                'path' => $path,
-                'url' => '/storage/' . $path
+                'path'    => $path,
+                'url'     => '/storage/' . $path
             ]);
         } catch (\Exception $e) {
             return response()->json([
                 'message' => 'Erro ao fazer upload da foto.',
-                'error' => $e->getMessage()
+                'error'   => $e->getMessage()
             ], 500);
         }
     }
 
     /**
-     * Upload de logo de campeonato (1:1)
-     * Organizado em: championships/{championship_id}/logo.ext
+     * Upload championship logo (1:1).
      */
-    public function uploadChampionshipLogo(Request $request, $championshipId)
+    public function uploadChampionshipLogo(Request $request, int $championshipId): JsonResponse
     {
         $request->validate([
-            'logo' => 'required|image|mimes:jpeg,png,jpg|max:5120', // 5MB
+            'logo' => 'required|image|mimes:jpeg,png,jpg|max:5120',
         ]);
 
         try {
@@ -84,7 +81,6 @@ public function uploadChampionshipLogo(Request $request, $championshipId)
             $extension = $image->getClientOriginalExtension();
             $filename = 'logo.' . $extension;
 
-            // Remove logo anterior se existir
             if ($championship->logo_url) {
                 $oldPath = str_replace('/storage/', '', parse_url($championship->logo_url, PHP_URL_PATH));
                 if (Storage::disk('public')->exists($oldPath)) {
@@ -92,35 +88,32 @@ public function uploadChampionshipLogo(Request $request, $championshipId)
                 }
             }
 
-            // Salva em: storage/app/public/championships/{id}/logo.ext
             $path = $image->storeAs("championships/{$championshipId}", $filename, 'public');
             $url = '/storage/' . $path;
 
-            // Atualiza o banco de dados
             $championship->logo_url = '/storage/' . $path;
             $championship->save();
 
             return response()->json([
-                'message' => 'Logo do campeonato atualizado com sucesso!',
+                'message'  => 'Logo do campeonato atualizado com sucesso!',
                 'logo_url' => $url
             ]);
         } catch (\Exception $e) {
-            \Log::error('Error uploading championship logo: ' . $e->getMessage());
+            Log::error('Error uploading championship logo: ' . $e->getMessage());
             return response()->json([
                 'message' => 'Erro ao fazer upload do logo.',
-                'error' => $e->getMessage()
+                'error'   => $e->getMessage()
             ], 500);
         }
     }
 
     /**
-     * Upload de banner/capa de campeonato (16:9)
-     * Organizado em: championships/{championship_id}/banner.ext
+     * Upload championship banner (16:9).
      */
-    public function uploadChampionshipBanner(Request $request, $championshipId)
+    public function uploadChampionshipBanner(Request $request, int $championshipId): JsonResponse
     {
         $request->validate([
-            'banner' => 'required|image|mimes:jpeg,png,jpg|max:5120', // 5MB
+            'banner' => 'required|image|mimes:jpeg,png,jpg|max:5120',
         ]);
 
         try {
@@ -129,7 +122,6 @@ public function uploadChampionshipBanner(Request $request, $championshipId)
             $extension = $image->getClientOriginalExtension();
             $filename = 'banner.' . $extension;
 
-            // Remove banner anterior se existir
             if ($championship->cover_image_url) {
                 $oldPath = str_replace('/storage/', '', parse_url($championship->cover_image_url, PHP_URL_PATH));
                 if (Storage::disk('public')->exists($oldPath)) {
@@ -137,61 +129,57 @@ public function uploadChampionshipBanner(Request $request, $championshipId)
                 }
             }
 
-            // Salva em: storage/app/public/championships/{id}/banner.ext
             $path = $image->storeAs("championships/{$championshipId}", $filename, 'public');
             $url = '/storage/' . $path;
 
-            // Atualiza o banco de dados
             $championship->cover_image_url = '/storage/' . $path;
             $championship->save();
 
             return response()->json([
-                'message' => 'Banner do campeonato atualizado com sucesso!',
-                'banner_url' => $url,
-                'cover_image_url' => $url // Para compatibilidade
+                'message'         => 'Banner do campeonato atualizado com sucesso!',
+                'banner_url'      => $url,
+                'cover_image_url' => $url
             ]);
         } catch (\Exception $e) {
-            \Log::error('Error uploading championship banner: ' . $e->getMessage());
+            Log::error('Error uploading championship banner: ' . $e->getMessage());
             return response()->json([
                 'message' => 'Erro ao fazer upload do banner.',
-                'error' => $e->getMessage()
+                'error'   => $e->getMessage()
             ], 500);
         }
     }
 
     /**
-     * Upload de imagem de campeonato (método genérico - manter por compatibilidade)
+     * Upload generic championship image.
      */
-    public function uploadChampionshipImage(Request $request)
+    public function uploadChampionshipImage(Request $request): JsonResponse
     {
         $request->validate([
-            'image' => 'required|image|mimes:jpeg,png,jpg|max:4096', // Max 4MB
+            'image' => 'required|image|mimes:jpeg,png,jpg|max:4096',
         ]);
 
         try {
             $image = $request->file('image');
             $filename = 'championship_' . time() . '_' . Str::random(10) . '.' . $image->getClientOriginalExtension();
-
-            // Salva na pasta public/storage/championships
             $path = $image->storeAs('championships', $filename, 'public');
 
             return response()->json([
                 'message' => 'Imagem enviada com sucesso!',
-                'path' => $path,
-                'url' => '/storage/' . $path
+                'path'    => $path,
+                'url'     => '/storage/' . $path
             ]);
         } catch (\Exception $e) {
             return response()->json([
                 'message' => 'Erro ao fazer upload da imagem.',
-                'error' => $e->getMessage()
+                'error'   => $e->getMessage()
             ], 500);
         }
     }
 
     /**
-     * Deletar imagem
+     * Delete image file from disk.
      */
-    public function deleteImage(Request $request)
+    public function deleteImage(Request $request): JsonResponse
     {
         $request->validate([
             'path' => 'required|string',
@@ -210,11 +198,11 @@ public function deleteImage(Request $request)
 
             return response()->json([
                 'message' => 'Imagem não encontrada.'
-            ], 404);
+                ], 404);
         } catch (\Exception $e) {
             return response()->json([
                 'message' => 'Erro ao deletar imagem.',
-                'error' => $e->getMessage()
+                'error'   => $e->getMessage()
             ], 500);
         }
     }
diff --git a/app/Http/Controllers/AuthController.php b/app/Http/Controllers/AuthController.php
index 8d3257a..fbd14b4 100644
--- a/app/Http/Controllers/AuthController.php
+++ b/app/Http/Controllers/AuthController.php
@@ -3,26 +3,30 @@
 namespace App\Http\Controllers;
 
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
 use Illuminate\Support\Facades\Hash;
 use Illuminate\Support\Facades\Auth;
 use Illuminate\Support\Facades\DB;
+use Illuminate\Support\Facades\Log;
+use Illuminate\Support\Facades\Mail;
 use App\Models\User;
 use App\Http\Resources\UserResource;
-use Illuminate\Support\Facades\Mail;
+use App\Mail\PasswordResetMail;
+use Carbon\Carbon;
 
 class AuthController extends Controller
 {
-    // 1. Registro de Novo Usuário (Atleta)
-    public function register(Request $request)
+    /**
+     * Register a new player user.
+     */
+    public function register(Request $request): JsonResponse
     {
-        // Normalizar CPF (apenas números)
         if ($request->has('cpf') && $request->input('cpf')) {
             $request->merge([
                 'cpf' => preg_replace('/\D/', '', $request->input('cpf'))
             ]);
         }
 
-        // Normalizar data de nascimento de DD/MM/YYYY para YYYY-MM-DD se necessário
         if ($request->has('birth_date') && $request->input('birth_date')) {
             $birthDate = $request->input('birth_date');
             if (preg_match('/^(\d{2})\/(\d{2})\/(\d{4})$/', $birthDate, $matches)) {
@@ -33,19 +37,19 @@ public function register(Request $request)
         }
 
         $validated = $request->validate([
-            'name' => 'required|string|max:255',
-            'email' => 'required|string|email|max:255|unique:users',
-            'password' => 'required|string|min:6|confirmed',
-            'phone' => 'nullable|string',
-            'cpf' => 'nullable|string|unique:users',
-            'birth_date' => 'nullable|date',
-            'rg' => 'nullable|string',
-            'mother_name' => 'nullable|string',
-            'gender' => 'nullable|in:M,F,O',
+            'name'            => 'required|string|max:255',
+            'email'           => 'required|string|email|max:255|unique:users',
+            'password'        => 'required|string|min:6|confirmed',
+            'phone'           => 'nullable|string',
+            'cpf'             => 'nullable|string|unique:users',
+            'birth_date'      => 'nullable|date',
+            'rg'              => 'nullable|string',
+            'mother_name'     => 'nullable|string',
+            'gender'          => 'nullable|in:M,F,O',
             'document_number' => 'nullable|string',
-            'photos' => 'nullable|array|max:3', // Allow up to 3 photos
-            'photos.*' => 'image|max:4096', // Max 4MB
-            'document' => 'nullable|image|max:4096', // Validação da foto do documento
+            'photos'          => 'nullable|array|max:3',
+            'photos.*'        => 'image|max:4096',
+            'document'        => 'nullable|image|max:4096',
         ]);
 
         $photoPaths = [];
@@ -56,7 +60,6 @@ public function register(Request $request)
             }
         }
 
-        // Set the main photo path to the first one uploaded, or null
         $mainPhotoPath = !empty($photoPaths) ? $photoPaths[0] : null;
 
         $documentPath = null;
@@ -68,19 +71,19 @@ public function register(Request $request)
             DB::beginTransaction();
 
             $user = User::create([
-                'name' => $validated['name'],
-                'email' => $validated['email'],
-                'password' => $validated['password'],
-                'phone' => $validated['phone'] ?? null,
-                'cpf' => $validated['cpf'] ?? null,
-                'birth_date' => $validated['birth_date'] ?? null,
-                'rg' => $validated['rg'] ?? null,
-                'mother_name' => $validated['mother_name'] ?? null,
-                'gender' => $validated['gender'] ?? null,
+                'name'            => $validated['name'],
+                'email'           => $validated['email'],
+                'password'        => $validated['password'], // Casts handles hashing if set up, but let's pass as validated password
+                'phone'           => $validated['phone'] ?? null,
+                'cpf'             => $validated['cpf'] ?? null,
+                'birth_date'      => $validated['birth_date'] ?? null,
+                'rg'              => $validated['rg'] ?? null,
+                'mother_name'     => $validated['mother_name'] ?? null,
+                'gender'          => $validated['gender'] ?? null,
                 'document_number' => $validated['document_number'] ?? null,
-                'photo_path' => $mainPhotoPath,
-                'photos' => $photoPaths,
-                'document_path' => $documentPath,
+                'photo_path'      => $mainPhotoPath,
+                'photos'          => $photoPaths,
+                'document_path'   => $documentPath,
             ]);
 
             $token = $user->createToken('auth_token')->plainTextToken;
@@ -88,26 +91,28 @@ public function register(Request $request)
             DB::commit();
 
             return response()->json([
-                'message' => 'Usuário cadastrado com sucesso!',
+                'message'      => 'Usuário cadastrado com sucesso!',
                 'access_token' => $token,
-                'token_type' => 'Bearer',
-                'user' => new UserResource($user)
+                'token_type'   => 'Bearer',
+                'user'         => new UserResource($user)
             ], 201);
 
         } catch (\Exception $e) {
             DB::rollBack();
-            \Log::error("Registration Error: " . $e->getMessage());
+            Log::error("Registration Error: " . $e->getMessage());
             return response()->json([
                 'message' => 'Erro ao realizar cadastro: ' . $e->getMessage()
             ], 500);
         }
     }
 
-    // 2. Login
-    public function login(Request $request)
+    /**
+     * Log in user.
+     */
+    public function login(Request $request): JsonResponse
     {
         $loginData = $request->validate([
-            'login' => 'required|string',
+            'login'    => 'required|string',
             'password' => 'required|string',
         ]);
 
@@ -121,46 +126,51 @@ public function login(Request $request)
         $token = $user->createToken('auth_token')->plainTextToken;
 
         return response()->json([
-            'message' => 'Login realizado com sucesso!',
+            'message'      => 'Login realizado com sucesso!',
             'access_token' => $token,
-            'token_type' => 'Bearer',
-            'user' => new UserResource($user)
+            'token_type'   => 'Bearer',
+            'user'         => new UserResource($user)
         ]);
     }
 
-    // 3. Meus Dados (Validado por Token)
-    public function me(Request $request)
+    /**
+     * Fetch current user profile.
+     */
+    public function me(Request $request): UserResource
     {
         return new UserResource($request->user());
     }
 
-    // 4. Logout
-    public function logout(Request $request)
+    /**
+     * Log out current user.
+     */
+    public function logout(Request $request): JsonResponse
     {
         $request->user()->currentAccessToken()->delete();
         return response()->json(['message' => 'Logout realizado com sucesso.']);
     }
 
-    // 5. Atualizar Perfil
-    public function update(Request $request)
+    /**
+     * Update user profile settings.
+     */
+    public function update(Request $request): JsonResponse
     {
         $user = $request->user();
 
         $validated = $request->validate([
-            'name' => 'sometimes|string|max:255',
-            'email' => 'sometimes|string|email|max:255|unique:users,email,' . $user->id,
-            'password' => 'sometimes|nullable|string|min:6',
-            'phone' => 'nullable|string',
-            'cpf' => 'nullable|string',
+            'name'         => 'sometimes|string|max:255',
+            'email'        => 'sometimes|string|email|max:255|unique:users,email,' . $user->id,
+            'password'     => 'sometimes|nullable|string|min:6',
+            'phone'        => 'nullable|string',
+            'cpf'          => 'nullable|string',
             'device_token' => 'nullable|string',
-            'photo' => 'nullable|image|max:4096', // Max 4MB
+            'photo'        => 'nullable|image|max:4096',
         ]);
 
         if ($request->hasFile('photo')) {
             $path = $request->file('photo')->store('players', 'public');
             $user->photo_path = $path;
 
-            // Sync photos array too
             $photos = $user->photos ?? [];
             $photos[0] = $path;
             $user->photos = $photos;
@@ -175,12 +185,14 @@ public function update(Request $request)
 
         return response()->json([
             'message' => 'Perfil atualizado com sucesso!',
-            'user' => new UserResource($user)
+            'user'    => new UserResource($user)
         ]);
     }
 
-    // 6. Esqueci Minha Senha (Solicitar Link)
-    public function forgotPassword(Request $request)
+    /**
+     * Request password reset token.
+     */
+    public function forgotPassword(Request $request): JsonResponse
     {
         $request->validate(['email' => 'required|email']);
 
@@ -190,37 +202,35 @@ public function forgotPassword(Request $request)
             return response()->json(['message' => 'Se este e-mail estiver cadastrado, você receberá instruções para resetar sua senha.'], 200);
         }
 
-        // Gera um token de 6 dígitos (mais amigável para mobile/app)
         $token = str_pad(random_int(0, 999999), 6, '0', STR_PAD_LEFT);
 
-        // Salva na tabela password_reset_tokens (padrão do Laravel)
         DB::table('password_reset_tokens')->updateOrInsert(
             ['email' => $user->email],
             [
-                'token' => Hash::make($token),
+                'token'      => Hash::make($token),
                 'created_at' => now()
             ]
         );
 
-        // Envia o e-mail
         try {
             $resetLink = "https://esportivo.techinteligente.site/reset-password?email=" . urlencode($user->email) . "&token=" . $token;
-
-            Mail::to($user->email)->send(new \App\Mail\PasswordResetMail($user, $token, $resetLink));
+            Mail::to($user->email)->send(new PasswordResetMail($user, $token, $resetLink));
         } catch (\Exception $e) {
-            \Log::error("Email Error: " . $e->getMessage());
+            Log::error("Email Error: " . $e->getMessage());
             return response()->json(['message' => 'Erro ao enviar e-mail. Tente novamente mais tarde.'], 500);
         }
 
         return response()->json(['message' => 'Código de recuperação enviado para seu e-mail.']);
     }
 
-    // 7. Resetar Senha com o Código
-    public function resetPassword(Request $request)
+    /**
+     * Reset password using validation token.
+     */
+    public function resetPassword(Request $request): JsonResponse
     {
         $request->validate([
-            'email' => 'required|email',
-            'token' => 'required|string|size:6',
+            'email'    => 'required|email',
+            'token'    => 'required|string|size:6',
             'password' => 'required|string|min:6|confirmed',
         ]);
 
@@ -230,52 +240,47 @@ public function resetPassword(Request $request)
             return response()->json(['message' => 'Código inválido ou expirado.'], 422);
         }
 
-        // Verifica expiração (60 min)
-        if (\Carbon\Carbon::parse($reset->created_at)->addMinutes(60)->isPast()) {
+        if (Carbon::parse($reset->created_at)->addMinutes(60)->isPast()) {
             return response()->json(['message' => 'Este código já expirou.'], 422);
         }
 
         $user = User::where('email', $request->email)->firstOrFail();
-        $user->password = $request->password; // O model já tem cast 'hashed'
+        $user->password = $request->password;
         $user->save();
 
-        // Remove o token usado
         DB::table('password_reset_tokens')->where('email', $request->email)->delete();
 
         return response()->json(['message' => 'Senha alterada com sucesso! Agora você pode fazer login.']);
     }
 
-    // 8. Excluir Conta (Anonimização para Google Play)
-    public function deleteAccount(Request $request)
+    /**
+     * Account Deletion (Google Play Compliance).
+     */
+    public function deleteAccount(Request $request): JsonResponse
     {
         $user = $request->user();
 
         try {
             DB::beginTransaction();
 
-            // 1. Logs de auditoria seriam bons aqui, mas vamos focar na anonimização
-            $oldEmail = $user->email;
             $anonymousEmail = "excluido_" . $user->id . "_" . bin2hex(random_bytes(4)) . "@esportivo.com.br";
 
-            // 2. Limpar dados sensíveis, mantendo apenas o nome para histórico
             $user->update([
-                'email' => $anonymousEmail,
-                'password' => Hash::make(bin2hex(random_bytes(16))), // Senha aleatória inacessível
-                'phone' => null,
-                'cpf' => null,
-                'birth_date' => null,
-                'rg' => null,
-                'mother_name' => null,
-                'gender' => null,
+                'email'           => $anonymousEmail,
+                'password'        => Hash::make(bin2hex(random_bytes(16))),
+                'phone'           => null,
+                'cpf'             => null,
+                'birth_date'      => null,
+                'rg'              => null,
+                'mother_name'     => null,
+                'gender'          => null,
                 'document_number' => null,
-                'photo_path' => null,
-                'photos' => null,
-                'document_path' => null,
-                'device_token' => null,
-                // Mantemos o 'name' conforme solicitado
+                'photo_path'      => null,
+                'photos'          => null,
+                'document_path'   => null,
+                'device_token'    => null,
             ]);
 
-            // 3. Revogar todos os tokens
             $user->tokens()->delete();
 
             DB::commit();
@@ -286,34 +291,30 @@ public function deleteAccount(Request $request)
 
         } catch (\Exception $e) {
             DB::rollBack();
-            \Log::error("Account Deletion Error: " . $e->getMessage());
+            Log::error("Account Deletion Error: " . $e->getMessage());
             return response()->json([
                 'message' => 'Erro ao processar exclusão de conta.'
             ], 500);
         }
     }
 
-    // 9. Solicitação Pública de Exclusão (Web)
-    public function requestDeletion(Request $request)
+    /**
+     * Deletion Request from external source (Web).
+     */
+    public function requestDeletion(Request $request): JsonResponse
     {
         $request->validate([
-            'identifier' => 'required|string', // Pode ser email ou CPF
-            'reason' => 'nullable|string|max:500',
+            'identifier' => 'required|string',
+            'reason'     => 'nullable|string|max:500',
         ]);
 
-        // Como é uma rota pública sem login, apenas registramos a intenção ou enviamos e-mail para o admin
-        // Para conformidade Google, ter um formulário que o admin recebe já é válido, 
-        // mas vamos tentar localizar o usuário e marcar algo se possível.
-
         $user = User::where('email', $request->identifier)
             ->orWhere('cpf', $request->identifier)
             ->first();
 
         if ($user) {
-            // Notificar admin (simulado via log ou e-mail se configurado)
-            \Log::info("Solicitação de exclusão de conta recebida via Web para: " . $request->identifier);
+            Log::info("Solicitação de exclusão de conta recebida via Web para: " . $request->identifier);
 
-            // Aqui poderíamos enviar um e-mail para o Felipe/Admin
             return response()->json([
                 'message' => 'Recebemos sua solicitação. Analisaremos os dados e processaremos a exclusão em até 48h úteis.'
             ]);
diff --git a/app/Http/Controllers/ClientPortalController.php b/app/Http/Controllers/ClientPortalController.php
index 41acf13..2ea57be 100644
--- a/app/Http/Controllers/ClientPortalController.php
+++ b/app/Http/Controllers/ClientPortalController.php
@@ -3,23 +3,20 @@
 namespace App\Http\Controllers;
 
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
 use Illuminate\Support\Facades\DB;
+use Symfony\Component\HttpFoundation\BinaryFileResponse;
 
 class ClientPortalController extends Controller
 {
     /**
-     * Retorna as estatísticas e inventário de dados consolidados para o portal seguro.
-     * Protegido por token de auditoria definido no .env do projeto.
+     * Consolidate metrics and statistics for client dashboard audit access.
      */
-    public function getAuditDashboard(Request $request)
+    public function getAuditDashboard(Request $request): JsonResponse
     {
-        // Recupera o token enviado no cabeçalho ou no corpo da requisição
         $token = $request->header('X-Audit-Token') ?? $request->input('access_token');
-        
-        // Recupera a chave mestre configurada no .env (ou usa o padrão para funcionamento imediato)
         $expectedToken = env('CLIENT_AUDIT_TOKEN', 'Felipe0110Auditoria');
 
-        // Valida se a funcionalidade está ativa e se a chave está correta
         if (!$expectedToken || $token !== $expectedToken) {
             return response()->json([
                 'success' => false,
@@ -28,7 +25,6 @@ public function getAuditDashboard(Request $request)
         }
 
         try {
-            // 1. Coleta de Métricas de Contagem Básica
             $metrics = [
                 'users'         => DB::table('users')->count(),
                 'clubs'         => DB::table('clubs')->count(),
@@ -41,14 +37,12 @@ public function getAuditDashboard(Request $request)
                 'paid_sales'    => DB::table('orders')->where('status', 'paid')->count(),
             ];
 
-            // 2. Últimos campeonatos registrados para exibição em grade
             $recentChampionships = DB::table('championships')
                 ->select('name', 'format', 'start_date', 'status')
                 ->orderBy('start_date', 'desc')
                 ->limit(8)
                 ->get();
 
-            // 3. Últimas compras na plataforma
             $recentOrders = DB::table('orders')
                 ->join('users', 'orders.user_id', '=', 'users.id')
                 ->select('users.name as cliente', 'orders.total_amount', 'orders.status', 'orders.created_at as data')
@@ -56,14 +50,13 @@ public function getAuditDashboard(Request $request)
                 ->limit(8)
                 ->get();
 
-            // Retorna o payload consolidado com sucesso
             return response()->json([
-                'success'      => true,
-                'timestamp'    => now()->format('Y-m-d H:i:s'),
-                'environment'  => 'Produção Protegida',
-                'metrics'      => $metrics,
-                'championships'=> $recentChampionships,
-                'orders'       => $recentOrders
+                'success'       => true,
+                'timestamp'     => now()->format('Y-m-d H:i:s'),
+                'environment'   => 'Produção Protegida',
+                'metrics'       => $metrics,
+                'championships' => $recentChampionships,
+                'orders'        => $recentOrders
             ]);
 
         } catch (\Exception $e) {
@@ -76,10 +69,9 @@ public function getAuditDashboard(Request $request)
     }
 
     /**
-     * Serve os arquivos binários de backup criptografados para o portal seguro.
-     * Valida o token e transmite o stream do arquivo mais recente.
+     * Download secure encrypted client backup package.
      */
-    public function downloadBackupFile(Request $request, $type)
+    public function downloadBackupFile(Request $request, string $type)
     {
         $token = $request->header('X-Audit-Token') ?? $request->input('access_token');
         $expectedToken = env('CLIENT_AUDIT_TOKEN', 'Felipe0110Auditoria');
@@ -91,7 +83,6 @@ public function downloadBackupFile(Request $request, $type)
             ], 401);
         }
 
-        // Mapeamento seguro de arquivos permitidos para download
         $map = [
             'database' => 'banco_latest.zip',
             'images'   => 'imagens_latest.zip',
@@ -108,7 +99,6 @@ public function downloadBackupFile(Request $request, $type)
         $fileName = $map[$type];
         $filePath = storage_path('app/backups_cliente/' . $fileName);
 
-        // Verifica a existência física do arquivo no disco de armazenamento protegido
         if (!file_exists($filePath)) {
             return response()->json([
                 'success' => false,
@@ -116,7 +106,6 @@ public function downloadBackupFile(Request $request, $type)
             ], 404);
         }
 
-        // Define o nome do arquivo para o download do cliente preservando extensão
         return response()->download($filePath, $fileName, [
             'Content-Type' => $type === 'pdf' ? 'application/pdf' : 'application/zip',
         ]);
diff --git a/app/Http/Controllers/CoreController.php b/app/Http/Controllers/CoreController.php
index 4b52d3e..9ac7895 100644
--- a/app/Http/Controllers/CoreController.php
+++ b/app/Http/Controllers/CoreController.php
@@ -3,42 +3,56 @@
 namespace App\Http\Controllers;
 
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
+use App\Models\City;
+use App\Models\Sport;
+use App\Models\Club;
 
 class CoreController extends Controller
 {
-    // 1. Listar todas as cidades disponíveis
-    public function cities()
+    /**
+     * List all cities that have active clubs.
+     */
+    public function cities(): JsonResponse
     {
-        return response()->json(\App\Models\City::whereHas('clubs', function ($q) {
+        $cities = City::whereHas('clubs', function ($q) {
             $q->where('is_active', true);
-        })->get());
+        })->get(['id', 'name', 'state', 'slug']);
+
+        return response()->json($cities);
     }
 
-    // 2. Listar clubes de uma cidade (Busca por Slug ou ID)
-    public function clubs($citySlug)
+    /**
+     * List active clubs within a specific city.
+     */
+    public function clubs(string $citySlug): JsonResponse
     {
-        $city = \App\Models\City::where('slug', $citySlug)->firstOrFail();
-        return response()->json($city->clubs()->where('is_active', true)->get());
+        $city = City::where('slug', $citySlug)->firstOrFail(['id', 'slug']);
+        $clubs = $city->clubs()->where('is_active', true)->get(['id', 'name', 'slug', 'logo_path', 'primary_color', 'secondary_color', 'city_id']);
+
+        return response()->json($clubs);
     }
 
-    // 3. Detalhes de um Clube Específico (incluindo cores e modalidades)
-    public function clubDetails($clubSlug)
+    /**
+     * Fetch detailed club information including its active sports.
+     */
+    public function clubDetails(string $clubSlug): JsonResponse
     {
-        $club = \App\Models\Club::where('slug', $clubSlug)->firstOrFail();
+        $club = Club::where('slug', $clubSlug)->firstOrFail();
 
-        // Buscar esportes que possuem campeonatos neste clube
         $sportIds = $club->championships()->select('sport_id')->distinct()->pluck('sport_id');
-        $activeSports = \App\Models\Sport::whereIn('id', $sportIds)->get();
+        $activeSports = Sport::whereIn('id', $sportIds)->get(['id', 'name', 'slug', 'category_type']);
 
-        // Adicionar atributo dinâmico
         $club->active_sports_list = $activeSports;
 
         return response()->json($club);
     }
 
-    // 4. Listar catálogo de esportes Global
-    public function sports()
+    /**
+     * List global sports catalog.
+     */
+    public function sports(): JsonResponse
     {
-        return response()->json(\App\Models\Sport::all());
+        return response()->json(Sport::all(['id', 'name', 'slug', 'category_type']));
     }
 }
diff --git a/app/Http/Controllers/EventController.php b/app/Http/Controllers/EventController.php
index 4001ad1..c625b40 100644
--- a/app/Http/Controllers/EventController.php
+++ b/app/Http/Controllers/EventController.php
@@ -3,15 +3,27 @@
 namespace App\Http\Controllers;
 
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
+use Illuminate\Http\Response as LaravelResponse;
 use App\Models\Championship;
 use App\Models\GameMatch;
 use App\Models\Team;
 use App\Models\Product;
+use App\Models\Race;
+use App\Models\RaceResult;
+use App\Models\CompetitorTime;
+use App\Models\Category;
+use App\Models\MatchEvent;
+use App\Models\User;
+use Illuminate\Support\Facades\DB;
+use Illuminate\Support\Facades\Log;
 
 class EventController extends Controller
 {
-    // 1. Listar Campeonatos de um Clube (Filtrado por Esporte)
-    public function championships(Request $request, $clubId)
+    /**
+     * List championships of a club (filtered by sport).
+     */
+    public function championships(Request $request, int $clubId): JsonResponse
     {
         $query = Championship::where('club_id', $clubId);
 
@@ -19,14 +31,21 @@ public function championships(Request $request, $clubId)
             $query->where('sport_id', $request->sport_id);
         }
 
-        return response()->json($query->with(['sport', 'club.city', 'races'])->orderBy('start_date', 'desc')->get());
+        $championships = $query->with(['sport:id,name,slug', 'club.city:id,name,state', 'races'])
+            ->orderBy('start_date', 'desc')
+            ->get();
+
+        return response()->json($championships);
     }
 
-    public function publicList(Request $request)
+    /**
+     * Public championship list with filters.
+     */
+    public function publicList(Request $request): JsonResponse
     {
         $this->syncStatuses();
 
-        $query = Championship::with(['club.city', 'sport', 'categories', 'races']);
+        $query = Championship::with(['club.city:id,name,state', 'sport:id,name,slug', 'categories:id,championship_id,name', 'races']);
 
         if ($request->has('status')) {
             $status = $request->status;
@@ -37,11 +56,9 @@ public function publicList(Request $request)
             } elseif ($status === 'finished') {
                 $query->where('status', 'finished');
             } elseif ($status === 'upcoming') {
-                // 'upcoming' and 'Agendado' (legacy/import) are future events
                 $query->whereIn('status', ['upcoming', 'scheduled', 'Agendado']);
             }
         } else {
-            // Default (Todos): Exclude only 'draft'
             $query->whereNotIn('status', ['draft']);
         }
 
@@ -58,15 +75,15 @@ public function publicList(Request $request)
         return response()->json($championships);
     }
 
-    // 2. Detalhes do Campeonato (com Categorias)
-    public function championshipDetails($id)
+    /**
+     * Championship details (with Categories).
+     */
+    public function championshipDetails(int $id): JsonResponse
     {
-        // Carrega o campeonato com TODAS as suas categorias (pai e filhas)
-        $champ = Championship::with(['categories', 'sport'])
+        $champ = Championship::with(['categories', 'sport:id,name,slug'])
             ->withCount('teams')
             ->findOrFail($id);
 
-        // Processa os produtos inclusos para todas as categorias
         $champ->categories->each(function ($category) {
             if ($category->included_products) {
                 $category->products_details = $category->products();
@@ -76,14 +93,13 @@ public function championshipDetails($id)
         return response()->json($champ);
     }
 
-
-
-
-    // 3. Tabela de Jogos (Partidas)
-    public function matches(Request $request, $championshipId)
+    /**
+     * Retrieve matches for a championship.
+     */
+    public function matches(Request $request, int $championshipId): JsonResponse
     {
         $query = GameMatch::where('championship_id', $championshipId)
-            ->with(['homeTeam', 'awayTeam', 'mvp'])
+            ->with(['homeTeam:id,name,logo_path', 'awayTeam:id,name,logo_path', 'mvp:id,name,nickname,photo_path'])
             ->orderBy('start_time');
 
         if ($request->has('category_id') && $request->category_id != 'null') {
@@ -93,23 +109,23 @@ public function matches(Request $request, $championshipId)
         return response()->json($query->get());
     }
 
-    // 4. Tabela de Classificação
-    public function leaderboard(Request $request, $championshipId)
+    /**
+     * Standings leaderboard logic.
+     */
+    public function leaderboard(Request $request, int $championshipId): JsonResponse
     {
         try {
-            $champ = Championship::with(['sport', 'teams'])->findOrFail($championshipId);
+            $champ = Championship::with(['sport:id,name,slug', 'teams:id,name,logo_path'])->findOrFail($championshipId);
             $sportSlug = $champ->sport ? $champ->sport->slug : 'futebol';
 
-            // Normalize for logic (Volei uses sets, others use goals)
             if (strpos($sportSlug, 'volei') !== false) {
                 $sportSlug = 'volei';
             }
 
             $query = GameMatch::where('championship_id', $championshipId)
-                ->with(['homeTeam', 'awayTeam'])
+                ->with(['homeTeam:id,name,logo_path', 'awayTeam:id,name,logo_path'])
                 ->where('status', 'finished');
 
-            // Excluir repescagem se não estiver configurado para contar na classificação
             if (!($champ->include_repescagem_standings ?? false)) {
                 $query->where(function($q) {
                     $q->where('round_name', '!=', 'Repescagem')
@@ -118,7 +134,6 @@ public function leaderboard(Request $request, $championshipId)
                 });
             }
 
-            // Excluir mata-mata se não estiver configurado para contar na classificação
             if (!($champ->include_knockout_standings ?? false)) {
                 $query->where(function($q) {
                     $q->where('is_knockout', '!=', true)
@@ -141,62 +156,61 @@ public function leaderboard(Request $request, $championshipId)
             $matches = $query->get();
             $teamsData = [];
 
-            // Helper para inicializar estrutura
             $initStats = function ($name, $logo = null) {
                 return [
-                    'name' => $name,
-                    'logo' => $logo,
-                    'stats' => [ //...
-                        'points' => 0,
-                        'played' => 0,
-                        'wins' => 0,
-                        'draws' => 0,
-                        'losses' => 0,
-                        'goals_for' => 0,
+                    'name'  => $name,
+                    'logo'  => $logo,
+                    'stats' => [
+                        'points'        => 0,
+                        'played'        => 0,
+                        'wins'          => 0,
+                        'draws'         => 0,
+                        'losses'        => 0,
+                        'goals_for'     => 0,
                         'goals_against' => 0,
-                        'sets_won' => 0,
-                        'sets_lost' => 0
+                        'sets_won'      => 0,
+                        'sets_lost'     => 0
                     ]
                 ];
             };
 
-            // 1. PRIMEIRA PASSADA: Inicializa todos os times com seus grupos REAIS (do pivot/cadastro)
             $teamsQuery = $champ->teams();
             if ($request->filled('category_id') && $request->category_id != 'null') {
                 $teamsQuery->wherePivot('category_id', $request->category_id);
             }
-            $allTeams = $teamsQuery->get();
+            $allTeams = $teamsQuery->get(['teams.id', 'teams.name', 'teams.logo_path']);
 
             $hasGroups = in_array($champ->format, ['groups', 'group_knockout']);
 
             foreach ($allTeams as $team) {
                 $rawG = $hasGroups ? ($team->pivot->group_name ?? 'Geral') : 'Geral';
                 $groupName = trim(str_ireplace('Grupo', '', $rawG));
-                if ($hasGroups && empty($groupName) && $rawG) $groupName = 'A';
+                if ($hasGroups && empty($groupName) && $rawG) {
+                    $groupName = 'A';
+                }
 
                 $teamsData[$team->id] = $initStats($team->name, $team->logo_url);
                 $teamsData[$team->id]['id'] = $team->id;
                 $teamsData[$team->id]['group_name'] = $groupName;
             }
 
-            // 2. SEGUNDA PASSADA: Processa os jogos e soma os pontos nos times já inicializados
             foreach ($matches as $m) {
                 $homeId = $m->home_team_id;
                 $awayId = $m->away_team_id;
-                if (!$homeId || !$awayId) continue;
+                if (!$homeId || !$awayId) {
+                    continue;
+                }
 
-                // Garante que o time existe no nosso mapa (caso tenha sido deletado ou algo assim)
-                if (!isset($teamsData[$homeId])) continue;
-                if (!isset($teamsData[$awayId])) continue;
+                if (!isset($teamsData[$homeId]) || !isset($teamsData[$awayId])) {
+                    continue;
+                }
 
-                // Lógica de Pontuação
                 $teamsData[$homeId]['stats']['played']++;
                 $teamsData[$awayId]['stats']['played']++;
 
                 $hScore = (int) $m->home_score;
                 $aScore = (int) $m->away_score;
 
-                // Vôlei
                 if ($sportSlug == 'volei') {
                     $teamsData[$homeId]['stats']['sets_won'] += $hScore;
                     $teamsData[$homeId]['stats']['sets_lost'] += $aScore;
@@ -219,7 +233,6 @@ public function leaderboard(Request $request, $championshipId)
                     $teamsData[$awayId]['stats']['goals_against'] = $teamsData[$awayId]['stats']['sets_lost'];
 
                 } elseif ($sportSlug == 'basquete') {
-                    // Basquete: Não tem empate. Saldo de pontos.
                     $teamsData[$homeId]['stats']['goals_for'] += $hScore;
                     $teamsData[$homeId]['stats']['goals_against'] += $aScore;
                     $teamsData[$awayId]['stats']['goals_for'] += $aScore;
@@ -228,14 +241,13 @@ public function leaderboard(Request $request, $championshipId)
                     if ($hScore > $aScore) {
                         $teamsData[$homeId]['stats']['wins']++;
                         $teamsData[$awayId]['stats']['losses']++;
-                        $teamsData[$homeId]['stats']['points'] += 2; // Basquete FIBA: 2 Vitória, 1 Derrota? Vamos usar 2/0 ou 2/1 se quiser ser fiel.
+                        $teamsData[$homeId]['stats']['points'] += 2;
                     } else {
                         $teamsData[$awayId]['stats']['wins']++;
                         $teamsData[$homeId]['stats']['losses']++;
                         $teamsData[$awayId]['stats']['points'] += 2;
                     }
                 } else {
-                    // Futebol / Futsal / Padrão
                     $teamsData[$homeId]['stats']['goals_for'] += $hScore;
                     $teamsData[$homeId]['stats']['goals_against'] += $aScore;
                     $teamsData[$awayId]['stats']['goals_for'] += $aScore;
@@ -258,17 +270,10 @@ public function leaderboard(Request $request, $championshipId)
                 }
             }
 
-            // O sorteio e loop de times já foi feito no início
-            // mantemos o loop original limpo apenas para garantir IDs se necessário 
-            // (mas foi movido para o topo para ser a fonte da verdade)
-
-            // Manual Tiebreaker Priority
             $priority = $champ->tiebreaker_priority ?? [];
             $priorityMap = array_flip($priority);
 
-            // Ordenação
             usort($teamsData, function ($a, $b) use ($priorityMap) {
-                // Sort by Group
                 $groupA = $a['group_name'] ?? 'Geral';
                 $groupB = $b['group_name'] ?? 'Geral';
                 if ($groupA !== $groupB) {
@@ -276,7 +281,6 @@ public function leaderboard(Request $request, $championshipId)
                 }
 
                 if ($a['stats']['points'] === $b['stats']['points']) {
-                    // Manual Tiebreaker Priority (Immediate override if available)
                     if (isset($priorityMap[$a['id'] ?? 0]) && isset($priorityMap[$b['id'] ?? 0])) {
                         if ($priorityMap[$a['id'] ?? 0] !== $priorityMap[$b['id'] ?? 0]) {
                             return $priorityMap[$a['id'] ?? 0] <=> $priorityMap[$b['id'] ?? 0];
@@ -284,14 +288,11 @@ public function leaderboard(Request $request, $championshipId)
                     }
 
                     if ($a['stats']['wins'] === $b['stats']['wins']) {
-                        // Saldo
                         $balA = $a['stats']['goals_for'] - $a['stats']['goals_against'];
                         $balB = $b['stats']['goals_for'] - $b['stats']['goals_against'];
 
                         if ($balA === $balB) {
-                            // Gols Pró (Opcional, mas comum)
                             if ($a['stats']['goals_for'] === $b['stats']['goals_for']) {
-                                // Ordem Alfabética as final fallback
                                 return strcmp($a['name'], $b['name']);
                             }
                             return $b['stats']['goals_for'] <=> $a['stats']['goals_for'];
@@ -304,43 +305,44 @@ public function leaderboard(Request $request, $championshipId)
                 return $b['stats']['points'] <=> $a['stats']['points'];
             });
 
-            // Adiciona ID e posição, e transforma para o formato esperado pelo frontend
             $position = 1;
             $formatted = [];
             foreach ($teamsData as $tid => &$t) {
-                if (!isset($t['id']))
+                if (!isset($t['id'])) {
                     $t['id'] = $tid;
+                }
 
-                // Transformar estrutura para frontend
                 $formatted[] = [
-                    'id' => $t['id'],
-                    'team_name' => $t['name'],
-                    'team_logo' => $t['logo'] ?? null,
-                    'position' => $position++,
-                    'points' => $t['stats']['points'],
-                    'played' => $t['stats']['played'],
-                    'won' => $t['stats']['wins'],
-                    'drawn' => $t['stats']['draws'],
-                    'lost' => $t['stats']['losses'],
+                    'id'              => $t['id'],
+                    'team_name'       => $t['name'],
+                    'team_logo'       => $t['logo'] ?? null,
+                    'position'        => $position++,
+                    'points'          => $t['stats']['points'],
+                    'played'          => $t['stats']['played'],
+                    'won'             => $t['stats']['wins'],
+                    'drawn'           => $t['stats']['draws'],
+                    'lost'            => $t['stats']['losses'],
                     'goal_difference' => $t['stats']['goals_for'] - $t['stats']['goals_against'],
-                    'goals_for' => $t['stats']['goals_for'],
-                    'goals_against' => $t['stats']['goals_against'],
-                    'group_name' => $t['group_name'] ?? 'Geral'
+                    'goals_for'       => $t['stats']['goals_for'],
+                    'goals_against'   => $t['stats']['goals_against'],
+                    'group_name'      => $t['group_name'] ?? 'Geral'
                 ];
             }
 
             return response()->json($formatted);
         } catch (\Exception $e) {
-            \Log::error("Leaderboard Error: " . $e->getMessage());
+            Log::error("Leaderboard Error: " . $e->getMessage());
             return response()->json(['message' => 'Erro ao carregar classificação'], 500);
         }
     }
 
-    // 4.5. Chaveamento Mata-Mata (Knockout Bracket)
-    public function knockoutBracket(Request $request, $championshipId)
+    /**
+     * Knockout bracket rendering data.
+     */
+    public function knockoutBracket(Request $request, int $championshipId): JsonResponse
     {
         $query = GameMatch::where('championship_id', $championshipId)
-            ->with(['homeTeam', 'awayTeam'])
+            ->with(['homeTeam:id,name,logo_path', 'awayTeam:id,name,logo_path'])
             ->whereNotNull('round_name')
             ->orderByRaw("CASE 
                 WHEN round_name LIKE '%16%' OR round_name LIKE '%oitavas%' THEN 1
@@ -355,22 +357,21 @@ public function knockoutBracket(Request $request, $championshipId)
 
         $matches = $query->get();
 
-        // Formatar para o frontend
         $formatted = $matches->map(function ($match) {
             return [
-                'id' => $match->id,
-                'team1_name' => $match->homeTeam->name ?? 'A definir',
-                'team2_name' => $match->awayTeam->name ?? 'A definir',
-                'team1_logo' => $match->homeTeam->logo_url ?? null,
-                'team2_logo' => $match->awayTeam->logo_url ?? null,
-                'team1_score' => $match->home_score,
-                'team2_score' => $match->away_score,
-                'team1_penalty' => $match->home_penalty_score,
-                'team2_penalty' => $match->away_penalty_score,
-                'round' => $match->round_name,
-                'match_date' => $match->start_time,
-                'location' => $match->location,
-                'status' => $match->status,
+                'id'             => $match->id,
+                'team1_name'     => $match->homeTeam->name ?? 'A definir',
+                'team2_name'     => $match->awayTeam->name ?? 'A definir',
+                'team1_logo'     => $match->homeTeam->logo_url ?? null,
+                'team2_logo'     => $match->awayTeam->logo_url ?? null,
+                'team1_score'    => $match->home_score,
+                'team2_score'    => $match->away_score,
+                'team1_penalty'  => $match->home_penalty_score,
+                'team2_penalty'  => $match->away_penalty_score,
+                'round'          => $match->round_name,
+                'match_date'     => $match->start_time,
+                'location'       => $match->location,
+                'status'         => $match->status,
                 'winner_team_id' => $match->home_score !== null && $match->away_score !== null
                     ? ($match->home_score > $match->away_score || ($match->home_score == $match->away_score && $match->home_penalty_score > $match->away_penalty_score) ? $match->home_team_id : $match->away_team_id)
                     : null
@@ -380,34 +381,37 @@ public function knockoutBracket(Request $request, $championshipId)
         return response()->json($formatted);
     }
 
-
-    // 5. Calendário
-    public function calendarEvents($clubId)
+    /**
+     * Get club matches calendar schedule list.
+     */
+    public function calendarEvents(int $clubId): JsonResponse
     {
         $events = [];
         $matches = GameMatch::whereHas('championship', function ($q) use ($clubId) {
             $q->where('club_id', $clubId);
-        })->with(['homeTeam', 'awayTeam'])->get();
+        })->with(['homeTeam:id,name', 'awayTeam:id,name'])->get(['id', 'home_team_id', 'away_team_id', 'start_time']);
 
         foreach ($matches as $match) {
             $events[] = [
-                'id' => 'M' . $match->id,
-                'type' => 'match',
+                'id'    => 'M' . $match->id,
+                'type'  => 'match',
                 'title' => ($match->homeTeam->name ?? '?') . ' x ' . ($match->awayTeam->name ?? '?'),
-                'date' => date('Y-m-d', strtotime($match->start_time)),
+                'date'  => date('Y-m-d', strtotime($match->start_time)),
                 'color' => '#009966'
             ];
         }
         return response()->json($events);
     }
 
-    // 6. Estatísticas (Artilharia / Pontuadores)
-    public function stats(Request $request, $championshipId)
+    /**
+     * Retrieve stats: Goals, Assists, Cards or Best Laps.
+     */
+    public function stats(Request $request, int $championshipId): JsonResponse
     {
         $type = $request->query('type', 'goals');
 
         if ($type === 'best_lap') {
-            $query = \App\Models\CompetitorTime::with(['user', 'team', 'gameMatch'])
+            $query = CompetitorTime::with(['user:id,name,nickname,photo_path', 'team:id,name,logo_path', 'gameMatch:id,round_name,round_number'])
                 ->where('championship_id', $championshipId)
                 ->where('status', 'completed')
                 ->whereHas('gameMatch', function ($q) {
@@ -457,13 +461,13 @@ public function stats(Request $request, $championshipId)
 
                     $bestTimes[$athleteKey] = [
                         'player_name' => $pName,
-                        'team_name' => $tName,
-                        'team_logo' => $time->team ? ($time->team->logo_url ?? $time->team->logo_path ?? null) : null,
-                        'value_raw' => $time->time_ms,
-                        'photo_url' => $photo,
-                        'value' => $formatted,
-                        'lap' => $time->lap ?? 1,
-                        'round_name' => $time->gameMatch ? ($time->gameMatch->round_name ?: "Bateria {$time->gameMatch->round_number}") : null
+                        'team_name'   => $tName,
+                        'team_logo'   => $time->team ? ($time->team->logo_url ?? $time->team->logo_path ?? null) : null,
+                        'value_raw'   => $time->time_ms,
+                        'photo_url'   => $photo,
+                        'value'       => $formatted,
+                        'lap'         => $time->lap ?? 1,
+                        'round_name'  => $time->gameMatch ? ($time->gameMatch->round_name ?: "Bateria {$time->gameMatch->round_number}") : null
                     ];
                 }
             }
@@ -477,9 +481,8 @@ public function stats(Request $request, $championshipId)
         }
 
         if ($type === 'defense') {
-            // Team-based stat: Least Conceded Goals (Defesa Menos Vazada)
-            $champ = \App\Models\Championship::with(['teams'])->findOrFail($championshipId);
-            $query = \App\Models\GameMatch::where('championship_id', $championshipId)
+            $champ = Championship::with(['teams:id,name,logo_path'])->findOrFail($championshipId);
+            $query = GameMatch::where('championship_id', $championshipId)
                 ->whereIn('status', ['finished', 'live', 'ongoing']);
 
             if ($request->filled('category_id') && $request->category_id != 'null') {
@@ -489,24 +492,23 @@ public function stats(Request $request, $championshipId)
             $matches = $query->get();
             $teamStats = [];
 
-            // Initialize teams from pivot to ensure all teams are shown even with 0 games
             $teamsQuery = $champ->teams();
             if ($request->filled('category_id') && $request->category_id != 'null') {
                 $teamsQuery->wherePivot('category_id', $request->category_id);
             }
-            $allTeams = $teamsQuery->get();
+            $allTeams = $teamsQuery->get(['teams.id', 'teams.name', 'teams.logo_path']);
 
             foreach ($allTeams as $team) {
                 $teamStats[$team->id] = [
-                    'id' => $team->id,
-                    'player_name' => $team->name, // Using player_name for frontend compatibility
-                    'team_name' => $team->name,
-                    'team_logo' => $team->logo_url,
-                    'photo_url' => $team->logo_url,
-                    'value' => 0,
+                    'id'             => $team->id,
+                    'player_name'    => $team->name,
+                    'team_name'      => $team->name,
+                    'team_logo'      => $team->logo_url,
+                    'photo_url'      => $team->logo_url,
+                    'value'          => 0,
                     'matches_played' => 0,
-                    'average' => 0,
-                    'details' => []
+                    'average'        => 0,
+                    'details'        => []
                 ];
             }
 
@@ -532,73 +534,52 @@ public function stats(Request $request, $championshipId)
 
             $sortedStats = array_values($teamStats);
             usort($sortedStats, function ($a, $b) {
-                // Primary: Least goals
                 if ($a['value'] !== $b['value']) {
                     return $a['value'] <=> $b['value'];
                 }
-                // Secondary: Most matches played (more impressive)
                 return $b['matches_played'] <=> $a['matches_played'];
             });
 
             return response()->json($sortedStats);
         }
 
-        // Map request type to DB event_type
         $dbTypes = [];
-        if ($type === 'goals')
+        if ($type === 'goals') {
             $dbTypes = ['goal'];
-        elseif ($type === 'yellow_cards')
+        } elseif ($type === 'yellow_cards') {
             $dbTypes = ['yellow_card'];
-        elseif ($type === 'red_cards')
+        } elseif ($type === 'red_cards') {
             $dbTypes = ['red_card'];
-        elseif ($type === 'points')
+        } elseif ($type === 'points') {
             $dbTypes = [
-                'point',
-                'block',
-                'ace',
-                'ataque',
-                'bloqueio',
-                'saque', // Volei/Outros
-                '1_point',
-                '2_points',
-                '3_points',
-                'free_throw',
-                'field_goal_2',
-                'field_goal_3', // Basquete
-                'jiu_jitsu_2',
-                'jiu_jitsu_3',
-                'jiu_jitsu_4',
-                'takedown',
-                'guard_pass',
-                'mount',
-                'back_control',
-                'knee_on_belly',
-                'sweep',
-                'advantage',
-                'penalty', // Lutas
-                'game_won' // Jogos puros
+                'point', 'block', 'ace', 'ataque', 'bloqueio', 'saque',
+                '1_point', '2_points', '3_points', 'free_throw',
+                'field_goal_2', 'field_goal_3', 'jiu_jitsu_2',
+                'jiu_jitsu_3', 'jiu_jitsu_4', 'takedown', 'guard_pass',
+                'mount', 'back_control', 'knee_on_belly', 'sweep',
+                'advantage', 'penalty', 'game_won'
             ];
-        elseif ($type === 'blocks')
+        } elseif ($type === 'blocks') {
             $dbTypes = ['block', 'bloqueio'];
-        elseif ($type === 'aces')
+        } elseif ($type === 'aces') {
             $dbTypes = ['ace', 'saque'];
-        elseif ($type === 'assists')
+        } elseif ($type === 'assists') {
             $dbTypes = ['assist'];
-        elseif ($type === 'rebounds')
+        } elseif ($type === 'rebounds') {
             $dbTypes = ['rebound'];
-        elseif ($type === '3_points')
+        } elseif ($type === '3_points') {
             $dbTypes = ['3_points', 'field_goal_3'];
-        elseif ($type === '2_points')
+        } elseif ($type === '2_points') {
             $dbTypes = ['2_points', 'field_goal_2'];
-        elseif ($type === 'blue_cards')
+        } elseif ($type === 'blue_cards') {
             $dbTypes = ['blue_card'];
+        }
 
         if (empty($dbTypes)) {
             return response()->json([]);
         }
 
-        // Query Events directly
-        $events = \App\Models\MatchEvent::whereIn('event_type', $dbTypes)
+        $events = MatchEvent::whereIn('event_type', $dbTypes)
             ->whereHas('gameMatch', function ($q) use ($championshipId, $request, $type) {
                 $q->where('championship_id', $championshipId)
                     ->whereIn('status', ['finished', 'live', 'ongoing']);
@@ -607,8 +588,7 @@ public function stats(Request $request, $championshipId)
                     $q->where('category_id', $request->category_id);
                 }
 
-                // Check championship settings for repescagem
-                $championship = \App\Models\Championship::find($championshipId);
+                $championship = Championship::find($championshipId);
                 $includeRepescagem = false;
                 $includeKnockout = false;
                 if ($type === 'goals' || $type === 'assists') {
@@ -641,23 +621,18 @@ public function stats(Request $request, $championshipId)
                     });
                 }
             })
-            ->with(['team', 'player', 'gameMatch.homeTeam', 'gameMatch.awayTeam']) // Load team and player
+            ->with(['team:id,name,logo_path', 'player:id,name,nickname,photo_path', 'gameMatch.homeTeam:id,name', 'gameMatch.awayTeam:id,name'])
             ->get();
 
         $playerStats = [];
 
-        // 1. Processar Eventos do Banco (da tabela match_events)
         foreach ($events as $event) {
             $metadata = is_string($event->metadata) ? json_decode($event->metadata, true) : (array) ($event->metadata ?? []);
-
-            // Prefer player relation name, fallback to metadata name, fallback to Desconhecido
-            // Since ImportOldData put 'player_id' as null often but 'original_player_name' in metadata
             $pName = $metadata['original_player_name'] ?? ($metadata['player_name'] ?? 'Desconhecido');
             $pPhoto = null;
 
             if ($event->player) {
                 $pName = $event->player->nickname ?: $event->player->name;
-                // Try to get photo if available
                 $pPhoto = $event->player->photo_url ?? $event->player->photo ?? null;
             }
 
@@ -666,48 +641,44 @@ public function stats(Request $request, $championshipId)
             if (!isset($playerStats[$pName])) {
                 $playerStats[$pName] = [
                     'player_name' => $pName,
-                    'team_name' => $teamName,
-                    'team_logo' => $event->team->logo_url ?? null,
-                    'value' => 0,
-                    'photo_url' => $pPhoto,
-                    'details' => []
+                    'team_name'   => $teamName,
+                    'team_logo'   => $event->team->logo_url ?? null,
+                    'value'       => 0,
+                    'photo_url'   => $pPhoto,
+                    'details'     => []
                 ];
             }
 
-            // Skip own goals for scorer stats
             if ($type === 'goals' && isset($metadata['own_goal']) && filter_var($metadata['own_goal'], FILTER_VALIDATE_BOOLEAN)) {
                 continue;
             }
 
             $playerStats[$pName]['value'] += $event->value;
 
-            // Add detail
             if ($event->gameMatch) {
                 $home = $event->gameMatch->homeTeam?->name ?? 'TBA';
                 $away = $event->gameMatch->awayTeam?->name ?? 'TBA';
                 $playerStats[$pName]['details'][] = [
-                    'match_id' => $event->gameMatch->id,
+                    'match_id'    => $event->gameMatch->id,
                     'match_label' => "$home vs $away",
-                    'game_time' => $event->game_time,
-                    'period' => $event->period ?? ($metadata['period'] ?? ($metadata['label'] ?? null)),
-                    'match_date' => $event->gameMatch->start_time,
-                    'round' => $event->gameMatch->round_name ?? $event->gameMatch->round_number ?? null,
-                    'phase' => $event->gameMatch->phase ?? null,
+                    'game_time'   => $event->game_time,
+                    'period'      => $event->period ?? ($metadata['period'] ?? ($metadata['label'] ?? null)),
+                    'match_date'  => $event->gameMatch->start_time,
+                    'round'       => $event->gameMatch->round_name ?? $event->gameMatch->round_number ?? null,
+                    'phase'       => $event->gameMatch->phase ?? null,
                 ];
             }
         }
 
-        // 2. Processar Eventos da Súmula (salvos em JSON no game_matches.match_details['events'])
-        $matchesWithJson = \App\Models\GameMatch::where('championship_id', $championshipId)
+        $matchesWithJson = GameMatch::where('championship_id', $championshipId)
             ->whereIn('status', ['finished', 'live', 'ongoing'])
-            ->with(['homeTeam', 'awayTeam']);
+            ->with(['homeTeam:id,name,logo_path', 'awayTeam:id,name,logo_path']);
 
         if ($request->filled('category_id') && $request->category_id != 'null') {
             $matchesWithJson->where('category_id', $request->category_id);
         }
 
-        // Get championship for settings
-        $championship = \App\Models\Championship::find($championshipId);
+        $championship = Championship::find($championshipId);
         $includeRepescagem = false;
         $includeKnockout = false;
         if ($type === 'goals' || $type === 'assists') {
@@ -740,19 +711,20 @@ public function stats(Request $request, $championshipId)
             foreach ($details['events'] as $jsonEvent) {
                 $eventType = $jsonEvent['type'] ?? 'unknown';
 
-                // Normaliza tipos de vôlei do JSON para bater com os tipos procurados no banco
-                if ($eventType === 'ataque')
+                if ($eventType === 'ataque') {
                     $eventType = 'point';
-                if ($eventType === 'bloqueio')
+                }
+                if ($eventType === 'bloqueio') {
                     $eventType = 'block';
-                if ($eventType === 'saque')
+                }
+                if ($eventType === 'saque') {
                     $eventType = 'ace';
+                }
 
                 if (!in_array($eventType, $dbTypes)) {
                     continue;
                 }
 
-                // O valor em JSON pra basquete por exemplo vem como 'points', default 1
                 $value = isset($jsonEvent['points']) ? (int) $jsonEvent['points'] : (isset($jsonEvent['value']) ? (int) $jsonEvent['value'] : 1);
 
                 $pName = $jsonEvent['player_name'] ?? 'Desconhecido';
@@ -760,7 +732,7 @@ public function stats(Request $request, $championshipId)
                 $teamId = $jsonEvent['team_id'] ?? null;
 
                 if (isset($jsonEvent['player_id']) && $jsonEvent['player_id']) {
-                    $player = \App\Models\User::find($jsonEvent['player_id']);
+                    $player = User::find($jsonEvent['player_id']);
                     if ($player) {
                         $pName = $player->name;
                         $pPhoto = $player->photo_url ?? $player->photo ?? null;
@@ -775,8 +747,8 @@ public function stats(Request $request, $championshipId)
                 } elseif ($teamId == $match->away_team_id) {
                     $teamName = $match->awayTeam->name ?? 'Time Desconhecido';
                     $teamLogo = $match->awayTeam->logo_url ?? null;
-                } else if ($teamId) {
-                    $team = \App\Models\Team::find($teamId);
+                } elseif ($teamId) {
+                    $team = Team::find($teamId);
                     if ($team) {
                         $teamName = $team->name;
                         $teamLogo = $team->logo_url ?? null;
@@ -786,11 +758,11 @@ public function stats(Request $request, $championshipId)
                 if (!isset($playerStats[$pName])) {
                     $playerStats[$pName] = [
                         'player_name' => $pName,
-                        'team_name' => $teamName,
-                        'team_logo' => $teamLogo,
-                        'value' => 0,
-                        'photo_url' => $pPhoto,
-                        'details' => []
+                        'team_name'   => $teamName,
+                        'team_logo'   => $teamLogo,
+                        'value'       => 0,
+                        'photo_url'   => $pPhoto,
+                        'details'     => []
                     ];
                 }
 
@@ -803,13 +775,13 @@ public function stats(Request $request, $championshipId)
                 $home = $match->homeTeam->name ?? 'TBA';
                 $away = $match->awayTeam->name ?? 'TBA';
                 $playerStats[$pName]['details'][] = [
-                    'match_id' => $match->id,
+                    'match_id'    => $match->id,
                     'match_label' => "$home vs $away",
-                    'game_time' => $jsonEvent['minute'] ?? ($jsonEvent['game_time'] ?? '00:00'),
-                    'period' => $jsonEvent['period'] ?? null,
-                    'match_date' => $match->start_time,
-                    'round' => $match->round_name ?? $match->round_number ?? null,
-                    'phase' => $match->phase ?? null,
+                    'game_time'   => $jsonEvent['minute'] ?? ($jsonEvent['game_time'] ?? '00:00'),
+                    'period'      => $jsonEvent['period'] ?? null,
+                    'match_date'  => $match->start_time,
+                    'round'       => $match->round_name ?? $match->round_number ?? null,
+                    'phase'       => $match->phase ?? null,
                 ];
             }
         }
@@ -821,22 +793,26 @@ public function stats(Request $request, $championshipId)
         return response()->json(array_values($playerStats));
     }
 
-    public function heats($championshipId)
+    public function heats(int $championshipId): JsonResponse
     {
         return response()->json([]);
     }
-    public function brackets($championshipId)
+
+    public function brackets(int $championshipId): JsonResponse
     {
         return response()->json([]);
     }
-    public function participants($championshipId)
+
+    /**
+     * Retrieve all participants in a tournament.
+     */
+    public function participants(int $championshipId): JsonResponse
     {
-        $championship = \App\Models\Championship::findOrFail($championshipId);
+        $championship = Championship::findOrFail($championshipId);
         $participants = [];
 
         if ($championship->registration_type === 'team') {
             try {
-                // Try to filter players by championship_id OR null (global team players)
                 $teams = $championship->teams()->with([
                     'players' => function($q) use ($championshipId) {
                         $q->where(function($sq) use ($championshipId) {
@@ -846,16 +822,14 @@ public function participants($championshipId)
                     }
                 ])->get();
             } catch (\Throwable $e) {
-                // Fallback: If migration hasn't been run on server yet, load all rostered players
-                \Log::warning("Erro ao carregar jogadores com championship_id, usando fallback: " . $e->getMessage());
+                Log::warning("Erro ao carregar jogadores com championship_id, usando fallback: " . $e->getMessage());
                 $teams = $championship->teams()->with('players')->get();
             }
 
             foreach ($teams as $team) {
                 $players = $team->players;
-                // If the filtered query returned no players, load all team roster as fallback
                 if ($players->isEmpty()) {
-                    $fullTeam = \App\Models\Team::with('players')->find($team->id);
+                    $fullTeam = Team::with('players')->find($team->id);
                     if ($fullTeam) {
                         $players = $fullTeam->players;
                     }
@@ -863,28 +837,28 @@ public function participants($championshipId)
 
                 foreach ($players as $player) {
                     $participants[] = [
-                        'user_id' => $player->id,
-                        'name' => $player->name,
-                        'team_id' => $team->id,
-                        'team' => ['name' => $team->name],
+                        'user_id'     => $player->id,
+                        'name'        => $player->name,
+                        'team_id'     => $team->id,
+                        'team'        => ['name' => $team->name],
                         'category_id' => $team->pivot->category_id ?? null,
                     ];
                 }
             }
         } else {
-            $race = \App\Models\Race::where('championship_id', $championshipId)->first();
+            $race = Race::where('championship_id', $championshipId)->first();
             if ($race) {
-                $results = \App\Models\RaceResult::with(['user', 'category'])->where('race_id', $race->id)->get();
+                $results = RaceResult::with(['user:id,name,photo_path', 'category:id,name'])->where('race_id', $race->id)->get();
                 foreach ($results as $result) {
                     if ($result->user) {
                         $participants[] = [
-                            'user_id' => $result->user->id,
-                            'name' => $result->name ?: $result->user->name,
-                            'bib_number' => $result->bib_number,
-                            'team_id' => null,
-                            'team' => null,
+                            'user_id'     => $result->user->id,
+                            'name'        => $result->name ?: $result->user->name,
+                            'bib_number'  => $result->bib_number,
+                            'team_id'     => null,
+                            'team'        => null,
                             'category_id' => $result->category_id,
-                            'category' => $result->category ? ['name' => $result->category->name] : null,
+                            'category'    => $result->category ? ['name' => $result->category->name] : null,
                         ];
                     }
                 }
@@ -893,19 +867,27 @@ public function participants($championshipId)
 
         return response()->json($participants);
     }
-    public function raceDetails($championshipId)
+
+    /**
+     * Get race details for individual championships.
+     */
+    public function raceDetails(int $championshipId): JsonResponse
     {
-        $race = \App\Models\Race::where('championship_id', $championshipId)->first();
+        $race = Race::where('championship_id', $championshipId)->first();
         return response()->json($race);
     }
-    public function raceResults(Request $request, $championshipId)
+
+    /**
+     * Retrieve individual race results list.
+     */
+    public function raceResults(Request $request, int $championshipId): JsonResponse
     {
-        $race = \App\Models\Race::where('championship_id', $championshipId)->first();
+        $race = Race::where('championship_id', $championshipId)->first();
         if (!$race) {
             return response()->json([]);
         }
 
-        $query = \App\Models\RaceResult::where('race_id', $race->id)->with(['category', 'user']);
+        $query = RaceResult::where('race_id', $race->id)->with(['category:id,name', 'user:id,name,photo_path']);
 
         if ($request->has('category')) {
             $query->where('category_id', $request->category);
@@ -919,15 +901,18 @@ public function raceResults(Request $request, $championshipId)
             });
         }
 
-        return response()->json(
-            $query->orderBy('lap', 'desc')
-                ->orderBy('net_time', 'asc')
-                ->orderBy('position_general', 'asc')
-                ->get()
-        );
+        $results = $query->orderBy('lap', 'desc')
+            ->orderBy('net_time', 'asc')
+            ->orderBy('position_general', 'asc')
+            ->get();
+
+        return response()->json($results);
     }
-    // 7. MVP (Most Valuable Player)
-    public function mvp(Request $request, $championshipId)
+
+    /**
+     * Most Valuable Player list aggregation.
+     */
+    public function mvp(Request $request, int $championshipId): JsonResponse
     {
         $query = GameMatch::where('championship_id', $championshipId)
             ->whereNotNull('mvp_player_id')
@@ -937,16 +922,17 @@ public function mvp(Request $request, $championshipId)
             $query->where('category_id', $request->category_id);
         }
 
-        $matches = $query->with(['mvp', 'homeTeam', 'awayTeam'])->get();
+        $matches = $query->with(['mvp:id,name,nickname,photo_path', 'homeTeam:id,name', 'awayTeam:id,name'])->get();
 
         $mvpCounts = [];
         foreach ($matches as $match) {
             $pid = $match->mvp_player_id;
-            if (!$pid || !$match->mvp)
+            if (!$pid || !$match->mvp) {
                 continue;
+            }
 
             if (!isset($mvpCounts[$pid])) {
-                $team = \DB::table('team_players')
+                $team = DB::table('team_players')
                     ->join('teams', 'teams.id', '=', 'team_players.team_id')
                     ->where('team_players.user_id', $pid)
                     ->where('team_players.championship_id', $championshipId)
@@ -954,12 +940,12 @@ public function mvp(Request $request, $championshipId)
                     ->first();
 
                 $mvpCounts[$pid] = [
-                    'player' => $match->mvp,
+                    'player'      => $match->mvp,
                     'player_name' => $match->mvp->nickname ?: $match->mvp->name,
-                    'team_name' => $team->name ?? 'Time não informado',
-                    'photo_url' => $match->mvp->photo_url ?? $match->mvp->photo ?? null,
-                    'count' => 0,
-                    'details' => []
+                    'team_name'   => $team->name ?? 'Time não informado',
+                    'photo_url'   => $match->mvp->photo_url ?? $match->mvp->photo ?? null,
+                    'count'       => 0,
+                    'details'     => []
                 ];
             }
             $mvpCounts[$pid]['count']++;
@@ -967,11 +953,11 @@ public function mvp(Request $request, $championshipId)
             $home = $match->homeTeam?->name ?? 'TBA';
             $away = $match->awayTeam?->name ?? 'TBA';
             $mvpCounts[$pid]['details'][] = [
-                'match_id' => $match->id,
+                'match_id'    => $match->id,
                 'match_label' => "$home vs $away",
-                'match_date' => $match->start_time,
-                'round' => $match->round_name ?? $match->round_number ?? null,
-                'phase' => $match->phase ?? null,
+                'match_date'  => $match->start_time,
+                'round'       => $match->round_name ?? $match->round_number ?? null,
+                'phase'       => $match->phase ?? null,
             ];
         }
 
@@ -982,8 +968,10 @@ public function mvp(Request $request, $championshipId)
         return response()->json(array_values($mvpCounts));
     }
 
-    // 8. Teams List
-    public function teamsList(Request $request, $championshipId)
+    /**
+     * Get registered teams.
+     */
+    public function teamsList(Request $request, int $championshipId): JsonResponse
     {
         $championship = Championship::findOrFail($championshipId);
 
@@ -1014,15 +1002,17 @@ public function teamsList(Request $request, $championshipId)
         return response()->json($teams);
     }
 
-    // 9. Head to Head
-    public function h2h(Request $request, $championshipId)
+    /**
+     * Head-to-head match history between two teams.
+     */
+    public function h2h(Request $request, int $championshipId): JsonResponse
     {
         $team1 = $request->query('team1');
         $team2 = $request->query('team2');
 
         $query = GameMatch::where('championship_id', $championshipId)
             ->where('status', 'finished')
-            ->with(['homeTeam', 'awayTeam'])
+            ->with(['homeTeam:id,name,logo_path', 'awayTeam:id,name,logo_path'])
             ->orderBy('start_time', 'desc');
 
         if ($request->has('category_id') && $request->category_id != 'null') {
@@ -1042,10 +1032,7 @@ public function h2h(Request $request, $championshipId)
                 $q->where('home_team_id', $team1)->orWhere('away_team_id', $team1);
             });
         }
-        // If no teams selected, maybe return latest matches? Or empty. 
-        // Let's return all if no filter, or user handles it.
-        // Usually H2H implies selection. If optional, we return empty if not enough params? 
-        // Let's return empty if no team1 at least, to avoid dumping all history
+
         if (!$team1 && !$team2) {
             return response()->json([]);
         }
@@ -1053,20 +1040,21 @@ public function h2h(Request $request, $championshipId)
         return response()->json($query->get());
     }
 
-    // 10. Agenda (Public with Filters)
-    public function agenda(Request $request, $clubId)
+    /**
+     * Get agenda schedule.
+     */
+    public function agenda(Request $request, int $clubId): JsonResponse
     {
         $statusMap = [
-            'agendado' => ['scheduled', 'upcoming', 'registrations_open', 'active'],
+            'agendado'  => ['scheduled', 'upcoming', 'registrations_open', 'active'],
             'concluido' => ['finished', 'completed'],
-            'ao_vivo' => ['live', 'ongoing', 'in_progress']
+            'ao_vivo'   => ['live', 'ongoing', 'in_progress']
         ];
 
         $query = GameMatch::whereHas('championship', function ($q) use ($clubId) {
             $q->where('club_id', $clubId);
-        })->with(['homeTeam', 'awayTeam', 'championship']);
+        })->with(['homeTeam:id,name', 'awayTeam:id,name', 'championship:id,name']);
 
-        // Date Filter
         if ($request->has('start_date') && $request->start_date) {
             $query->whereDate('start_time', '>=', $request->start_date);
         }
@@ -1074,7 +1062,6 @@ public function agenda(Request $request, $clubId)
             $query->whereDate('start_time', '<=', $request->end_date);
         }
 
-        // Status Filter
         if ($request->has('status') && isset($statusMap[$request->status])) {
             $query->whereIn('status', $statusMap[$request->status]);
         }
@@ -1083,27 +1070,30 @@ public function agenda(Request $request, $clubId)
 
         $events = $matches->map(function ($match) {
             return [
-                'id' => $match->id,
-                'title' => ($match->homeTeam->name ?? 'TBA') . ' vs ' . ($match->awayTeam->name ?? 'TBA'),
-                'date' => $match->start_time ? $match->start_time->format('Y-m-d H:i:s') : null,
-                'time' => $match->start_time ? $match->start_time->format('H:i') : null,
-                'location' => $match->location ?? 'Local não definido',
-                'category' => $match->championship->name ?? 'Campeonato',
-                'status' => $match->status,
-                'home_team' => $match->homeTeam->name ?? 'TBA',
-                'away_team' => $match->awayTeam->name ?? 'TBA',
-                'home_score' => $match->home_score,
-                'away_score' => $match->away_score,
+                'id'              => $match->id,
+                'title'           => ($match->homeTeam->name ?? 'TBA') . ' vs ' . ($match->awayTeam->name ?? 'TBA'),
+                'date'            => $match->start_time ? $match->start_time->format('Y-m-d H:i:s') : null,
+                'time'            => $match->start_time ? $match->start_time->format('H:i') : null,
+                'location'        => $match->location ?? 'Local não definido',
+                'category'        => $match->championship->name ?? 'Campeonato',
+                'status'          => $match->status,
+                'home_team'       => $match->homeTeam->name ?? 'TBA',
+                'away_team'       => $match->awayTeam->name ?? 'TBA',
+                'home_score'      => $match->home_score,
+                'away_score'      => $match->away_score,
                 'championship_id' => $match->championship_id
             ];
         });
 
         return response()->json($events);
     }
-    // 11. Detalhes da Partida (Público - para Modal Ao Vivo/Súmula)
-    public function matchDetails($id)
+
+    /**
+     * Retrieve detailed match statistics for modals.
+     */
+    public function matchDetails(int $id): JsonResponse
     {
-        $match = GameMatch::with(['championship.sport', 'events.player', 'mvp', 'pernaDePau'])->findOrFail($id);
+        $match = GameMatch::with(['championship.sport', 'events.player', 'mvp:id,name,nickname,photo_path', 'pernaDePau:id,name,nickname,photo_path'])->findOrFail($id);
 
         $champId = $match->championship_id;
         $match->load([
@@ -1113,9 +1103,9 @@ public function matchDetails($id)
             'awayTeam.players' => function ($q) use ($champId) {
                 $q->where('team_players.championship_id', $champId);
             },
-            'competitorTimes.user',
-            'competitorTimes.team',
-            'competitorTimes.category'
+            'competitorTimes.user:id,name,nickname,photo_path',
+            'competitorTimes.team:id,name,logo_path',
+            'competitorTimes.category:id,name'
         ]);
 
         $details = $match->match_details ?? [];
@@ -1124,35 +1114,31 @@ public function matchDetails($id)
             $details['events'] = [];
         }
 
-        // Se houver eventos na tabela MatchEvent, eles são a fonte de verdade
-        // Tipos internos de auditoria nunca devem aparecer na página pública
         $auditTypes = ['system_error', 'user_action', 'user_action_blocked', 'timer_control', 'voice_input'];
 
         $eventLabels = [
-            'goal' => ['label' => 'Gol', 'icon' => '?'],
-            'yellow_card' => ['label' => 'Cartão Amarelo', 'icon' => '??'],
-            'red_card' => ['label' => 'Cartão Vermelho', 'icon' => '??'],
-            'blue_card' => ['label' => 'Cartão Azul', 'icon' => '??'],
-            'foul' => ['label' => 'Falta', 'icon' => '??'],
-            'assist' => ['label' => 'Assistência', 'icon' => '??'],
-            'mvp' => ['label' => 'Craque do Jogo', 'icon' => '?'],
-            'timeout' => ['label' => 'Tempo Técnico', 'icon' => '??'],
+            'goal'          => ['label' => 'Gol', 'icon' => '?'],
+            'yellow_card'   => ['label' => 'Cartão Amarelo', 'icon' => '??'],
+            'red_card'      => ['label' => 'Cartão Vermelho', 'icon' => '??'],
+            'blue_card'     => ['label' => 'Cartão Azul', 'icon' => '??'],
+            'foul'          => ['label' => 'Falta', 'icon' => '??'],
+            'assist'        => ['label' => 'Assistência', 'icon' => '??'],
+            'mvp'           => ['label' => 'Craque do Jogo', 'icon' => '?'],
+            'timeout'       => ['label' => 'Tempo Técnico', 'icon' => '??'],
             'shootout_goal' => ['label' => 'Pênalti (Gol)', 'icon' => '?'],
             'shootout_miss' => ['label' => 'Pênalti (Erro)', 'icon' => '?'],
-            'penalty_goal' => ['label' => 'Pênalti (Gol)', 'icon' => '?'],
-            'match_start' => ['label' => 'Início da Partida', 'icon' => '??'],
-            'match_end' => ['label' => 'Fim de Jogo', 'icon' => '??'],
-            'period_start' => ['label' => 'Início de Período', 'icon' => '??'],
-            'period_end' => ['label' => 'Fim de Período', 'icon' => '??'],
-            // Vôlei / Beach Tennis
-            'ataque' => ['label' => 'Ataque', 'icon' => '??'],
-            'bloqueio' => ['label' => 'Bloqueio', 'icon' => '??'],
-            'saque' => ['label' => 'Ace (Saque)', 'icon' => '??'],
-            'erro' => ['label' => 'Erro', 'icon' => '?'],
-            // Tênis
-            'game' => ['label' => 'Game', 'icon' => '??'],
-            'set' => ['label' => 'Set', 'icon' => '??'],
-            'point' => ['label' => 'Ponto', 'icon' => '??'],
+            'penalty_goal'  => ['label' => 'Pênalti (Gol)', 'icon' => '?'],
+            'match_start'   => ['label' => 'Início da Partida', 'icon' => '??'],
+            'match_end'     => ['label' => 'Fim de Jogo', 'icon' => '??'],
+            'period_start'  => ['label' => 'Início de Período', 'icon' => '??'],
+            'period_end'    => ['label' => 'Fim de Período', 'icon' => '??'],
+            'ataque'        => ['label' => 'Ataque', 'icon' => '??'],
+            'bloqueio'      => ['label' => 'Bloqueio', 'icon' => '??'],
+            'saque'         => ['label' => 'Ace (Saque)', 'icon' => '??'],
+            'erro'          => ['label' => 'Erro', 'icon' => '?'],
+            'game'          => ['label' => 'Game', 'icon' => '??'],
+            'set'           => ['label' => 'Set', 'icon' => '??'],
+            'point'         => ['label' => 'Ponto', 'icon' => '??'],
         ];
 
         if ($match->events->count() > 0) {
@@ -1169,16 +1155,13 @@ public function matchDetails($id)
                         $label = 'Gol Contra';
                     }
 
-                    // Resolve Player Name
                     $pName = null;
                     if ($e->player) {
                         $pName = $e->player->nickname ?: $e->player->name;
                     } else {
-                        // Fallback to metadata
                         $pName = $metadata['player_name'] ?? ($metadata['original_player_name'] ?? null);
                     }
 
-                    // Final fallback if still null: check if it's a team event or just "Equipe"
                     if (!$pName) {
                         if ($e->team_id) {
                             $team = ($e->team_id == $match->home_team_id) ? $match->homeTeam : $match->awayTeam;
@@ -1189,83 +1172,83 @@ public function matchDetails($id)
                     }
 
                     return [
-                        'id' => $e->id,
-                        'type' => $e->event_type,
-                        'label' => $label,
-                        'icon' => $info['icon'],
-                        'team_id' => $e->team_id,
-                        'player_id' => $e->player_id,
+                        'id'          => $e->id,
+                        'type'        => $e->event_type,
+                        'label'       => $label,
+                        'icon'        => $info['icon'],
+                        'team_id'     => $e->team_id,
+                        'player_id'   => $e->player_id,
                         'player_name' => $pName,
-                        'minute' => $e->game_time ?? '00:00',
-                        'period' => $e->period ?? '1º Tempo',
-                        'value' => $e->value,
-                        'metadata' => $metadata,
+                        'minute'      => $e->game_time ?? '00:00',
+                        'period'      => $e->period ?? '1º Tempo',
+                        'value'       => $e->value,
+                        'metadata'    => $metadata,
                     ];
                 })->values();
             $details['events'] = $tableEvents;
         }
 
-        // Garante que o cronômetro sincronizado vá para o frontend
         if ($match->match_details && isset($match->match_details['sync_timer'])) {
             $details['sync_timer'] = $match->match_details['sync_timer'];
         }
 
-        if (!isset($details['sets']))
+        if (!isset($details['sets'])) {
             $details['sets'] = [];
-        if (!isset($details['positions']))
+        }
+        if (!isset($details['positions'])) {
             $details['positions'] = [];
+        }
 
-        // Carregar jogadores reais dos times
         $rosters = [
             'home' => $this->formatRoster($match->homeTeam),
             'away' => $this->formatRoster($match->awayTeam),
         ];
 
-        // Calcular votação do MVP
         $mvpVotesSummary = [
-            'public' => null,
+            'public'  => null,
             'mesario' => null,
             'arbitro' => null,
         ];
 
         foreach (['public', 'mesario', 'arbitro'] as $type) {
-            $mostVoted = \Illuminate\Support\Facades\DB::table('mvp_votes')
+            $mostVoted = DB::table('mvp_votes')
                 ->where('game_match_id', $match->id)
                 ->where('voter_type', $type)
-                ->select('voted_player_id', \Illuminate\Support\Facades\DB::raw('count(*) as votes_count'))
+                ->select('voted_player_id', DB::raw('count(*) as votes_count'))
                 ->groupBy('voted_player_id')
                 ->orderByDesc('votes_count')
                 ->first();
 
             if ($mostVoted) {
-                $player = \App\Models\User::find($mostVoted->voted_player_id);
+                $player = User::find($mostVoted->voted_player_id);
                 if ($player) {
                     $mvpVotesSummary[$type] = [
-                        'player_id' => $player->id,
-                        'name' => $player->nickname ?: $player->name,
+                        'player_id'   => $player->id,
+                        'name'        => $player->nickname ?: $player->name,
                         'votes_count' => $mostVoted->votes_count,
-                        'photo_url' => $player->photo_url ?? ($player->photo ?? null),
+                        'photo_url'   => $player->photo_url ?? ($player->photo ?? null),
                     ];
                 }
             }
         }
 
         return response()->json([
-            'match' => $match,
-            'details' => $details,
-            'rosters' => $rosters,
+            'match'             => $match,
+            'details'           => $details,
+            'rosters'           => $rosters,
             'mvp_votes_summary' => $mvpVotesSummary,
-            'server_time' => now()->timestamp * 1000, // milisegundos
-            'sport' => $match->championship->sport->slug ?? 'football'
+            'server_time'       => now()->timestamp * 1000,
+            'sport'             => $match->championship->sport->slug ?? 'football'
         ]);
     }
 
-    // 12. Sincronizar Statuses com base nas datas (Automático)
-    protected function syncStatuses()
+    /**
+     * Sincronizar Statuses.
+     */
+    protected function syncStatuses(): void
     {
         try {
             $now = now();
-            // Apenas campeonatos que estão marcados para atualização automática e não são rascunhos 'draft'
             $championships = Championship::where('is_status_auto', true)
                 ->where('status', '!=', 'draft')
                 ->get();
@@ -1273,61 +1256,56 @@ protected function syncStatuses()
             foreach ($championships as $champ) {
                 $newStatus = $champ->status;
 
-                // 1. Verificar se terminou (End Date)
                 if ($champ->end_date && $now->isAfter($champ->end_date->endOfDay())) {
                     $newStatus = 'finished';
-                }
-                // 2. Verificar se está em andamento (Start Date ou fim das inscrições)
-                else if (
+                } elseif (
                     ($champ->start_date && $now->isAfter($champ->start_date->startOfDay())) ||
                     ($champ->registration_end_date && $now->isAfter($champ->registration_end_date->endOfDay()))
                 ) {
-                    // Se já não estiver finalizado, passa para em andamento
                     $newStatus = 'ongoing';
-                }
-                // 3. Verificar se as inscrições abriram (Registration Start)
-                else if ($champ->registration_start_date && $now->isAfter($champ->registration_start_date)) {
-                    // Se as inscrições começaram e ainda não é data de jogo, abre inscrições
+                } elseif ($champ->registration_start_date && $now->isAfter($champ->registration_start_date)) {
                     $newStatus = 'registrations_open';
                 }
 
-                // Se o status mudou, salva mantendo o is_status_auto como true (pois foi auto)
                 if ($newStatus !== $champ->status) {
                     Championship::where('id', $champ->id)->update(['status' => $newStatus]);
                 }
             }
         } catch (\Exception $e) {
-            // Silenciosamente falha se a coluna não existir (migration não rodada)
-            \Log::warning("Erro ao sincronizar status: " . $e->getMessage());
+            Log::warning("Erro ao sincronizar status: " . $e->getMessage());
         }
     }
 
-    // Helper para formatar elenco
-    private function formatRoster($team)
+    /**
+     * Format roster simple helper.
+     */
+    private function formatRoster(?Team $team): array
     {
-        if (!$team)
+        if (!$team) {
             return [];
+        }
 
         return $team->players->map(function ($player) {
             return [
-                'id' => $player->id,
-                'number' => $player->pivot->number ?? '',
-                'name' => $player->name,
+                'id'       => $player->id,
+                'number'   => $player->pivot->number ?? '',
+                'name'     => $player->name,
                 'position' => $player->pivot->position
             ];
-        });
+        })->toArray();
     }
 
-    public function times(Request $request, $championshipId)
+    /**
+     * Retrieve competitor times list.
+     */
+    public function times(Request $request, int $championshipId): JsonResponse
     {
-        $query = \App\Models\CompetitorTime::with(['user', 'team', 'category', 'gameMatch'])
+        $query = CompetitorTime::with(['user:id,name,nickname,photo_path', 'team:id,name,logo_path', 'category:id,name', 'gameMatch:id,round_name,round_number'])
             ->where('championship_id', $championshipId);
 
         if ($request->has('game_match_id') && $request->game_match_id !== 'null' && $request->game_match_id !== '') {
             $query->where('game_match_id', $request->game_match_id);
         } else {
-            // Ao obter tempos gerais para a classificação/resultados:
-            // incluir tempos avulsos (game_match_id = null) E tempos de baterias normais (excluindo qualificatórias/eliminatórias)
             $query->where(function ($mainQ) {
                 $mainQ->whereNull('game_match_id')
                     ->orWhereHas('gameMatch', function ($q) {
@@ -1348,7 +1326,10 @@ public function times(Request $request, $championshipId)
         return response()->json($query->get());
     }
 
-    public function matchPdf($id)
+    /**
+     * Render match HTML summary.
+     */
+    public function matchPdf(int $id): LaravelResponse
     {
         $match = GameMatch::with(['homeTeam.players', 'awayTeam.players', 'championship.sport', 'events.player', 'mvp'])->findOrFail($id);
 
diff --git a/app/Http/Controllers/MatchOperationController.php b/app/Http/Controllers/MatchOperationController.php
index 413da2d..8c8e356 100644
--- a/app/Http/Controllers/MatchOperationController.php
+++ b/app/Http/Controllers/MatchOperationController.php
@@ -3,6 +3,7 @@
 namespace App\Http\Controllers;
 
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
 use App\Models\GameMatch;
 use App\Models\Team;
 use App\Models\User;
@@ -11,13 +12,14 @@
 
 class MatchOperationController extends Controller
 {
-    // RETORNA DADOS COMPLETOS PARA A SÚMULA (Players, histórico, sets, etc)
-    public function show($id)
+    /**
+     * Retrieve complete details for a match sheet (Súmula).
+     */
+    public function show(int $id): JsonResponse
     {
         $match = GameMatch::findOrFail($id);
-
-        // 1. CARREGAR JOGADORES FILTRADOS POR CAMPEONATO (Evitar vazamento de outros campeonatos)
         $champId = $match->championship_id;
+
         $match->load([
             'homeTeam.players' => function ($q) use ($champId) {
                 $q->where('team_players.championship_id', $champId);
@@ -33,23 +35,24 @@ public function show($id)
 
         $details = $match->match_details ?? [];
 
-        // 2. BUSCAR POSIÇÕES (Para Participação)
         $positions = DB::table('match_positions')
             ->where('game_match_id', $match->id)
             ->get();
 
-        // 3. CALCULAR HORÁRIOS DOS SETS (Via Events created_at)
         $setTimes = [];
         foreach ($match->events as $event) {
             $p = $event->period;
-            if (!$p) continue;
+            if (!$p) {
+                continue;
+            }
             
-            // Normalize period to set number if possible (e.g. "1º Set" -> 1)
             $setNum = null;
             if (preg_match('/(\d+)/', $p, $matches)) {
                 $setNum = (int)$matches[1];
             }
-            if (!$setNum) continue;
+            if (!$setNum) {
+                continue;
+            }
 
             if (!isset($setTimes[$setNum])) {
                 $setTimes[$setNum] = ['start' => null, 'end' => null];
@@ -57,16 +60,11 @@ public function show($id)
 
             $createdAt = $event->created_at;
             
-            // Se for início de período, é o start definitivo
             if ($event->event_type === 'period_start' || $event->event_type === 'match_start') {
                 $setTimes[$setNum]['start'] = $createdAt;
-            } 
-            // Se for fim de período, é o end definitivo
-            elseif ($event->event_type === 'period_end' || $event->event_type === 'match_end') {
+            } elseif ($event->event_type === 'period_end' || $event->event_type === 'match_end') {
                 $setTimes[$setNum]['end'] = $createdAt;
-            }
-            // Fallback: Earliest and Latest
-            else {
+            } else {
                 if (!$setTimes[$setNum]['start'] || $createdAt->lt($setTimes[$setNum]['start'])) {
                     $setTimes[$setNum]['start'] = $createdAt;
                 }
@@ -80,49 +78,45 @@ public function show($id)
             $details['events'] = [];
         }
 
-        // Otimização: Cache de números de camisa do campeonato atual
         $rosterNumbers = DB::table('team_players')
             ->where('championship_id', $champId)
             ->get()
             ->groupBy(['team_id', 'user_id']);
 
-        // Se houver eventos na tabela MatchEvent, eles são a fonte de verdade
-        // Tipos internos de auditoria nunca devem aparecer na página pública
         $auditTypes = ['system_error', 'user_action', 'user_action_blocked', 'timer_control', 'voice_input'];
 
         $eventLabels = [
-            'goal' => ['label' => 'Gol', 'icon' => '?'],
-            'yellow_card' => ['label' => 'Cartão Amarelo', 'icon' => '??'],
-            'red_card' => ['label' => 'Cartão Vermelho', 'icon' => '??'],
-            'blue_card' => ['label' => 'Cartão Azul', 'icon' => '??'],
-            'foul' => ['label' => 'Falta', 'icon' => '??'],
-            'assist' => ['label' => 'Assistência', 'icon' => '??'],
-            'mvp' => ['label' => 'Craque do Jogo', 'icon' => '?'],
-            'timeout' => ['label' => 'Tempo Técnico', 'icon' => '??'],
+            'goal'          => ['label' => 'Gol', 'icon' => '?'],
+            'yellow_card'   => ['label' => 'Cartão Amarelo', 'icon' => '??'],
+            'red_card'      => ['label' => 'Cartão Vermelho', 'icon' => '??'],
+            'blue_card'     => ['label' => 'Cartão Azul', 'icon' => '??'],
+            'foul'          => ['label' => 'Falta', 'icon' => '??'],
+            'assist'        => ['label' => 'Assistência', 'icon' => '??'],
+            'mvp'           => ['label' => 'Craque do Jogo', 'icon' => '?'],
+            'timeout'       => ['label' => 'Tempo Técnico', 'icon' => '??'],
             'shootout_goal' => ['label' => 'Pênalti (Gol)', 'icon' => '?'],
             'shootout_miss' => ['label' => 'Pênalti (Erro)', 'icon' => '?'],
-            'penalty_goal' => ['label' => 'Pênalti (Gol)', 'icon' => '?'],
-            'match_start' => ['label' => 'Início da Partida', 'icon' => '??'],
-            'match_end' => ['label' => 'Fim de Jogo', 'icon' => '??'],
-            'period_start' => ['label' => 'Início de Período', 'icon' => '??'],
-            'period_end' => ['label' => 'Fim de Período', 'icon' => '??'],
-            'point' => ['label' => 'Ponto', 'icon' => '??'],
-            'ace' => ['label' => 'Ponto de Saque (Ace)', 'icon' => '??'],
-            'block' => ['label' => 'Ponto de Bloqueio', 'icon' => '??'],
-            'ataque' => ['label' => 'Ponto de Ataque', 'icon' => '??'],
-            'bloqueio' => ['label' => 'Ponto de Bloqueio', 'icon' => '??'],
-            'saque' => ['label' => 'Ponto de Saque (Ace)', 'icon' => '??'],
+            'penalty_goal'  => ['label' => 'Pênalti (Gol)', 'icon' => '?'],
+            'match_start'   => ['label' => 'Início da Partida', 'icon' => '??'],
+            'match_end'     => ['label' => 'Fim de Jogo', 'icon' => '??'],
+            'period_start'  => ['label' => 'Início de Período', 'icon' => '??'],
+            'period_end'    => ['label' => 'Fim de Período', 'icon' => '??'],
+            'point'         => ['label' => 'Ponto', 'icon' => '??'],
+            'ace'           => ['label' => 'Ponto de Saque (Ace)', 'icon' => '??'],
+            'block'         => ['label' => 'Ponto de Bloqueio', 'icon' => '??'],
+            'ataque'        => ['label' => 'Ponto de Ataque', 'icon' => '??'],
+            'bloqueio'      => ['label' => 'Ponto de Bloqueio', 'icon' => '??'],
+            'saque'         => ['label' => 'Ponto de Saque (Ace)', 'icon' => '??'],
         ];
 
         if ($match->events->count() > 0) {
             $tableEvents = $match->events
-                ->sortByDesc('id') // O mais recente primeiro
+                ->sortByDesc('id')
                 ->filter(fn($e) => !in_array($e->event_type, $auditTypes))
                 ->map(function ($e) use ($eventLabels, $match, $rosterNumbers) {
                     $isVolley = ($match->championship->sport->slug ?? '') === 'volei';
                     $info = $eventLabels[$e->event_type] ?? ['label' => ucfirst(str_replace('_', ' ', $e->event_type)), 'icon' => '??'];
 
-                    // Ajuste de ícone dinâmico baseado no esporte
                     $icon = $info['icon'] ?? '??';
                     $metadata = is_string($e->metadata) ? json_decode($e->metadata, true) : $e->metadata;
                     if ($isVolley && in_array($e->event_type, ['point', 'ace', 'ataque', 'saque', 'block', 'bloqueio'])) {
@@ -130,10 +124,10 @@ public function show($id)
                         if (isset($metadata['volley_type'])) {
                             $vType = $metadata['volley_type'];
                             $labelMap = [
-                                'ataque' => 'Ponto de Ataque',
+                                'ataque'   => 'Ponto de Ataque',
                                 'bloqueio' => 'Ponto de Bloqueio',
-                                'saque' => 'Ponto de Saque (Ace)',
-                                'erro' => 'Erro Adversário',
+                                'saque'    => 'Ponto de Saque (Ace)',
+                                'erro'     => 'Erro Adversário',
                             ];
                             $info['label'] = $metadata['label'] ?? $labelMap[$vType] ?? 'Ponto';
                         }
@@ -147,16 +141,14 @@ public function show($id)
                         $number = $rosterNumbers[$e->team_id][$player->id][0]->number ?? null;
                     }
 
-                    // Resolve Player Name
                     $pName = null;
                     if ($player) {
                         $pName = $player->nickname ?: $player->name;
                     } elseif (!empty($metadata['player_name'])) {
                         $pName = $metadata['player_name'];
                     } elseif (!empty($metadata['label']) && preg_match('/\((.*?)\)/', $metadata['label'], $matches)) {
-                        $pName = $matches[1]; // Tenta extrair de "Ponto (Nome)"
+                        $pName = $matches[1];
                     } else {
-                        // For team level events or missing records, use team name if possible
                         if ($e->team_id) {
                             $team = ($e->team_id == $match->home_team_id) ? $match->homeTeam : $match->awayTeam;
                             $pName = $team->name ?? 'Equipe';
@@ -166,56 +158,54 @@ public function show($id)
                     }
 
                     return [
-                        'id' => $e->id,
-                        'type' => $e->event_type,
-                        'label' => $info['label'],
-                        'icon' => $icon,
-                        'team_id' => $e->team_id,
-                        'player_id' => $e->player_id,
-                        'player_name' => $pName,
+                        'id'            => $e->id,
+                        'type'          => $e->event_type,
+                        'label'         => $info['label'],
+                        'icon'          => $icon,
+                        'team_id'       => $e->team_id,
+                        'player_id'     => $e->player_id,
+                        'player_name'   => $pName,
                         'player_number' => $number,
-                        'minute' => $e->game_time ?? '00:00',
-                        'period' => $e->period ?? ($isVolley ? '1º Set' : '1º Tempo'),
-                        'value' => $e->value,
-                        'metadata' => $metadata,
+                        'minute'        => $e->game_time ?? '00:00',
+                        'period'        => $e->period ?? ($isVolley ? '1º Set' : '1º Tempo'),
+                        'value'         => $e->value,
+                        'metadata'      => $metadata,
                     ];
                 })->values();
             $details['events'] = $tableEvents;
         }
+
         if (!isset($details['sets']) || empty($details['sets'])) {
             $sportSlug = $match->championship->sport->slug ?? '';
             $isVolley = $sportSlug === 'volei';
             
-            // For all sports, if we have setTimes, we can populate a 'sets' structure for the frontend
             if ($isVolley || count($setTimes) > 0) {
-                // Se for vôlei, usamos os MatchSet reais se existirem
                 if ($isVolley && $match->sets->count() > 0) {
                     $details['sets'] = $match->sets
                         ->sortBy('set_number')
                         ->map(function ($s) use ($setTimes) {
                             $st = $setTimes[$s->set_number] ?? null;
                             return [
-                                'id' => $s->id,
+                                'id'         => $s->id,
                                 'set_number' => $s->set_number,
                                 'home_score' => $s->home_score,
                                 'away_score' => $s->away_score,
-                                'status' => $s->status,
+                                'status'     => $s->status,
                                 'start_time' => $s->start_time ?: ($st['start'] ?? null),
-                                'end_time' => $s->end_time ?: ($st['end'] ?? null),
+                                'end_time'   => $s->end_time ?: ($st['end'] ?? null),
                             ];
                         })->toArray();
                 } else {
-                    // Para outros esportes ou vôlei sem match_sets, usamos os tempos descobertos nos eventos
                     $details['sets'] = [];
                     $maxSet = count($setTimes) > 0 ? max(array_keys($setTimes)) : ($isVolley ? 3 : 2);
                     for ($i = 1; $i <= $maxSet; $i++) {
                         $st = $setTimes[$i] ?? null;
                         $details['sets'][] = [
                             'set_number' => $i,
-                            'home_score' => null, // Placar por período opcional aqui
+                            'home_score' => null,
                             'away_score' => null,
                             'start_time' => $st['start'] ?? null,
-                            'end_time' => $st['end'] ?? null,
+                            'end_time'   => $st['end'] ?? null,
                         ];
                     }
                 }
@@ -223,7 +213,6 @@ public function show($id)
                 $details['sets'] = [];
             }
         } else {
-            // Se já existirem sets no JSON (legacy), tentamos injetar os horários se faltarem
             foreach ($details['sets'] as &$s) {
                 $num = $s['set_number'] ?? null;
                 if ($num && empty($s['start_time'])) {
@@ -235,10 +224,6 @@ public function show($id)
             }
         }
 
-        // 4. CALCULAR PARTICIPAÇÃO (Sets que cada jogador jogou)
-        // Um jogador participou se:
-        // - Estava em match_positions para aquele set
-        // - Foi citado em evento de substitution naquele set (como player_in ou player_out)
         $participationMap = [];
         foreach ($positions as $pos) {
             $pId = $pos->player_id;
@@ -259,23 +244,26 @@ public function show($id)
                     $setNum = (int)$m[1];
                 }
 
-                if ($pIn) $participationMap[$pIn][$setNum] = true;
-                if ($pOut) $participationMap[$pOut][$setNum] = true;
+                if ($pIn) {
+                    $participationMap[$pIn][$setNum] = true;
+                }
+                if ($pOut) {
+                    $participationMap[$pOut][$setNum] = true;
+                }
             }
         }
 
-        if (!isset($details['positions']))
+        if (!isset($details['positions'])) {
             $details['positions'] = $positions;
+        }
 
-        // Carregar jogadores reais dos times (Rosters já carregados e filtrados no topo)
         $rosters = [
             'home' => $this->formatRoster($match->homeTeam, $participationMap),
             'away' => $this->formatRoster($match->awayTeam, $participationMap),
         ];
 
-        // Calcular votação do MVP
         $mvpVotesSummary = [
-            'public' => null,
+            'public'  => null,
             'mesario' => null,
             'arbitro' => null,
         ];
@@ -293,81 +281,83 @@ public function show($id)
                 $player = User::find($mostVoted->voted_player_id);
                 if ($player) {
                     $mvpVotesSummary[$type] = [
-                        'player_id' => $player->id,
-                        'name' => $player->nickname ?: $player->name,
+                        'player_id'   => $player->id,
+                        'name'        => $player->nickname ?: $player->name,
                         'votes_count' => $mostVoted->votes_count,
-                        'photo_url' => $player->photo_url ?? ($player->photo ?? null),
+                        'photo_url'   => $player->photo_url ?? ($player->photo ?? null),
                     ];
                 }
             }
         }
 
         return response()->json([
-            'match' => $match,
-            'details' => $details,
-            'rosters' => $rosters,
+            'match'             => $match,
+            'details'           => $details,
+            'rosters'           => $rosters,
             'mvp_votes_summary' => $mvpVotesSummary,
-            'server_time' => now()->timestamp * 1000,
-            'sport' => $match->championship->sport->slug ?? 'football'
+            'server_time'       => now()->timestamp * 1000,
+            'sport'             => $match->championship->sport->slug ?? 'football'
         ]);
     }
 
-    // LISTA DE JOGADORES (ROSTER) REAL
-    private function formatRoster($team, $participationMap = [])
+    /**
+     * Helper to format team rosters.
+     */
+    private function formatRoster(?Team $team, array $participationMap = []): array
     {
-        if (!$team)
+        if (!$team) {
             return [];
+        }
 
         return $team->players->map(function ($player) use ($participationMap) {
             $pId = $player->id;
             $participatedSets = isset($participationMap[$pId]) ? array_keys($participationMap[$pId]) : [];
             
             return [
-                'id' => $player->id,
-                'number' => $player->pivot->number ?? '',
-                'name' => $player->name,
-                'nickname' => $player->nickname,
-                'position' => $player->pivot->position,
+                'id'                => $player->id,
+                'number'            => $player->pivot->number ?? '',
+                'name'              => $player->name,
+                'nickname'          => $player->nickname,
+                'position'          => $player->pivot->position,
                 'participated_sets' => $participatedSets
             ];
-        })->sortBy('name', SORT_NATURAL | SORT_FLAG_CASE)->values();
+        })->sortBy('name', SORT_NATURAL | SORT_FLAG_CASE)->values()->toArray();
     }
 
-    // LANÇAR EVENTO (GOL, PONTO, CARTÃO)
-    public function addEvent(Request $request, $id)
+    /**
+     * Add a match event (e.g. goal, point, card).
+     */
+    public function addEvent(Request $request, int $id): JsonResponse
     {
         $match = GameMatch::findOrFail($id);
         $details = $match->match_details ?? [];
-        if (!isset($details['events']))
+        if (!isset($details['events'])) {
             $details['events'] = [];
+        }
 
-        $type = $request->input('type'); // goal, point, card, set_end
+        $type = $request->input('type');
         $teamId = $request->input('team_id');
-        $playerPoints = $request->input('points', 1); // 1, 2, 3 (basquete)
+        $playerPoints = (int)$request->input('points', 1);
 
-        // Adiciona evento
         $newEvent = [
-            'id' => uniqid(),
-            'type' => $type,
-            'team_id' => $teamId,
+            'id'          => uniqid(),
+            'type'        => $type,
+            'team_id'     => $teamId,
             'player_name' => $request->input('player_name', 'Desconhecido'),
-            'minute' => $request->input('minute', '00:00'),
-            'period' => $request->input('period', '1º Tempo'),
-            'created_at' => now()->toIso8601String()
+            'minute'      => $request->input('minute', '00:00'),
+            'period'      => $request->input('period', '1º Tempo'),
+            'created_at'  => now()->toIso8601String()
         ];
 
-        // Atualiza Placar Geral
         if ($type === 'goal' || $type === 'point' || strpos($type, 'pt') !== false) {
             if ($teamId == $match->home_team_id) {
                 $match->home_score += $playerPoints;
 
-                // Vôlei/Sets: Atualiza placar do set corrente também
                 if (isset($details['current_set_score'])) {
                     $details['current_set_score']['home'] += $playerPoints;
                 } else {
                     $details['current_set_score'] = ['home' => $playerPoints, 'away' => 0];
                 }
-
             } else {
                 $match->away_score += $playerPoints;
 
@@ -379,11 +369,10 @@ public function addEvent(Request $request, $id)
             }
         }
 
-        // Push event
-        array_unshift($details['events'], $newEvent); // O mais recente primeiro
+        array_unshift($details['events'], $newEvent);
 
         $match->match_details = $details;
-        $match->status = 'live'; // Garante que está ao vivo
+        $match->status = 'live';
         $match->save();
 
         $champId = $match->championship_id;
@@ -398,46 +387,36 @@ public function addEvent(Request $request, $id)
 
         return response()->json([
             'success' => true,
-            'match' => $match, // Retorna objeto atualizado com rosters filtrados
-            'event' => $newEvent
+            'match'   => $match,
+            'event'   => $newEvent
         ]);
     }
 
-    // CONTROLE DE SETS (Início/Fim)
-    public function updateSet(Request $request, $id)
+    /**
+     * Update match set information.
+     */
+    public function updateSet(Request $request, int $id): JsonResponse
     {
         $match = GameMatch::findOrFail($id);
         $details = $match->match_details ?? [];
 
-        $action = $request->input('action'); // start_set, end_set
+        $action = $request->input('action');
 
         if ($action === 'end_set') {
-            // Salva o set atual no histórico
             $currentScore = $details['current_set_score'] ?? ['home' => 0, 'away' => 0];
             $details['sets'][] = [
                 'label' => $request->input('label', count($details['sets']) + 1 . 'º Set'),
-                'home' => $currentScore['home'],
-                'away' => $currentScore['away']
+                'home'  => $currentScore['home'],
+                'away'  => $currentScore['away']
             ];
-            // Reseta placar do set
             $details['current_set_score'] = ['home' => 0, 'away' => 0];
 
-            // Incrementa placar "Geral" de sets na tabela principal?
-            // No Vôlei, home_score geralmente é Sets Vencidos.
-            // Minha lógica no addEvent estava somando pontos corridos. Vamos ajustar.
-            // Se for Vôlei, home_score guarda SETS. Se for Basquete, PONTOS.
-            // Complexidade do Polimorfismo. Vamos assumir que home_score = Placar Principal que define vencedor.
-            // No Vôlei, ao fechar set, incrementamos o home_score.
-
             if ($match->championship->sport && $match->championship->sport->slug === 'volei') {
                 if ($currentScore['home'] > $currentScore['away']) {
                     $match->home_score++;
                 } else {
                     $match->away_score++;
                 }
-                // Resetamos os pontos "corridos" de addEvent? Não, addEvent no volei soma 'points' internos.
-                // Precisamos separar: placar_principal (sets) vs placar_set (pontos).
-                // O frontend deve saber exibir.
             }
         }
 
@@ -457,11 +436,13 @@ public function updateSet(Request $request, $id)
         return response()->json(['success' => true, 'match' => $match]);
     }
 
-    // ATUALIZAR STATUS (Timer, Intervalo, Fim de Jogo)
-    public function updateStatus(Request $request, $id)
+    /**
+     * Update current match live status.
+     */
+    public function updateStatus(Request $request, int $id): JsonResponse
     {
         $match = GameMatch::findOrFail($id);
-        $status = $request->input('status'); // live, warmpup, finished, paused
+        $status = $request->input('status');
 
         if ($status) {
             $match->status = $status;
diff --git a/app/Http/Controllers/SecurityController.php b/app/Http/Controllers/SecurityController.php
index fa2abd7..e94a2ed 100644
--- a/app/Http/Controllers/SecurityController.php
+++ b/app/Http/Controllers/SecurityController.php
@@ -3,54 +3,49 @@
 namespace App\Http\Controllers;
 
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
 use App\Models\User;
-use App\Models\Team;
 
 class SecurityController extends Controller
 {
-    // Valida o status de um jogador pelo QR Code (CPF ou ID criptografado)
-    public function validatePlayer(Request $request, $code)
+    /**
+     * Validate player status via QR Code or CPF string.
+     */
+    public function validatePlayer(Request $request, string $code): JsonResponse
     {
-        // code pode ser o ID do usuario direto
-        $user = User::with(['club', 'teams'])->find($code);
+        $user = User::with(['club:id,name', 'teams:id,name'])->find($code);
 
         if (!$user) {
             return response()->json([
-                'status' => 'invalid',
+                'status'  => 'invalid',
                 'message' => 'Jogador não encontrado na base de dados.'
             ], 404);
         }
 
-        // Validações de Negócio
-        // 1. Está com mensalidade em dia? (Mock)
         $isFinancialOk = true;
-
-        // 2. Está suspenso de jogos anteriores? (Mock)
         $isSuspended = false;
-
-        // 3. Documentação está aprovada? (Assumindo que OCR já rodou e aprovou)
         $isDocsOk = $user->profile_verified_at != null;
 
         if ($isSuspended) {
             return response()->json([
-                'status' => 'blocked',
+                'status'  => 'blocked',
                 'message' => 'Jogador Suspenso (Cartão Vermelho pendente).',
-                'player' => $user
+                'player'  => $user
             ]);
         }
 
         if (!$isFinancialOk) {
             return response()->json([
-                'status' => 'alert',
+                'status'  => 'alert',
                 'message' => 'Pendência Financeira com o Clube.',
-                'player' => $user
+                'player'  => $user
             ]);
         }
 
         return response()->json([
-            'status' => 'authorized',
+            'status'  => 'authorized',
             'message' => 'Jogador APTO para a partida.',
-            'player' => $user
+            'player'  => $user
         ]);
     }
 }
diff --git a/app/Http/Controllers/TournamentManagerController.php b/app/Http/Controllers/TournamentManagerController.php
index e86aa54..63165c4 100644
--- a/app/Http/Controllers/TournamentManagerController.php
+++ b/app/Http/Controllers/TournamentManagerController.php
@@ -3,53 +3,75 @@
 namespace App\Http\Controllers;
 
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
 use App\Models\Championship;
 use App\Models\Category;
 
 class TournamentManagerController extends Controller
 {
-    // Listar todos (Admin)
-    public function index()
+    /**
+     * List all championships.
+     */
+    public function index(): JsonResponse
     {
-        return response()->json(Championship::with('club')->orderBy('created_at', 'desc')->get());
+        $championships = Championship::with('club:id,name,logo_path')
+            ->orderBy('created_at', 'desc')
+            ->get();
+
+        return response()->json($championships);
     }
 
-    // Criar Novo
-    public function store(Request $request)
+    /**
+     * Create a new championship.
+     */
+    public function store(Request $request): JsonResponse
     {
         $validated = $request->validate([
-            'club_id' => 'required|exists:clubs,id',
-            'sport_id' => 'required|exists:sports,id',
-            'name' => 'required|string',
+            'club_id'    => 'required|exists:clubs,id',
+            'sport_id'   => 'required|exists:sports,id',
+            'name'       => 'required|string|max:255',
             'start_date' => 'required|date',
-            'end_date' => 'required|date',
+            'end_date'   => 'required|date',
         ]);
 
         $champ = Championship::create($validated);
         return response()->json($champ, 201);
     }
 
-    // Atualizar
-    public function update(Request $request, $id)
+    /**
+     * Update an existing championship.
+     */
+    public function update(Request $request, int $id): JsonResponse
     {
         $champ = Championship::findOrFail($id);
-        $champ->update($request->all());
+        
+        $validated = $request->validate([
+            'club_id'    => 'sometimes|exists:clubs,id',
+            'sport_id'   => 'sometimes|exists:sports,id',
+            'name'       => 'sometimes|string|max:255',
+            'start_date' => 'sometimes|date',
+            'end_date'   => 'sometimes|date',
+            'status'     => 'sometimes|string',
+        ]);
+
+        $champ->update($validated);
         return response()->json($champ);
     }
 
-    // Adicionar Categoria
-    public function addCategory(Request $request, $id)
+    /**
+     * Add a category to the championship.
+     */
+    public function addCategory(Request $request, int $id): JsonResponse
     {
         $validated = $request->validate([
-            'name' => 'required|string',
-            'price' => 'numeric',
+            'name'   => 'required|string|max:255',
+            'price'  => 'nullable|numeric|min:0',
             'gender' => 'required|in:M,F,MISTO'
         ]);
 
-        $category = Category::create([
-            'championship_id' => $id,
-            ...$validated
-        ]);
+        $category = Category::create(array_merge([
+            'championship_id' => $id
+        ], $validated));
 
         return response()->json($category, 201);
     }
diff --git a/app/Http/Controllers/WalletController.php b/app/Http/Controllers/WalletController.php
index fc4b140..d525e30 100644
--- a/app/Http/Controllers/WalletController.php
+++ b/app/Http/Controllers/WalletController.php
@@ -3,29 +3,31 @@
 namespace App\Http\Controllers;
 
 use Illuminate\Http\Request;
+use Illuminate\Http\JsonResponse;
 
 class WalletController extends Controller
 {
-    // Obter dados da Carteirinha Digital
-    public function getWallet(Request $request)
+    /**
+     * Retrieve current user's digital wallet card.
+     */
+    public function getWallet(Request $request): JsonResponse
     {
         $user = $request->user();
 
-        // QR Code Data (Ex: JSON com ID e Hash de segurança)
         $qrData = json_encode([
-            'id' => $user->id,
-            'email' => $user->email,
+            'id'        => $user->id,
+            'email'     => $user->email,
             'timestamp' => now()->timestamp,
-            'valid' => true
+            'valid'     => true
         ]);
 
         return response()->json([
-            'user_name' => $user->name,
-            'user_id' => $user->id,
+            'user_name'       => $user->name,
+            'user_id'         => $user->id,
             'qr_code_content' => $qrData,
-            'club_name' => 'Clube Toledão', // Idealmente viria de user->club
-            'status' => 'Ativo',
-            'expires_at' => '31/12/2026'
+            'club_name'       => 'Clube Toledão',
+            'status'          => 'Ativo',
+            'expires_at'      => '31/12/2026'
         ]);
     }
 }
diff --git a/database/factories/ChampionshipFactory.php b/database/factories/ChampionshipFactory.php
index 4894959..0282c49 100644
--- a/database/factories/ChampionshipFactory.php
+++ b/database/factories/ChampionshipFactory.php
@@ -15,13 +15,11 @@ public function definition(): array
 
         return [
             'name' => $name,
-            'slug' => Str::slug($name),
             'club_id' => Club::factory(),
             'sport_id' => Sport::factory(),
             'start_date' => now()->addDays(10),
             'end_date' => now()->addDays(30),
-            'status' => 'inscricoes_abertas',
-            'max_teams_per_category' => 16,
+            'status' => 'upcoming',
         ];
     }
 }
diff --git a/database/factories/SportFactory.php b/database/factories/SportFactory.php
index 7874c8a..041c21e 100644
--- a/database/factories/SportFactory.php
+++ b/database/factories/SportFactory.php
@@ -8,9 +8,11 @@ class SportFactory extends Factory
 {
     public function definition(): array
     {
+        $name = fake()->randomElement(['Futebol', 'Vôlei', 'Basquete', 'Futsal', 'Corrida']);
         return [
-            'name' => fake()->randomElement(['Futebol', 'Vôlei', 'Basquete', 'Futsal', 'Corrida']),
-            'icon' => 'fa-futbol',
+            'name' => $name,
+            'slug' => \Illuminate\Support\Str::slug($name),
+            'category_type' => fake()->randomElement(['team', 'racing', 'match']),
         ];
     }
 }
diff --git a/database/migrations/2026_03_03_212803_add_captain_id_to_championship_team.php b/database/migrations/2026_03_03_212803_add_captain_id_to_championship_team.php
index 5a1063f..f857eac 100644
--- a/database/migrations/2026_03_03_212803_add_captain_id_to_championship_team.php
+++ b/database/migrations/2026_03_03_212803_add_captain_id_to_championship_team.php
@@ -14,10 +14,13 @@ public function up(): void
             $table->unsignedBigInteger('captain_id')->nullable()->after('category_id');
         });
 
-        // Set existing records to the team's main captain to keep consistency
-        DB::statement('UPDATE championship_team 
-                       JOIN teams ON teams.id = championship_team.team_id 
-                       SET championship_team.captain_id = teams.captain_id');
+        if (DB::getDriverName() === 'sqlite') {
+            DB::statement('UPDATE championship_team SET captain_id = (SELECT captain_id FROM teams WHERE teams.id = championship_team.team_id)');
+        } else {
+            DB::statement('UPDATE championship_team 
+                           JOIN teams ON teams.id = championship_team.team_id 
+                           SET championship_team.captain_id = teams.captain_id');
+        }
 
         Schema::table('championship_team', function (Blueprint $table) {
             $table->foreign('captain_id')->references('id')->on('users')->onDelete('set null');
diff --git a/database/migrations/2026_07_09_000000_update_mvp_votes_table.php b/database/migrations/2026_07_09_000000_update_mvp_votes_table.php
index 4b918bb..b50697e 100644
--- a/database/migrations/2026_07_09_000000_update_mvp_votes_table.php
+++ b/database/migrations/2026_07_09_000000_update_mvp_votes_table.php
@@ -11,6 +11,15 @@
      */
     public function up(): void
     {
+        if (DB::getDriverName() === 'sqlite') {
+            Schema::table('mvp_votes', function (Blueprint $table) {
+                $table->unsignedBigInteger('voter_user_id')->nullable()->change();
+                $table->string('voter_type')->default('public');
+                $table->string('ip_address', 45)->nullable();
+            });
+            return;
+        }
+
         // 1. Drop foreign keys if they exist (needed because MySQL requires an index on foreign key columns,
         // and the unique constraint we are dropping might be satisfying that requirement for game_match_id)
         $fks = ['mvp_votes_game_match_id_foreign', 'mvp_votes_voter_user_id_foreign'];
@@ -62,6 +71,14 @@ public function up(): void
      */
     public function down(): void
     {
+        if (DB::getDriverName() === 'sqlite') {
+            Schema::table('mvp_votes', function (Blueprint $table) {
+                $table->dropColumn(['voter_type', 'ip_address']);
+                $table->unsignedBigInteger('voter_user_id')->nullable(false)->change();
+            });
+            return;
+        }
+
         // 1. Drop foreign keys if they exist
         $fks = ['mvp_votes_game_match_id_foreign', 'mvp_votes_voter_user_id_foreign'];
         foreach ($fks as $fk) {
diff --git a/tests/Feature/AuthTest.php b/tests/Feature/AuthTest.php
index f38810d..252e3e5 100644
--- a/tests/Feature/AuthTest.php
+++ b/tests/Feature/AuthTest.php
@@ -32,8 +32,8 @@ public function test_user_can_register_with_full_data(): void
             'rg' => '123456789',
             'mother_name' => 'Maria Silva',
             'document_number' => '987654321',
-            'photo' => UploadedFile::fake()->create('perfil.txt', 100),
-            'document' => UploadedFile::fake()->create('documento.txt', 100),
+            'photos' => [UploadedFile::fake()->image('perfil.jpg')],
+            'document' => UploadedFile::fake()->image('documento.jpg'),
         ]);
 
         $response->assertStatus(201)
diff --git a/tests/Feature/BracketTest.php b/tests/Feature/BracketTest.php
index ee271c2..9b91fc4 100644
--- a/tests/Feature/BracketTest.php
+++ b/tests/Feature/BracketTest.php
@@ -38,11 +38,11 @@ public function test_bracket_generator_creates_correct_number_of_matches_for_8_t
             $matches[] = GameMatch::create([
                 'championship_id' => $championship->id,
                 'category_id' => $category->id,
-                'team_a_id' => $teams[$i * 2]->id,
-                'team_b_id' => $teams[$i * 2 + 1]->id,
-                'scheduled_date' => now()->addDays(7),
-                'phase' => 'quartas',
-                'status' => 'agendada',
+                'home_team_id' => $teams[$i * 2]->id,
+                'away_team_id' => $teams[$i * 2 + 1]->id,
+                'start_time' => now()->addDays(7),
+                'round_name' => 'quartas',
+                'status' => 'scheduled',
             ]);
         }
 
@@ -68,16 +68,17 @@ public function test_winner_advances_to_next_phase(): void
         $quarterfinal = GameMatch::create([
             'championship_id' => $championship->id,
             'category_id' => $category->id,
-            'team_a_id' => $teamA->id,
-            'team_b_id' => $teamB->id,
-            'phase' => 'quartas',
-            'status' => 'finalizada',
-            'score_team_a' => 3,
-            'score_team_b' => 1,
+            'home_team_id' => $teamA->id,
+            'away_team_id' => $teamB->id,
+            'round_name' => 'quartas',
+            'status' => 'finished',
+            'home_score' => 3,
+            'away_score' => 1,
+            'start_time' => now(),
         ]);
 
         // Time A venceu, então deve avançar para Semifinal
-        $winner = $quarterfinal->score_team_a > $quarterfinal->score_team_b
+        $winner = $quarterfinal->home_score > $quarterfinal->away_score
             ? $teamA
             : $teamB;
 
@@ -87,16 +88,17 @@ public function test_winner_advances_to_next_phase(): void
         $semifinal = GameMatch::create([
             'championship_id' => $championship->id,
             'category_id' => $category->id,
-            'team_a_id' => $winner->id,
-            'team_b_id' => Team::factory()->create()->id,
-            'phase' => 'semi',
-            'status' => 'agendada',
+            'home_team_id' => $winner->id,
+            'away_team_id' => Team::factory()->create()->id,
+            'round_name' => 'semi',
+            'status' => 'scheduled',
+            'start_time' => now()->addDays(1),
         ]);
 
         $this->assertDatabaseHas('game_matches', [
             'id' => $semifinal->id,
-            'phase' => 'semi',
-            'team_a_id' => $teamA->id,
+            'round_name' => 'semi',
+            'home_team_id' => $teamA->id,
         ]);
     }
 }
diff --git a/tests/Feature/BulkInscriptionTest.php b/tests/Feature/BulkInscriptionTest.php
index 50a924c..b871962 100644
--- a/tests/Feature/BulkInscriptionTest.php
+++ b/tests/Feature/BulkInscriptionTest.php
@@ -18,7 +18,12 @@ class BulkInscriptionTest extends TestCase
 
     public function test_bulk_registration_applies_correct_discounts_and_creates_single_payment(): void
     {
-        $club = Club::factory()->create();
+        $club = Club::factory()->create([
+            'payment_settings' => [
+                'asaas_token' => 'fake_token_for_tests',
+                'asaas_environment' => 'sandbox'
+            ]
+        ]);
         $sport = Sport::factory()->create();
 
         // Championship with progressive discount settings
@@ -84,6 +89,7 @@ public function test_bulk_registration_applies_correct_discounts_and_creates_sin
             'payment_method' => 'PIX'
         ]);
 
+        $response->dump();
         $response->assertStatus(201);
         $response->assertJsonStructure(['payment_group_id', 'price', 'payment_data']);
 
diff --git a/tests/Feature/ChampionshipTest.php b/tests/Feature/ChampionshipTest.php
index 98cb77b..f4e66f7 100644
--- a/tests/Feature/ChampionshipTest.php
+++ b/tests/Feature/ChampionshipTest.php
@@ -29,12 +29,11 @@ public function test_club_admin_can_create_championship(): void
 
         $championship = Championship::create([
             'name' => 'Campeonato de Verão 2026',
-            'slug' => 'campeonato-verao-2026',
             'club_id' => $club->id,
             'sport_id' => $sport->id,
             'start_date' => now()->addDays(10),
             'end_date' => now()->addDays(30),
-            'status' => 'inscricoes_abertas',
+            'status' => 'upcoming',
         ]);
 
         $this->assertDatabaseHas('championships', [
diff --git a/tests/Feature/CheckoutTest.php b/tests/Feature/CheckoutTest.php
index 68b820a..728ee9d 100644
--- a/tests/Feature/CheckoutTest.php
+++ b/tests/Feature/CheckoutTest.php
@@ -19,15 +19,20 @@ class CheckoutTest extends TestCase
      */
     public function test_checkout_creates_order_successfully(): void
     {
+        $club = Club::factory()->create();
         $user = User::factory()->create();
         $product = Product::factory()->create([
+            'club_id' => $club->id,
             'name' => 'Inscrição Campeonato',
             'price' => 100.00,
         ]);
 
         $order = Order::create([
+            'club_id' => $club->id,
             'user_id' => $user->id,
-            'total' => 100.00,
+            'total_amount' => 100.00,
+            'fee_platform' => 0.00,
+            'net_club' => 100.00,
             'status' => 'pending',
         ]);
 
@@ -40,7 +45,7 @@ public function test_checkout_creates_order_successfully(): void
 
         $this->assertDatabaseHas('orders', [
             'user_id' => $user->id,
-            'total' => 100.00,
+            'total_amount' => 100.00,
         ]);
 
         $this->assertDatabaseHas('order_items', [
@@ -74,15 +79,16 @@ public function test_coupon_applies_correct_discount(): void
         $this->assertEquals(90.00, $finalPrice);
 
         $order = Order::create([
+            'club_id' => $club->id,
             'user_id' => $user->id,
-            'total' => $finalPrice,
+            'total_amount' => $finalPrice,
             'coupon_id' => $coupon->id,
-            'discount' => $discount,
+            'fee_platform' => 0.00,
+            'net_club' => 90.00,
         ]);
 
         $this->assertDatabaseHas('orders', [
-            'total' => 90.00,
-            'discount' => 10.00,
+            'total_amount' => 90.00,
             'coupon_id' => $coupon->id,
         ]);
     }
